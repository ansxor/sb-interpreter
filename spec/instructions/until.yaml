# AUTO-GENERATED from sb-docs/smilebasic-3/until.md by tools/gen_specs.py — do not edit.
# Verified tests + oracle-harvested expects live in spec/tests/ (merged by sb-spec).
id: "UNTIL"
kind: statement
category: "Basic instructions (control and branching)"
system: "SmileBASIC 3"
summary: "Repeats the process from REPEAT until the conditional expression is satisfied"
semantics:
  - "The REPEAT instruction should be placed at the beginning of the loop"
  - "Unlike the WHILE instruction, this executes the process before determining the condition"
  - "Exits the loop if the condition is satisfied or when the BREAK instruction is executed"
forms:
  - format: "UNTIL conditional expression"
    description: "Repeats the process from REPEAT until the conditional expression is satisfied"
    args: []
    examples:
      - "A=0:B=4\nREPEAT\n A=A+1\nUNTIL A<B"
sources:
  - { type: documented, ref: "sb-docs/smilebasic-3/until.md" }
confidence: documented
