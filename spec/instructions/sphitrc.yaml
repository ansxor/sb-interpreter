# AUTO-GENERATED from sb-docs/smilebasic-3/sphitrc.md by tools/gen_specs.py — do not edit.
# Verified tests + oracle-harvested expects live in spec/tests/ (merged by sb-spec).
id: "SPHITRC"
kind: statement
category: "Sprites"
system: "SmileBASIC 3"
summary: "Detects collision between a moving quadrangle and any sprite"
semantics:
  - "SPCOL and SPCOLVEC should be called beforehand"
  - "If used before SPSET, an error will occur"
forms:
  - format: "SPHITRC( Start point X,Start point Y,Width,Height[,[Mask],Movement amount X,Movement amount Y] )"
    description: "Detects collision between a moving quadrangle and any sprite"
    args:
      - { name: "Start point X,Y", desc: "Top left coordinates of the quadrangle to detect collision with" }
      - { name: "Width,Height", desc: "Width and height of the quadrangle to detect collision with" }
      - { name: "Mask", desc: "0 - &HFFFFFFFF (32 bits)<br>* For collision detection, the AND of the bits is determined,<br>and if it is 0, it is regarded as no collision (If omitted, &HFFFFFFFF)." }
      - { name: "Movement amount<br>X,Y", desc: "Movement amount of the quadrangle to detect collision with" }
    examples:
      - "H=SPHITRC( 0,0,16,16 )"
  - format: "SPHITRC( Management number,Start point X,Start point Y,Width,Height[,[Mask],Movement amount X,Movement amount Y] )"
    description: "Detects collision between the specified sprite and a quadrangle"
    args:
      - { name: "Management number", desc: "Management number of the collision opponent sprite: 0-511" }
      - { name: "Start point X,Y", desc: "Top left coordinates of the quadrangle to detect collision with" }
      - { name: "Width,Height", desc: "Width and height of the quadrangle to detect collision with" }
      - { name: "Mask", desc: "0 - &HFFFFFFFF (32 bits)<br>* For collision detection, the AND of the bits is determined,<br>and if it is 0, it is regarded as no collision (If omitted, &HFFFFFFFF)." }
      - { name: "Movement amount<br>X,Y", desc: "Movement amount of the quadrangle to detect collision with" }
    examples:
      - "H=SPHITRC( 1,0,0,16,16 )"
  - format: "SPHITRC( First ID,Last ID, Start point x,Start point y,Width,Height[,[Mask],Movement amount X, Movement amount Y]\n)"
    description: "Detects collision between the specified range of sprites and a quadrangle"
    args:
      - { name: "First ID,Last ID", desc: "Range of sprites to detect (0-511)" }
      - { name: "Start point X,Y", desc: "Top left coordinates of the quadrangle to detect collision with" }
      - { name: "Width,Height", desc: "Width and height of the quadrangle to detect collision with" }
      - { name: "Mask", desc: "0 - &HFFFFFFFF (32 bits)<br>* For collision detection, the AND of the bits is determined,<br>and if it is 0, it is regarded as no collision (If omitted, &HFFFFFFFF)." }
      - { name: "Movement amount<br>X,Y", desc: "Movement amount of the quadrangle to detect collision with" }
    examples:
      - "H=SPHITRC( 0,0,16,16 )"
sources:
  - { type: documented, ref: "sb-docs/smilebasic-3/sphitrc.md" }
confidence: documented
