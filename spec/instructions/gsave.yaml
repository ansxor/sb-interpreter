# AUTO-GENERATED from sb-docs/smilebasic-3/gsave.md by tools/gen_specs.py — do not edit.
# Verified tests + oracle-harvested expects live in spec/tests/ (merged by sb-spec).
id: "GSAVE"
kind: statement
category: "Graphics"
system: "SmileBASIC 3"
summary: "Copies an image (whole screen) to an array"
semantics: []
forms:
  - format: "GSAVE [Transfer source page,] [X,Y,Width,Height,] Transfer destination array, Color conversion flag"
    description: "Copies an image (whole screen) to an array"
    args:
      - { name: "Transfer source<br>page", desc: "0-5 (GRP0-GRP5), -1(GRPF) If omitted: Current drawing page" }
      - { name: "X,Y,Width,Height", desc: "Start point X-coordinate, start point Y-coordinate, and width/height (in pixels) of the copy<br>source range<br>If omitted: Current drawing area" }
      - { name: "Transfer<br>destination array", desc: "Array variable to store the image<br>* If the number of elements in the array is insufficient, the required element(s) will be<br>added automatically, provided that the array is one-dimensional." }
      - { name: "Color conversion<br>flag", desc: "0: Performs color conversion (Converts to 32-bit logical colors)<br>1: Leaves the physical codes as they are (16-bit)" }
    examples:
      - "DIM WORK[0]\nGSAVE 0,0,0,512,512,WORK,1"
sources:
  - { type: documented, ref: "sb-docs/smilebasic-3/gsave.md" }
confidence: documented
