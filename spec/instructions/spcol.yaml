# AUTO-GENERATED from sb-docs/smilebasic-3/spcol.md by tools/gen_specs.py — do not edit.
# Verified tests + oracle-harvested expects live in spec/tests/ (merged by sb-spec).
id: "SPCOL"
kind: statement
category: "Sprites"
system: "SmileBASIC 3"
summary: "Sets sprite collision detection information"
semantics:
  - "Must be called before any SPHIT instruction is used"
  - "SPCOLVEC should also be called"
  - "If used before SPSET, an error will occur"
forms:
  - format: "SPCOL Management number [,Scale adjustment]"
    description: "Sets sprite collision detection information"
    args:
      - { name: "Management number", desc: "Management number of the target sprite: 0-511" }
      - { name: "Scale adjustment", desc: "FALSE = Ignores this specification (If omitted = FALSE)<br>TRUE = Synchronizes the detection area with SPSCALE<br>* This specification will be effective for SPSCALE instructions set after the SPCOL<br>instruction." }
    examples:
      - "SPCOL 3,TRUE"
  - format: "SPCOL Management number,[Scale adjustment],Mask"
    description: "Sets sprite collision detection information (including mask specification)"
    args:
      - { name: "Management number", desc: "Management number of the target sprite: 0-511" }
      - { name: "Scale adjustment", desc: "FALSE = Ignores this specification (If omitted = FALSE)<br>TRUE = Synchronizes the detection area with SPSCALE<br>* This specification will be effective for SPSCALE instructions set after the SPCOL<br>instruction." }
      - { name: "Mask", desc: "0 - &HFFFFFFFF (32 bits)<br>* For collision detection, the AND of the bits is determined,<br>and if it is 0, it is regarded as no collision (If omitted, &HFFFFFFFF)." }
    examples:
      - "SPCOL 3,TRUE,31\nSPCOL 3,,31"
  - format: "SPCOL Management number,Start point X,Start point Y,Width,Height,[Scale adjustment],Mask"
    description: "Sets sprite collision detection information (including range specification)"
    args:
      - { name: "Management number", desc: "Management number of the target sprite: 0-511" }
      - { name: "Start point X,Y", desc: "- Start point coordinates of the detection area: X,Y (-32768 to 32767)<br>- Relative coordinates with SPHOME as the origin (0,0)" }
      - { name: "Width,Height", desc: "Width and height of the detection area: W,H (0-65535)" }
      - { name: "Scale adjustment", desc: "FALSE = Ignores this specification (If omitted = FALSE)<br>TRUE = Synchronizes the detection area with SPSCALE<br>* This specification will be effective for SPSCALE instructions set after the SPCOL<br>instruction." }
      - { name: "Mask", desc: "0 - &HFFFFFFFF (32 bits)<br>* For collision detection, the AND of the bits is determined,<br>and if it is 0, it is regarded as no collision (If omitted, &HFFFFFFFF)." }
    examples:
      - "SPCOL 3,0,0,32,32,TRUE,255\nSPCOL 3,0,0,32,32,,255"
  - format: "SPCOL Management number OUT Scale adjustment [,Mask]"
    description: "Gets sprite collision detection information (scale adjustment and mask) If used before SPSET, an error will occur"
    args:
      - { name: "Management number", desc: "Management number of the target sprite: 0-511" }
    returns:
      - { name: "Scale adjustment", desc: "Variable to receive the scale value" }
      - { name: "Mask", desc: "Variable to receive the mask value" }
    examples:
      - "SPCOL 3 OUT SC,MSK"
  - format: "SPCOL Management number OUT Start point X,Start point Y,Width,Height"
    description: "Gets sprite collision detection information (range) If used before SPSET, an error will occur"
    args:
      - { name: "Management number", desc: "Management number of the target sprite: 0-511" }
    returns:
      - { name: "Start point X,Y", desc: "Variables to receive the start point coordinates of the detection area" }
      - { name: "Width,Height", desc: "Variables to receive the width and height of the detection area" }
    examples:
      - "SPCOL 3 OUT X,Y,W,H"
  - format: "SPCOL Management number OUT Start point X,Start point Y,Width,Height,Scale adjustment"
    description: "Gets sprite collision detection information (range and scale adjustment) If used before SPSET, an error will occur"
    args:
      - { name: "Management number", desc: "Management number of the target sprite: 0-511" }
    returns:
      - { name: "Start point X,Y", desc: "Variables to receive the start point coordinates of the detection area" }
      - { name: "Width,Height", desc: "Variables to receive the width and height of the detection area" }
      - { name: "Scale adjustment", desc: "Variable to receive the scale value" }
    examples:
      - "SPCOL 3 OUT X,Y,W,H,SC"
  - format: "SPCOL Management number OUT Start point X,Start point Y,Width,Height,Scale adjustment,Mask"
    description: "Gets sprite collision detection information (all information) If used before SPSET, an error will occur"
    args:
      - { name: "Management number", desc: "Management number of the target sprite: 0-511" }
    returns:
      - { name: "Start point X,Y", desc: "Variables to receive the start point coordinates of the detection area" }
      - { name: "Width,Height", desc: "Variables to receive the width and height of the detection area" }
      - { name: "Scale adjustment", desc: "Variable to receive the scale value" }
      - { name: "Mask", desc: "Variable to receive the mask value" }
    examples:
      - "SPCOL 3 OUT X,Y,W,H,SC,MSK"
sources:
  - { type: documented, ref: "sb-docs/smilebasic-3/spcol.md" }
confidence: documented
