# AUTO-GENERATED from sb-docs/smilebasic-3/sphitinfo.md by tools/gen_specs.py — do not edit.
# Verified tests + oracle-harvested expects live in spec/tests/ (merged by sb-spec).
id: "SPHITINFO"
kind: statement
category: "Sprites"
system: "SmileBASIC 3"
summary: "Gets information on collision detection results (Time of collision) If used before SPSET, an error will occur"
semantics: []
forms:
  - format: "SPHITINFO OUT TM"
    description: "Gets information on collision detection results (Time of collision) If used before SPSET, an error will occur"
    args:
      - { name: "None", desc: "" }
    returns:
      - { name: "TM", desc: "- Variable that returns time of collision: real-type number from 0 to 1<br>- Position at collision detection + speed x collision time = collision X-Y coordinates" }
    examples:
      - "SPHITINFO OUT TM"
  - format: "SPHITINFO OUT TM,X1,Y1,X2,Y2"
    description: "Gets information on collision detection results (Time of collision and coordinates) If used before SPSET, an error will occur"
    args:
      - { name: "None", desc: "" }
    returns:
      - { name: "TM", desc: "- Variable that returns time of collision: real-type number from 0 to 1<br>- Position at collision detection + speed x collision time = collision X-Y coordinates" }
      - { name: "X1,Y1", desc: "Variable that returns the X-Y coordinates of object 1 at the time of collision" }
      - { name: "X2,Y2", desc: "Variable that returns the X-Y coordinates of object 2 at the time of collision" }
    examples:
      - "SPHITINFO OUT TM,X1,Y1,X2,Y2"
  - format: "SPHITINFO OUT TM,X1,Y1,VX1,VY1,X2,Y2,VX2,VY2"
    description: "Gets information on collision detection results (Time of collision, coordinates and speed) If used before SPSET, an error will occur"
    args:
      - { name: "None", desc: "" }
    returns:
      - { name: "Time of collision", desc: "- Variable that returns time of collision: real-type number from 0 to 1<br>- Position at collision detection + speed x collision time = collision X-Y coordinates" }
      - { name: "X1,Y1", desc: "Variable that returns the X-Y coordinates of object 1 at the time of collision" }
      - { name: "VX1,VY1", desc: "Variable that returns the speed of object 1 at the time of collision" }
      - { name: "X2,Y2", desc: "Variable that returns the X-Y coordinates of object 2 at the time of collision" }
      - { name: "VX2,VY2", desc: "Variable that returns the speed of object 2 at the time of collision" }
    examples:
      - "SPHITINFO OUT TM,X1,Y1,VX1,VY1,X2,Y2,VX2,VY2"
sources:
  - { type: documented, ref: "sb-docs/smilebasic-3/sphitinfo.md" }
confidence: documented
