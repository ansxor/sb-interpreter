# AUTO-GENERATED from sb-docs/smilebasic-3/spchr.md by tools/gen_specs.py — do not edit.
# Verified tests + oracle-harvested expects live in spec/tests/ (merged by sb-spec).
id: "SPCHR"
kind: statement
category: "Sprites"
system: "SmileBASIC 3"
summary: "Changes the character definition of a sprite (using the specified template) If used before SPSET, an error will occur"
semantics:
  - "Arguments other than the management number can be omitted"
  - "If used before SPSET, an error will occur"
forms:
  - format: "SPCHR Management number, Definition number"
    description: "Changes the character definition of a sprite (using the specified template) If used before SPSET, an error will occur"
    args:
      - { name: "Management number", desc: "Management number of the sprite to change the definition of: 0-511" }
      - { name: "Definition number", desc: "Definition number of the template registered using the SPDEF instruction: 0-4095" }
    examples:
      - "SPCHR 0,500"
  - format: "SPCHR Management number,[U],[V],[W],[H],[Attribute]"
    description: "Changes the character definition of a sprite (using a definition specified directly)"
    args:
      - { name: "Management number", desc: "Management number of the target sprite: 0-511" }
      - { name: "U,V", desc: "Coordinates of the original image to define (U: 0-511, V: 0-511)" }
      - { name: "W,H", desc: "Size of the original image to define (If omitted: 16,16)<br>* U+W and/or V+H values bigger than 512 will give an error" }
      - { name: "Attribute", desc: "|b00| Display (0=OFF, 1=ON) #SPSHOW<br>|b01|<br>↑<br>Rotation by 90 degrees (Specified with two bits: b01 and b02)<br>↓<br>|b02|<br>#SPROT0, #SPROT90, #SPROT0180, #SPROT270<br>|b03| Horizontal inversion (0=OFF, 1=ON), #SPREVH<br>|b04| Vertical inversion (0=OFF, 1=ON), #SPREVV<br>|b05| Additive synthesis (0=OFF, 1=ON), #SPADD<br>If omitted, 0x01 (Only display is set to ON)" }
    examples:
      - "SPCHR 5,64,64,16,16,1\nSPCHR 6,,,32,32,1 'UV skip"
  - format: "SPCHR Management number OUT U,V [,W,H [,A] ]"
    description: "Gets information on the character definition of a sprite If used before SPSET, an error will occur"
    args:
      - { name: "Management number", desc: "Management number of the target sprite: 0-511" }
    returns:
      - { name: "U,V", desc: "Variables to store the coordinates of the original image" }
      - { name: "W,H", desc: "Variables to store the size of the original image" }
      - { name: "A", desc: "Variables to store the attribute" }
    examples:
      - "SPCHR 5 OUT U,V,W,H,ATR"
  - format: "SPCHR Management number OUT DEFNO"
    description: "Gets the character definition number of a sprite If used before SPSET, an error will occur"
    args:
      - { name: "Management number", desc: "Management number of the target sprite: 0-511" }
    returns:
      - { name: "DEFNO", desc: "Variable to receive the definition number" }
    examples:
      - "SPCHR 5 OUT DEFNO"
sources:
  - { type: documented, ref: "sb-docs/smilebasic-3/spchr.md" }
confidence: documented
