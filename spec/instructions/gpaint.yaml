# AUTO-GENERATED from sb-docs/smilebasic-3/gpaint.md by tools/gen_specs.py — do not edit.
# Verified tests + oracle-harvested expects live in spec/tests/ (merged by sb-spec).
id: "GPAINT"
kind: statement
category: "Graphics"
system: "SmileBASIC 3"
summary: "Fills the graphic screen with color If the border color is omitted, the color range at the start point coordinates will be used"
semantics: []
forms:
  - format: "GPAINT Start point X, Start point Y [ ,Fill Color [, Border color ] ]"
    description: "Fills the graphic screen with color If the border color is omitted, the color range at the start point coordinates will be used"
    args:
      - { name: "Start point X,Y", desc: "Coordinates to start filling from (X: 0-399, Y: 0-239)" }
      - { name: "Fill color", desc: "Color code consisting of an 8-bit value for each ARGB element * See GCOLOR" }
      - { name: "Border color", desc: "Should be specified in the same way as Fill color" }
    examples:
      - "GPAINT 200,120,RGB(255,0,0),RGB(0,0,0)"
sources:
  - { type: documented, ref: "sb-docs/smilebasic-3/gpaint.md" }
confidence: documented
