# AUTO-GENERATED from sb-docs/smilebasic-3/mprecv.md by tools/gen_specs.py — do not edit.
# Verified tests + oracle-harvested expects live in spec/tests/ (merged by sb-spec).
id: "MPRECV"
kind: statement
category: "Wireless communication"
system: "SmileBASIC 3"
summary: "Receives data from MPSEND"
semantics:
  - "If there is no data to receive, the sender ID will contain the value -1"
  - "Communication will be terminated if the system goes into sleep mode"
forms:
  - format: "MPRECV OUT SID,RCV$"
    description: "Receives data from MPSEND"
    args:
      - { name: "SID", desc: "0-3: Connection destination number from which the string will be sent" }
      - { name: "RCV$", desc: "String variable to store the received data" }
    examples:
      - "MPRECV OUT SID,RCV$\nPRINT SID;\":\";RCV$"
sources:
  - { type: documented, ref: "sb-docs/smilebasic-3/mprecv.md" }
confidence: documented
