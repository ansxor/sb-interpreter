# AUTO-GENERATED from sb-docs/smilebasic-3/bgfunc.md by tools/gen_specs.py — do not edit.
# Verified tests + oracle-harvested expects live in spec/tests/ (merged by sb-spec).
id: "BGFUNC"
kind: statement
category: "BG"
system: "SmileBASIC 3"
summary: "Assigns a callback process to a BG layer"
semantics:
  - "An instruction for advanced users that is used when callback processing is required"
  - "All BG layer processes are executed with CALL BG"
  - "Instead of @Label, a user process defined using DEF can also be specified"
  - "At the processing target, a management number can be obtained using a CALLIDX system variable"
forms:
  - format: "BGFUNC Layer, @Label"
    description: "Assigns a callback process to a BG layer"
    args:
      - { name: "Layer", desc: "Layer number: 0-3" }
      - { name: "@Label", desc: "The label of the process target (or a user-defined process)" }
    examples:
      - "BGFUNC 0,@LAYERSUB0"
sources:
  - { type: documented, ref: "sb-docs/smilebasic-3/bgfunc.md" }
confidence: documented
