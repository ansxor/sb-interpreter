# AUTO-GENERATED from sb-docs/smilebasic-3/input.md by tools/gen_specs.py — do not edit.
# Verified tests + oracle-harvested expects live in spec/tests/ (merged by sb-spec).
id: "INPUT"
kind: statement
category: "Console input/output"
system: "SmileBASIC 3"
summary: "Inputs numerical values or character strings from the keyboard"
semantics:
  - "Waits for input until the ENTER key is input"
  - "If the number of input items is insufficient, \"?Redo from start\" will be displayed for re-input"
forms:
  - format: "INPUT [\"Guiding text string\";] Variable[,Variable 2 …\n]"
    description: "Inputs numerical values or character strings from the keyboard"
    args:
      - { name: "Guiding text<br>string", desc: "- Guidance message for input (Optional)<br>- If , (comma) is used instead of ; after the guiding text string, a ? mark will not be<br>displayed<br>- Only when ; is used, a string variable can be used for the guiding text string" }
      - { name: "Variables", desc: "- Variables to receive the input (numerical values or string variables)<br>- When specifying multiple variables, they should be delimited with commas (,)" }
    examples:
      - "INPUT \"Your name and age\";NM$,AG"
sources:
  - { type: documented, ref: "sb-docs/smilebasic-3/input.md" }
confidence: documented
