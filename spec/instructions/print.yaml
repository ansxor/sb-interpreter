# AUTO-GENERATED from sb-docs/smilebasic-3/print.md by tools/gen_specs.py — do not edit.
# Verified tests + oracle-harvested expects live in spec/tests/ (merged by sb-spec).
id: "PRINT"
kind: statement
category: "Console input/output"
system: "SmileBASIC 3"
summary: "Displays characters on the console screen"
semantics:
  - "Omitting expressions causes only a line break to occur"
  - "? can be used instead of PRINT"
forms:
  - format: "PRINT [Expression [; or, Expression …\n]]"
    description: "Displays characters on the console screen"
    args:
      - { name: "Expression", desc: "- Variables, string variables, numerical values, or character strings to display<br>- Formulas are also allowed, including the four arithmetic operations, and functional<br>calculations (The calculation results will be displayed)" }
      - { name: "; (semicolon)", desc: "Without beginning a new line after the previous display item, displays the next display item<br>without any space" }
      - { name: ", (comma)", desc: "- Without beginning a new line after the previous display item, places a set interval before<br>the next display item<br>- The display location is determined according to a system variable (the TABSTEP unit)" }
    examples:
      - "PRINT \"RESULT(X,Y)=\";DX*4+1,DY+1"
sources:
  - { type: documented, ref: "sb-docs/smilebasic-3/print.md" }
confidence: documented
