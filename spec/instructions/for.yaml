# AUTO-GENERATED from sb-docs/smilebasic-3/for.md by tools/gen_specs.py — do not edit.
# Verified tests + oracle-harvested expects live in spec/tests/ (merged by sb-spec).
id: "FOR"
kind: statement
category: "Basic instructions (control and branching)"
system: "SmileBASIC 3"
summary: "Repeats the process for the specified number of times"
semantics:
  - "The NEXT instruction should be placed at the end of the process"
  - "If the condition is not satisfied, the process may not be executed at all"
forms:
  - format: "FOR Loop variable=Initial value TO End value [STEP Increment]"
    description: "Repeats the process for the specified number of times"
    args:
      - { name: "Loop variable", desc: "Variable for loop count (On each iteration of the loop, the increment is added to the count)" }
      - { name: "Initial value", desc: "Value or expression for the loop variable at the start of the loop" }
      - { name: "TO End value", desc: "Value or expression for the loop variable at the end of the loop" }
      - { name: "STEP Increment", desc: "- Increment added to the loop variable at the end of the loop (If omitted, 1)<br>- If the increment is specified as a fractional value, the intended loop count may not be<br>achieved due to operational errors." }
    examples:
      - "FOR I=0 TO 9 STEP 2\n PRINT I;\",\";\nNEXT"
sources:
  - { type: documented, ref: "sb-docs/smilebasic-3/for.md" }
confidence: documented
