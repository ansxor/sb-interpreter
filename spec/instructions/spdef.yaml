# AUTO-GENERATED from sb-docs/smilebasic-3/spdef.md by tools/gen_specs.py — do not edit.
# Verified tests + oracle-harvested expects live in spec/tests/ (merged by sb-spec).
id: "SPDEF"
kind: statement
category: "Sprites"
system: "SmileBASIC 3"
summary: "Resets the sprite character definition template to its initial state"
semantics:
  - "Unnecessary elements can be omitted (Separating commas (',') are required)"
  - "Arguments are used to adjust the copied template"
forms:
  - format: "SPDEF"
    description: "Resets the sprite character definition template to its initial state"
    args:
      - { name: "None", desc: "" }
    examples:
      - "SPDEF"
  - format: "SPDEF Definition number, U,V [,W,H [,Origin X,Origin Y]] [,Attribute]"
    description: "Creates a template for sprite character definition"
    args:
      - { name: "Definition number", desc: "Definition number of the template: 0-4095" }
      - { name: "U,V", desc: "Coordinates of the original image to define (U: 0-511, V: 0-511)" }
      - { name: "W,H", desc: "Size of the original image to define If omitted: 16,16<br>* U+W and/or V+H values greater than 512 will give an error." }
      - { name: "Origin X,Y", desc: "Reference point for the coordinates of the sprite If omitted: 0,0" }
      - { name: "Attribute", desc: "|b00| Display (0=OFF, 1=ON) #SPSHOW<br>|b01|<br>↑<br>Rotation by 90 degrees (Specified with two bits: b01 and b02)<br>|b02|<br>↓<br>#SPROT0, #SPROT90, #SPROT0180, #SPROT270<br>|b03| Horizontal inversion (0=OFF, 1=ON), #SPREVH<br>|b04| Vertical inversion (0=OFF, 1=ON), #SPREVV<br>|b05| Additive synthesis (0=OFF, 1=ON), #SPADD<br>If omitted, 0x01 (Only display is set to ON)" }
    examples:
      - "SPDEF 0,192,352,32,32,16,16,1"
  - format: "SPDEF Numerical value array"
    description: "Creates templates for sprite character definition collectively from an array"
    args:
      - { name: "Numerical value<br>array", desc: "Numerical value array containing sprite template data<br>- One sprite template consists of the following 7 elements: U,V,W,H,Origin X,Origin<br>Y,Attribute<br>- The number of elements must be a multiple of 7<br>- A specific number of sprite templates (the number of elements divided by 7) will be defined<br>in order, starting with 0" }
    examples:
      - "SPDEF SRCDATA"
  - format: "SPDEF \"@Label string\""
    description: "Creates templates for sprite character definition collectively from a DATA sequence"
    args:
      - { name: "@Label string", desc: "Label of the DATA instruction that enumerates the sprite template data<br>- The @Label name should be enclosed in \"\" or specified with a string variable<br>- The first data should be the number of sprites to define, followed by enumeration of the<br>data for each sprite (7 data elements per sprite)<br>- One sprite template consists of the following 7 elements: U,V,W,H,Origin X,Origin<br>Y,Attribute" }
    examples:
      - "SPDEF \"@SRCDATA\""
  - format: "SPDEF Definition number OUT U,V [,W,H [,HX,HY]] [,A]"
    description: "Gets information on a sprite character definition template"
    args:
      - { name: "Definition number", desc: "Definition number of the template: 0-4095" }
    returns:
      - { name: "U,V", desc: "Variables to receive the image coordinates" }
      - { name: "W,H", desc: "Variable to receive the image size" }
      - { name: "HX,HY", desc: "Variable to receive the reference point for the sprite coordinates" }
      - { name: "A", desc: "Variable to receive the attribute" }
    examples:
      - "SPDEF 2 OUT U,V,ATR"
  - format: "SPDEF Definition number,Source definition number,[U],[V],[W],[H],[Origin X],[Origin Y],[Attribute]"
    description: "Copies a template for sprite character definition"
    args:
      - { name: "Definition number", desc: "Definition number of the template: 0-4095" }
      - { name: "Source definition<br>number", desc: "Definition number of the copy source: 0-4095" }
      - { name: "U,V", desc: "Coordinates of the original image to define (U: 0-511, V: 0-511)" }
      - { name: "W,H", desc: "Size of the original image to define If omitted: 16,16<br>* U+W and/or V+H values greater than 512 will give an error." }
      - { name: "Origin X,Y", desc: "Reference point for the coordinates of the sprite If omitted: 0,0" }
      - { name: "Attribute", desc: "|b00| Display (0=OFF, 1=ON) #SPSHOW<br>|b01|<br>↑<br>Rotation by 90 degrees (Specified with two bits: b01 and b02)<br>|b02|<br>↓<br>#SPROT0, #SPROT90, #SPROT0180, #SPROT270<br>|b03| Horizontal inversion (0=OFF, 1=ON), #SPREVH<br>|b04| Vertical inversion (0=OFF, 1=ON), #SPREVV<br>|b05| Additive synthesis (0=OFF, 1=ON), #SPADD<br>If omitted, 0x01 (Only display is set to ON)" }
    examples:
      - "SPDEF 0,255,192,352,32,32,16,16,1\nSPDEF 1,255,,,32,32,,,"
sources:
  - { type: documented, ref: "sb-docs/smilebasic-3/spdef.md" }
confidence: documented
