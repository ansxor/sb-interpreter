# AUTO-GENERATED from sb-docs/smilebasic-3/elseif.md by tools/gen_specs.py — do not edit.
# Verified tests + oracle-harvested expects live in spec/tests/ (merged by sb-spec).
id: "ELSEIF"
kind: statement
category: "Basic instructions (control and branching)"
system: "SmileBASIC 3"
summary: "Additional conditional evaluation if the IF condition is not satisfied"
semantics:
  - "Used to evaluate another condition if the IF condition is not satisfied"
  - "See Comment for IF for details regarding conditional evaluation"
forms:
  - format: "IF Conditional expression THEN Process to execute when the condition is satisfied ELSEIF Conditional expression\nTHEN Process to execute when the condition is satisfied ENDIF"
    description: "Additional conditional evaluation if the IF condition is not satisfied"
    args: []
    examples:
      - "IF A==1 THEN\n PRINT \"Congratulations\":BEEP 0\nELSEIF A==2 THEN\n PRINT \"Bad luck\"\nELSE IF A==3 THEN\n PRINT \"So-so\"\nENDIF '--- Required when using ELSE IF\nENDIF"
sources:
  - { type: documented, ref: "sb-docs/smilebasic-3/elseif.md" }
confidence: documented
