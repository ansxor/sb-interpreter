# v2 spec contract — authored from docs + osb cross-check + oracle (prd/specs.md).
id: PI
kind: function
category: Mathematics
version: { introduced: "3.0.0", verified_on: "3.6.0" }

signatures:
  - args: []
    returns: { type: double, desc: "The ratio of a circle's circumference to its diameter" }

summary: "Returns the constant pi (3.14159265358979...) as a Double."

semantics:
  - "Takes no arguments but the parentheses are required: PI()."
  - "Full f64 precision internally; PRINT/STR$ shows 6 significant figures: PI()=3.14159."

sources:
  - { type: documented, ref: "sb-docs/smilebasic-3/pi.md" }
  - { type: hw_verified, ref: "sb-oracle run 2026-06-21: PI()=3.14159", confidence: hw_verified }

confidence: hw_verified

tests:
  - { name: value, code: "PRINT PI()", expect: { stdout: "3.14159" } }
