# AUTO-GENERATED from sb-docs/smilebasic-3/gosub.md by tools/gen_specs.py — do not edit.
# Verified tests + oracle-harvested expects live in spec/tests/ (merged by sb-spec).
id: "GOSUB"
kind: statement
category: "Basic instructions (control and branching)"
system: "SmileBASIC 3"
summary: "Calls a sub-routine"
semantics:
  - "The branch number begins with 0, unlike in conventional BASIC"
forms:
  - format: "GOSUB @Label"
    description: "Calls a sub-routine"
    args:
      - { name: "@Label", desc: "- @Label name of the sub-routine to call<br>- Label string, which is the Label name enclosed in \"\" (String variables are also allowed)<br>- A program SLOT can be specified in the following format: \"1:@Label name\"<br>- The target SLOT should be enabled beforehand with the USE instruction" }
    examples:
      - "GOSUB @SUB"
  - format: "ON Control variable GOSUB @Label 0, @Label 1 …"
    description: "Calls a sub-routine according to the control variable value"
    args:
      - { name: "@Label 0", desc: "Sub-routine when the control variable is 0" }
      - { name: "@Label 1", desc: "Sub-routine when the control variable is 1<br>:<br>- Prepare the necessary number of branch destinations<br>- Label strings cannot be used in the ON to GOSUB labels" }
    examples:
      - "ON IDX GOSUB @SUB_A,@SUB_B\nPRINT \"EXIT\":END\n@SUB_A\nPRINT \"IDX=0\":RETURN\n@SUB_B\nPRINT \"IDX=1\":RETURN"
sources:
  - { type: documented, ref: "sb-docs/smilebasic-3/gosub.md" }
confidence: documented
