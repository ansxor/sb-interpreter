[package]
name = "sb-core"
description = "Faithful SmileBASIC 3.6.0 language core: lexer, parser, compiler, bytecode VM. No I/O, no GUI (wasm-safe)."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
