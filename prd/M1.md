# M1 — Core VM + a real window (PRD)

> Status: ⬜ gated on S · Depends on: M0 · Read `prd/README.md` first. Tasks: `PRD.md` (M1-T*).
> NOTE: `lexer.rs`/`ast.rs` exist from a **pre-spec-first attempt** (osb-port, ASCII-only
> identifiers). Rewrite/validate them spec-first against the disassembly + oracle — do not
> trust them. osb is a STRUCTURAL reference only (see prd/README.md fidelity rule).

## Context / why

This is the heart of the project: the SmileBASIC language running for real. By the end
of M1 a program like `PRINT FLOOR(-2.1)` executes and you can **watch it in a window**,
and the result is validated against the oracle. Everything after M1 (graphics, sprites,
audio) hangs off this core. We bring up a window *alongside* the core (user's choice) so
there's visual feedback early, but the core must be bit-exact, not a toy.

## Goal (Definition of Done)

- Source → lexer → parser → compiler → bytecode → stack VM, all in `sb-core`.
- Core language works: variables (int/double/string + suffix typing), 1–4D arrays,
  operators with SB precedence, IF/FOR/WHILE/REPEAT/GOTO/GOSUB/ON, DEF/COMMON DEF,
  DATA/READ/RESTORE, INPUT.
- Math + string builtins per their specs; PRINT/LOCATE/COLOR/CLS render to a 50×30
  console → framebuffer → window (native + wasm).
- TinyMT RNG bit-exact (validated against `otya_test.sb3` RND asserts).
- Errors raise the correct `ERRNUM`/`ERRLINE`.
- `cargo test` executes every `spec/tests/` overlay + `corpus/cases` through sb-core and
  runs `otya_test.sb3` via an `ASSERT__` test builtin. Native + wasm builds stay green.

## Scope

**In:** the language core, math/string/console builtins, RNG, console rendering, window,
headless runner, conformance wiring.
**Out (later):** graphics drawing (M2), sprites/BG (M3), real input devices/timing (M4 —
M1 may stub BUTTON/VSYNC as no-ops to let programs run), audio (M5), files (M6).

## Reference sources

- Specs: `spec/instructions/*.yaml` (math = `category: Mathematics`, strings = `Strings`,
  control = `Control`, console = `Console`), `spec/reference/errors.yaml`.
- Docs: `sb-docs/smilebasic-3/manual/{print-and-variables,using-variables,conditional-judgment}.md`,
  `.../screen-layout.md` (console model).
- osb: `parser.d` (Lexical + Parser, `getOPRank`), `node.d` (AST), `compiler.d`
  (`compileExpression`, scopes, DataTable), `VM.d` (`Code` opcodes, `run()` ≈ L204-216,
  frame layout), `type.d` (Value, `Array!T`, `ArrayReference`), `builtinfunctions.d`
  (registration `static this()` ≈ L4169; `ABS/SIN/...` ≈ L142-235), `random.d` +
  `tinymt32.d`, `console.d`, `error.d`, `systemvariable.d`.
- Disassembly: confirm rounding/format/RNG numbers (FLOOR, RND handlers via the keyword
  table — see `prd/README.md` gotchas). STR$ float formatting is RE'd in M7-T4; for M1 a
  best-effort formatter is fine but mark those specs `documented`, not `hw_verified`.

## Architecture decisions

- **Opcode representation:** prefer a flat `enum Op { ... }` + `Vec<Op>` with a `match`
  dispatch loop over osb's object-per-opcode design (better for Rust/wasm + serialization
  + determinism). Keep opcode *semantics* faithful to `VM.d`.
- **Stack machine** with explicit operand stack, frame base pointer, pc, and a slot index
  (SB has 4 program slots + shared COMMON functions). Mirror `VM.d`'s frame layout.
- **Strings** are mutable UTF-16 (`Value::Str`). Arrays are reference types (`Rc<RefCell<…>>`
  or an arena+handle; pick one and document it — must be wasm-safe, no threads).
- **No I/O in `sb-core`.** The console writes into an in-memory grid; rendering to pixels
  lives in `sb-render`; the window lives in `sb-platform-*`.

## Tasks

### M1-T1 — Lexer
- **Files:** `crates/sb-core/src/token.rs` (new), `src/lexer.rs`.
- **Approach:** implement the SB3 lexer; use `parser.d`'s `Lexical` as a STRUCTURAL reference
  only (scan loop, token kinds) — do NOT reproduce its ASCII-only identifiers; verify the
  identifier rules (incl. full-width/kana) against the disassembly/oracle. Handle: decimal + `.`-leading numbers, `&H`/`&B`
  literals, identifiers with `$`/`%`/`#` suffixes, `@label`, `#const`, string literals
  (tolerate unterminated like SB), `'` and `REM` comments, two-char ops `== != <= >= << >>
  && ||`, `TRUE`/`FALSE` → int 1/0. Track line numbers / `SourceLocation`.
- **Acceptance:** unit tests tokenizing representative snippets incl. every literal form
  and operator; line numbers correct across `:` and newlines.

### M1-T2 — AST ✅
> Done. Wrote a fresh, self-contained `crates/sb-core/src/ast.rs` aligned to the M1-T1
> lexer (`SourceLoc`/`TokenKind`/`Suffix`). The pre-pivot `ast.rs` (commit df06667) was
> bound to a `value.rs`/`SbString`/`TokenType` the fresh crate doesn't have, so it was
> rewritten spec-first rather than ported. Highlights: `Expr`/`Stmt` `{kind, loc}` nodes;
> typed `BinOp`/`UnOp` enums with `from_token` (symbolic ops), `from_word` (word ops
> `AND`/`OR`/`XOR`/`MOD`/`DIV`/`NOT` that lex as idents) and `rank` (mirrors `getOPRank`);
> an AST-local `Lit` so the AST never depends on M1-T4's runtime `Value`; a
> `Name {ident, suffix}` carrying suffix identity; `is_lvalue`; and the full statement
> set. `^` power op omitted (no caret token; rank still queued). 7 unit tests cover
> Debug/Clone/PartialEq round-trips, operator mapping/rank, lvalue rules, and a spread of
> statement nodes — used by M1-T3.
- **Files:** `crates/sb-core/src/ast.rs` (new).
- **Approach:** expression nodes (literal, var ref, array index, unary, binary, call,
  OUT-arg) + statement nodes (assign, IF/ELSE/ENDIF, FOR/NEXT, WHILE/WEND, REPEAT/UNTIL,
  GOTO/GOSUB/RETURN/ON, DEF/END, DIM, PRINT, INPUT, DATA/READ/RESTORE, label, bare call).
  Mirror `node.d`. → M1-T1
- **Acceptance:** types compile; `Debug` round-trips; used by M1-T3 tests.

### M1-T3 — Parser
- **Files:** `crates/sb-core/src/parser.rs`.
- **Approach:** recursive descent; expressions via precedence-climbing using SB ranks
  (`getOPRank`: `||`=11, `&&`=10, OR/XOR=9, AND=7, comparisons=6, shifts=5, `+`/`-`=4,
  `*`/`/`/DIV/MOD=3, index=2). Constant-fold constant-vs-constant ops + unary minus
  during parse (match `constcalc` semantics). → M1-T2
- **Acceptance:** parse-tree tests for precedence (`2+3*4`, `-2^…`), const folding,
  multi-statement lines, block constructs; malformed input → `SyntaxError` (errnum 3).

### M1-T4 — Value / Array completion
- **Files:** `crates/sb-core/src/value.rs`, `src/array.rs` (new).
- **Approach:** extend M0's `Value` with `Array` (1–4D, row-major, `dim[4]`+`dimcount`)
  and reference variants (for OUT/by-ref args + SWAP). Implement push/pop/shift/unshift/
  len/resize. Encode int↔double coercion rules used on assignment + in ops (port `PopG`/
  `PopL` + `Operate` from `VM.d`). Must be wasm-safe (no threads). → M0 value.rs
- **Acceptance:** tests for 1–4D indexing, row-major layout, push/pop, coercion, and
  `Subscript out of range` (errnum 31).

### M1-T5 — Bytecode + Compiler
- **Files:** `crates/sb-core/src/bytecode.rs` (new, `Op` enum), `src/compiler.rs`.
- **Approach:** define the opcode set (push/pushvar/popvar global+local, operate(op),
  jumps/cond-jumps/computed-goto, gosub/return, array new/index/store, call user fn, call
  builtin, read/restore, print, end). Compiler walks the AST: resolve var slots (global vs
  bp-relative), labels (two-pass), DEF/COMMON functions, DATA table. Reference `compiler.d`
  + the `Code` subclasses in `VM.d`. → M1-T3, M1-T4
- **Acceptance:** compiles all corpus programs without panics; golden bytecode snapshot
  test for a small program; OPTION STRICT / auto-declare behavior matches docs.

### M1-T6 — VM
- **Files:** `crates/sb-core/src/vm.rs`.
- **Approach:** stack machine; operand stack, `bp`, `pc`, slot index; 4 `VMSlot`s (each
  with its own code/globals/functions/DATA) + shared `commonFunctions`. Dispatch loop
  over `Op`. Cross-slot addresses for GOTO/GOSUB. Reference `VM.d` `run()` + frame layout
  (3-slot frame: bp, pc, data table). → M1-T5
- **Acceptance:** executes arithmetic/control-flow programs to correct results; GOSUB/
  RETURN + ON..GOTO/GOSUB; recursion depth → `Stack overflow` (errnum 5).

### M1-T7 — Builtin registration + math/string builtins
- **Files:** `crates/sb-core/src/builtins/mod.rs`, `builtins/math.rs`, `builtins/string.rs`.
- **Approach:** a registration macro mapping a native fn signature → arg/return
  `ValueType`s + overload table (echo `builtinfunctions.d` `static this()`; `$`-suffix for
  string-returning fns). Implement Mathematics + Strings categories per their specs
  (ABS, FLOOR, CEIL, ROUND, SGN, MIN, MAX, SQR, POW, EXP, LOG, trig, RAD/DEG, PI; LEN,
  MID$/LEFT$/RIGHT$, INSTR, CHR$/ASC, VAL, STR$, HEX$/BIN, FORMAT$, SUBST$). → M1-T6
- **Acceptance:** each implemented instruction's `spec/tests/` cases pass; overload
  resolution by arg/out count; bad types → `Type mismatch` (8), bad arg count →
  `Illegal function call` (4). Add `confidence: disassembled` to specs whose exact numeric
  behavior you confirmed in the listing.

### M1-T8 — Control-flow + console builtins
- **Files:** `crates/sb-core/src/builtins/console.rs`.
- **Approach:** PRINT (`;`/`,`/TAB via TABSTEP, `?` alias), LOCATE (ranges X0-49/Y0-29, Z
  depth, omission keeps prev), COLOR/BACKCOLOR, CLS, INPUT/LINPUT, ACLS. Console writes to
  the grid in M1-T10. Reference `console.d`, `spec/instructions/{print,locate,color,...}`.
  → M1-T7, M1-T10
- **Acceptance:** `PRINT` formatting cases (separators, negative numbers, strings);
  console state (cursor) updates; `corpus/cases/basics.yaml` passes.

### M1-T9 — TinyMT RNG
- **Files:** `crates/sb-core/src/rng.rs` (new, port `tinymt32.d`), RND/RNDF/RANDOMIZE in
  `builtins/math.rs`.
- **Approach:** 8 independent TinyMT engines (SB's seed series 0-7). Reproduce the RNDF
  double-draw quirk (`random.d:54`). RANDOMIZE seeding must match. → M1-T7
- **Acceptance:** the RND/RNDF asserts in `otya_test.sb3` pass exactly (e.g. after
  `RANDOMIZE I,1`, `RND(I,100)`==89,33,33,52; `RNDF`≈0.836095). Mark RND/RNDF specs
  `hw_verified` once cross-checked against the oracle.

### M1-T10 — Console model + render
- **Files:** `crates/sb-render/src/console.rs` (new).
- **Approach:** 50×30 character grid (per `screen-layout.md`), per-cell char + attribute
  (color, ATTR rotation/reflection), font from a font table; renderer rasterizes the grid
  into the `Framebuffer`. Use `spec/reference/constants.yaml` text colors (#TWHITE=15 …).
  Reference `console.d`. → M0 sb-render
- **Acceptance:** rendering a known grid produces a deterministic framebuffer; a golden
  PNG for a simple `PRINT` screen (committed) matches (this is the first golden fixture).

### M1-T11 — Headless runner `sb-run`
- **Files:** `crates/sb-platform-native/src/bin/sb-run.rs` (new).
- **Approach:** load a `.sb3` file, run it headless, dump the console grid as text to
  stdout (and `ERRNUM`/`ERRLINE` on error). This is what `harness/diff/replay.py` shells
  out to. → M1-T8
- **Acceptance:** `sb-run corpus/programs/fizzbuzz.sb3` prints the expected lines; exit
  code reflects error vs success.

### M1-T12 — Window
- **Files:** `crates/sb-platform-native/src/main.rs` (winit + pixels/softbuffer),
  `crates/sb-platform-wasm/src/lib.rs` (canvas/WebGL + wasm-bindgen).
- **Approach:** create a window/canvas, run the VM, blit the `Framebuffer` each frame at
  60 fps. Native uses target-gated deps so the wasm build still works (don't pull winit
  into wasm). → M1-T10
- **Acceptance:** `cargo run -p sb-platform-native --bin sb -- <prog>` shows program
  output in a window; wasm build produces a loadable module rendering to a canvas.

### M1-T13 — Error model
- **Files:** `crates/sb-core/src/vm.rs`, `src/error.rs`, `src/sysvars.rs` (new).
- **Approach:** propagate `SbError` with the source line; expose ERRNUM/ERRLINE/ERRPRG
  sysvars; halt semantics matching SB (program stops, vars readable). → M1-T6
- **Acceptance:** `spec/tests` error cases assert the right errnum (e.g. FLOOR("x")→8,
  1/0→7, subscript→31); ERRLINE points at the right line.

### M1-T14 — Conformance wiring
- **Files:** `crates/sb-core/tests/conformance.rs` (new), `ASSERT__` in `builtins`,
  `harness/diff/replay.py` (wire to `sb-run`).
- **Approach:** a test that loads all `spec/tests/` overlays + `harness/corpus/cases/*.yaml`,
  runs each `code` through sb-core, asserts `expect` (stdout/error). Implement `ASSERT__`
  (test-mode builtin) and run `otya_test.sb3` as an integration test. → M1-T11
- **Acceptance:** all current overlay/corpus cases pass; `otya_test.sb3` passes; CI green
  on native + wasm.
- **Smoke fuel (optional):** `harness/corpus/sbsave/` holds 3,329 real scraped programs
  (`INDEX.json` lists them; `python3 tools/extract_sbsave.py` unpacks). Useful as a
  *parse-doesn't-panic* / e2e sweep — pick small `type:"TXT"` entries. They are **inputs,
  not goldens**: assert no-crash, not output (no oracle ⇒ no verified expectation yet).

## Milestone verification

```bash
cargo test --workspace                       # incl. conformance + otya_test
cargo run -p sb-platform-native --bin sb-run -- harness/corpus/programs/fizzbuzz.sb3
cargo run -p sb-platform-native --bin sb -- harness/corpus/programs/fizzbuzz.sb3   # window
cargo build --workspace --target wasm32-unknown-unknown
cargo run -p sb-spec --bin sb-spec-coverage  # math/string/console specs now have tests
```

## Risks / open questions
- Exact STR$/PRINT float formatting is deferred to M7-T4; M1 uses a best-effort formatter
  (don't claim `hw_verified` for those).
- Array sharing model (Rc<RefCell> vs arena) — decide in M1-T4 and document; must be
  single-threaded/wasm-safe.
- Integer overflow semantics (e.g. MAX overflow cases in `otya_test.sb3`) must match SB's
  i32 wrap/promotion — confirm in the disassembly, capture as tests.
