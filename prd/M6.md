# M6 — Files, projects, system, faithful stubs (PRD)

> Status: ⬜ · Depends on: M1 (parallel with M2-M5) · Read `prd/README.md`. Tasks: `PRD.md` (M6-T*).

## Context / why

SmileBASIC programs save/load files and projects, read system variables, edit program
source at runtime (PRG* family), and interact with device features (mic, motion, wireless)
that we must reproduce *faithfully including their limitations*. This milestone also
finalizes multi-slot semantics (EXEC/USE/CALL/COMMON DEF). Storage must work on both
native (filesystem) and wasm (IndexedDB).

## Goal (Definition of Done)

- A storage layer (native FS / wasm IndexedDB) with an extdata-compatible layout.
- File commands (SAVE/LOAD/FILES/DELETE/RENAME/COPY/CHKFILE/PROJECT) per spec.
- System variables behave per spec (incl. faithful FREEMEM/HARDWARE models).
- PRG* source-editing commands work; mic/motion/MP behave with the right
  availability errors; DIALOG/FONTDEF/CLIPBOARD implemented.
- Multi-slot + COMMON DEF + EXEC/USE/CALL semantics complete.

## Reference sources
- Specs: `category` Files / Source-manip / Various input / Advanced control in
  `spec/instructions/`; `spec/reference/sysvars.yaml`, `errors.yaml` (36/37/38/43/44/45/46).
- Docs: `sb-docs/smilebasic-3/manual/` file/project topics.
- osb: `project.d` (Projects, Resource, FileName), `program.d` (Program/Slot, PRG* edits),
  `systemvariable.d`, `dialog.d`, `petitcomputer.d` (slots, EXEC/USE), `compiler.d`
  (MIC/MOTION/MP sysvars stubbed to 0).
- Disassembly: file format details + exact sysvar values where they matter.

## Architecture decisions
- `sb-core` defines a **`Storage` trait** (list/read/write/delete/rename); native and wasm
  implement it. Keep actual I/O out of `sb-core`. The extdata-compatible layout lets the
  oracle harvest interoperate (O-T3).
- "Faithful stub" means: reproduce the *observable behavior* of unavailable features
  (e.g. MIC without `XON MIC` → errnum 36; MP sysvars → fixed values), not a real
  implementation.

## Tasks

### M6-T1 — Storage abstraction
- **Files:** `crates/sb-core/src/storage.rs` (new, trait); native impl in
  `crates/sb-platform-native`, wasm impl in `crates/sb-platform-wasm`.
- **Approach:** trait for project/file CRUD with SB's resource types (TXT/DAT/GRP/etc.) and
  an extdata-compatible on-disk layout. Provide an in-memory impl for tests.
- **Acceptance:** in-memory storage tests; native writes/reads round-trip; wasm uses
  IndexedDB.
- **Fixtures:** `harness/corpus/sbsave/files/<KEY>/{TXT,DAT}/` is already laid out
  extdata-style (mirrors SmileBASIC `PROJECTS/<name>/{TXT,DAT}/`) — drop it in as a ready
  project tree for LOAD/FILES tests. `DAT/*` are real `PCBN0001` blobs (M6-T2 LOAD fuel).
  Regenerate via `python3 tools/extract_sbsave.py`; format spec is in that tool's docstring.

### M6-T2 — File commands
- **Files:** `crates/sb-core/src/builtins/files.rs` (new).
- **Approach:** SAVE/LOAD (TXT/DAT/program), FILES (listing), DELETE, RENAME, COPY,
  CHKFILE, PROJECT (current project). Errors: 35 (illegal format), 46 (load failed). → M6-T1
- **Acceptance:** per-spec tests against in-memory storage; SAVE then LOAD round-trips a
  string/array; FILES lists correctly.

### M6-T3 — System variables
- **Files:** `crates/sb-core/src/sysvars.rs`.
- **Approach:** implement all sysvars per `spec/reference/sysvars.yaml`: DATE$/TIME$
  (deterministic clock injection for tests), MAINCNT (from M4 clock), VERSION
  (&HXXYYZZZZ for 3.6.0), FREEMEM (faithful memory model), RESULT, ERR*/PRGSLOT/CALLIDX,
  TABSTEP/SYSBEEP (writable). → M4-T3
- **Acceptance:** sysvar reads match spec; writable ones (TABSTEP/SYSBEEP) take effect;
  VERSION encodes 3.6.0; DATE$/TIME$ deterministic under an injected clock.

### M6-T4 — Source-edit (PRG*)
- **Files:** `crates/sb-core/src/builtins/prg.rs` (new).
- **Approach:** PRGEDIT (select edit target), PRGGET$/PRGSET/PRGINS/PRGDEL, PRGNAME$,
  PRGSIZE. Enforce errnum 38 (use PRGEDIT first). → M1 program model
- **Acceptance:** PRG* round-trip edits a program slot's source; errnum 38 when misused.

### M6-T5 — Misc + faithful limitation stubs
- **Files:** `builtins/dialog.rs`, `builtins/device.rs` (new); `dialog`/`fontdef`.
- **Approach:** DIALOG (RESULT), FONTDEF, CLIPBOARD; MIC family + MOTION (GYRO/ACCEL)
  with availability gating (XON MIC/MOTION) → errnum 36/37; MP wireless family + sysvars
  as faithful stubs (fixed values, like SB offline). DIRECT-mode/program-only restrictions
  → errnum 43/44/45. → M6-T1
- **Acceptance:** DIALOG sets RESULT; MIC/MOTION without XON → 36/37; MP sysvars match
  offline values; DIRECT-only commands error in program (43) and vice versa (44).

**Progress (done 2026-06-23):** `builtins/device.rs` (pure logic + `DeviceState` XON/XOFF
feature flags) + VM `call_device` routing implement the faithful limitation-stub family:
`XON`/`XOFF` (parser keyword form, EXPAD→RESULT TRUE), MIC* (`MICSTART`/`MICSTOP`/`MICDATA`/
`MICSAVE`), MOTION (`GYROA`/`GYROV`/`GYROSYNC`/`ACCEL`), MP* (`MPSTART`/`MPEND`/`MPSET`/
`MPSTAT`/`MPSEND`/`MPRECV`/`MPGET`/`MPNAME$`) and `DIALOG`. Acceptance met: **DIALOG sets
RESULT** (statement→0 Time out, file-name form→-1 Canceled, confirm form returns 0); **MIC/
MOTION without XON → 36/37** (hw_verified, conformance gate); **MP sysvars match offline
values** (MPHOST/MPLOCAL=-1, MPCOUNT=0 from M6-T3; MPSTART leaves RESULT 0, MPSTAT()→0,
MPRECV→SID -1, peer reads→errnum 10 with 0 terminals). The arg-shape/range/type/syntax guards
fold into `IN_SCOPE_DEVICE`; 6 device.rs + 5 VM e2e tests; full gate green incl. wasm.
Scope notes: FONTDEF already lives in M6-T2/console (done); SB3 has **no CLIPBOARD**
instruction (planning placeholder — COPY is the array op, S-T4c). The **DIRECT-only→43**
direction is tied to the unimplemented DIRECT-mode keyword commands (RUN/CONT/NEW/CLEAR/LIST,
S-T12e specs, landing with M6-T6 multi-slot); the program→44 direction is already covered by
`PROJECT` (M6-T2). Live device output + interactive DIALOG/MP-session values are
oracle-pending (HARVEST_QUEUE.md).

### M6-T6 — Multi-slot semantics
- **Files:** `crates/sb-core/src/vm.rs`.
- **Approach:** finalize 4 slots + shared COMMON DEF; EXEC (run another slot), USE (mark
  slot), CALL (call by name), cross-slot GOTO/GOSUB and variable scoping. → M1-T6
- **Acceptance:** programs spanning slots with COMMON DEF run; EXEC/USE/CALL behave per
  spec; CALLIDX correct in SPFUNC/BGFUNC callbacks.
- **Progress (CALL by name landed):** `Op::CallDynamic` is now implemented
  (`vm.rs::call_dynamic` + the shared `invoke_function` split out of `call_user`): `CALL
  "name" [,args] [OUT outs]` resolves a `DEF` instruction by a **runtime** name string
  (ASCII case-folded, no type suffix) and invokes it exactly like a literal `CallUser` —
  a non-string name → Type mismatch (8), an unknown instruction → Undefined function (16),
  both matching the hw_verified `call.yaml`. `CALL`/`COMMON` are folded into the
  conformance gate (`IN_SCOPE_CONTROL`): `call.yaml` (`calls_by_name`,
  `undefined_instruction`) + `common.yaml` (`common_def_same_slot`) now replay green. 7 new
  VM e2e tests (by-name, case-insensitive, string-var target, value args, OUT results,
  undefined → 16, non-string → 8). **Still `[ ]` — remaining multi-slot work:** EXEC (run
  another slot) + USE (mark a slot executable) are still `VmError::Unsupported` — both need
  a multi-program execution model (the VM holds one `Program`/`globals` for the running
  slot; a faithful EXEC context-switch + cross-slot COMMON DEF dispatch + per-slot variable
  scoping require threading a "current slot" execution context through `run`/`cell`/
  `var_suffix`/function lookup, plus a host hook to load compiled programs into slots 1-3).
  EXEC/USE runtime effect + errnums are all hypothesis/oracle-pending (their handlers aren't
  body-readable — `exec.yaml`/`use.yaml`), so faking a no-op switch would be wrong; left
  Unsupported until the multi-program model lands. CALLIDX is wired (`sysvars`, reads 0
  outside any callback) but the SPFUNC/BGFUNC per-frame callback **dispatch** that would set
  it is the M3-T6/frame-loop subsystem (no headless caller invokes the registered callbacks
  yet) — queued.
- **Progress (USE/EXEC error model + slot-executable flag landed, 2026-06-23):** harvested the
  full USE/EXEC validation behavior from the oracle and implemented it (`vm.rs::do_use`/
  `do_exec` + `parse_prg_operand`). **hw_verified:** USE numeric — slot outside 0..3 → errnum
  10, the running slot (`USE 0`) → 4, a valid non-running slot → marks it executable
  (`slot_used` flag + `Vm::slot_used` getter); USE string — `PRG0:` (running) → 4, unknown
  resource / PRG index past family (`FOO:`/`PRG4:`/`PRG5:`) / empty name → 4 (NOT 10 like the
  SAVE resolver), valid slot + missing file → 46, valid slot + existing file → marks it. EXEC —
  numeric slot out of range → 10, an empty non-running slot → Syntax error 3, string unknown
  resource → 4, missing file → 46. Specs `use.yaml`/`exec.yaml` corrected (the old hypotheses
  had range → 4, which is wrong) + folded into the conformance gate (`IN_SCOPE_MULTISLOT`); 13
  new vm.rs e2e tests. **Still `[ ]` — the multi-program CONTROL TRANSFER is the remaining
  work and is left `VmError::Unsupported` (not faked):** loading a compiled program into a
  slot (no host compile hook / slot-program registry yet — `EXEC`/`USE "PRGn:file"` success
  paths), the actual EXEC program switch + the no-return-vs-`END`-returns-across-slots rule,
  per-slot vs shared (COMMON) variable scoping, and cross-slot DEF/label resolution from a
  USE'd slot. These need a multi-program execution model + a ≥2-slot oracle harness to ground
  (the handlers are parser special forms with no body-readable disassembly) — queued in
  `HARVEST_QUEUE.md`.
- **Progress (cross-slot COMMON DEF dispatch landed, 2026-06-23):** the first piece of the
  multi-program execution model — "programs spanning slots with COMMON DEF run." The VM now
  holds a per-slot program registry (`slot_programs: [Option<LoadedSlot>; 4]`, each slot's own
  `Program` + `globals`) + an `active_slot`; a host/test seeds slots 1-3 via the new
  `Vm::load_slot_program(slot, Program)`. Call resolution (`resolve_user_function` behind both
  `Op::CallUser` and `Op::CallDynamic`) now falls back from the active program to a **`COMMON
  DEF` in a `USE`'d, loaded slot**; the cross-slot invoke swaps that slot's program/globals into
  the active context (`activate_slot`), tags the frame with its `caller_slot`, and `return_func`
  swaps the caller's context back before resuming. `ERRPRG` now reports `active_slot`. This is
  the **documented** model (common.yaml: COMMON makes a DEF callable from another slot after
  USE; call/arg/OUT/return semantics are those of DEF): `CALL "name"` after `USE n` runs the
  COMMON DEF with value args / OUT results / return value; an un-USE'd slot or a non-COMMON DEF
  → Undefined function (16). 6 new vm.rs e2e tests (cross-slot call after USE, args+OUT, return
  value, no-USE→16, non-COMMON→16, caller-context restored). **Still `[ ]`:** EXEC whole-program
  control transfer (form 1 string LOAD + form 2 numeric switch, the END-returns-across-slots
  rule) is still `VmError::Unsupported`; and whether a *free* (non-param/local) variable inside a
  COMMON DEF binds to the defining slot's globals (what we implement) or the caller's is
  oracle-pending (needs a 2-slot oracle sharing a same-named global) — both queued in
  `HARVEST_QUEUE.md`.
- **Progress (EXEC numeric loaded-slot control transfer landed, 2026-06-23):** the second
  piece of the multi-program execution model — `EXEC n` now *transfers control* to a loaded,
  non-running slot (`vm.rs::exec_transfer`). This is the **documented** model (exec.yaml:
  "Executes a program in a different SLOT"): `exec_transfer` swaps the target slot's
  program/globals into the active context (reusing `activate_slot`), resets the PC to the
  top, and — because it is impossible to return to the previous program — discards the
  caller's whole execution state (`DEF` frames, `GOSUB` stack, operand stack, `DATA` cursor);
  the EXEC'd program then runs against its own globals and, when it ends, the run ends.
  `current_slot`/`active_slot` both move to the target so `SLOT()`/`ERRPRG` report it. An
  *empty* non-running slot stays Syntax error (3, hw_verified); out-of-range stays 10. A
  host/test seeds slots 1-3 via `Vm::load_slot_program`. 5 new vm.rs e2e tests (transfer +
  abandoned tail, own-globals scoping, empty-slot→3, GOSUB-state discarded, ERRPRG=target
  slot). **Still `[ ]`:** the string FORM 1 file LOAD (needs a compile-from-storage hook
  threading the builtin registry into the VM), the running-slot RESTART (`EXEC 0`), the
  nested END-returns-across-slots rule, and the per-slot vs shared variable-scoping
  confirmation all remain `VmError::Unsupported`/oracle-pending — they need a ≥2-slot oracle
  harness, NOT a single injected program (queued, `HARVEST_QUEUE.md`). CALLIDX in
  SPFUNC/BGFUNC callbacks still rides the M3-T6 frame-loop dispatch.
- **Progress (EXEC/USE string-form `"PRGn:file"` file LOAD landed, 2026-06-23):** the third
  piece of the multi-program execution model — loading a program into a slot *from storage*.
  The blocker noted above ("needs a compile-from-storage hook threading the builtin registry
  into the VM") turned out not to need an external host hook: sb-core already has the whole
  `parse`→`compile_with(&StdBuiltins)` pipeline, so the new `vm.rs::compile_slot_file` reads
  the TXT body from the VM's `Storage` (UTF-8, like `LOAD "TXT:"`) and lowers it in-VM. Wired
  into both string forms: `EXEC "PRGn:file"` into a non-running `PRGn:` slot loads the file's
  program into that slot (`load_slot_program`) and **transfers control** to it (`exec_transfer`)
  — documented form 1, "Loads and executes a program"; `USE "PRGn:file"` loads the program and
  marks the slot executable, so its `COMMON DEF`s resolve cross-slot via `CALL "name"` (the
  dispatch landed earlier). The hw_verified error model is unchanged (missing file → 46,
  bad/empty resource → 4, running slot → 4 for USE). 7 new vm.rs e2e tests (EXEC load+transfer,
  own-globals scoping, missing-file 46, running-slot still deferred; USE load+call, args+OUT,
  missing-file 46) + the prior `exec_valid_target…unsupported` test refit to assert the real
  SAVE→EXEC transfer. **Still `[ ]`:** the running-slot RESTART (`EXEC 0`), the bare-name
  (no `PRGn:`) default-slot load, the nested END-returns-across-slots rule, and the per-slot vs
  shared variable-scoping confirmation stay `VmError::Unsupported`/oracle-pending (need a
  ≥2-slot oracle harness; queued, `HARVEST_QUEUE.md`). A *malformed* stored program currently
  maps to Syntax error (3) as a hypothesis — exact errnum oracle-pending. CALLIDX in
  SPFUNC/BGFUNC still rides the M3-T6 frame-loop dispatch.
- **Progress (numeric running-slot RESTART `EXEC 0` landed, 2026-06-23):** the fourth piece —
  restarting the *running* program. `EXEC <running slot>` (e.g. `EXEC 0`, the corpus
  "restart the game" idiom — `IF DIALOG("RETRY?",1)<0 THEN END ELSE EXEC 0`, 1DVK34J/TXT/HNZBUS)
  now re-runs the active program from the top via `vm.rs::restart_active_slot`: re-initialise the
  running slot's globals to their declared-type zeros (a fresh re-run — `DIM`/variable init
  re-execute, the documented "Loads and executes a program") and discard all execution state
  (`DEF`/`GOSUB`/operand stacks, `DATA` cursor) before the PC jumps to the top; per-slot I/O state
  (graphics/sprites/console) is untouched, matching `exec_transfer`. This is the only coherent
  restart semantics — keeping old variable values would just be `GOTO @TOP`, and the re-run's
  `DIM` would redim. Deterministic VM e2e test (`exec_running_slot_restart_reruns_from_top`) uses
  a DAT file counter (persists across the restart) to terminate after 2 passes and asserts the
  global reset; the old `…_is_unsupported_pending` assertion is retired. spec `exec.yaml`
  semantics + `HARVEST_QUEUE.md` updated. **Still `[ ]`:** whether real SB clears variables on
  `EXEC 0` (sb-core clears) vs preserves is oracle-pending — the single-program oracle can't drive
  a self-restart without the persistent-counter SAVE-dialog hanging the harness (needs the
  deferred multi-slot / file-diff harness). The STRING running/default-slot file LOAD
  (`EXEC "PRG0:…"`), the `END`-returns-across-slots rule, per-slot vs shared scoping, and
  CALLIDX-in-callbacks (M3-T6) remain the open multi-program pieces.
- **Progress (string running-slot file LOAD `EXEC "PRG0:file"` landed, 2026-06-23):** the fifth
  piece — loading a program file *into the running slot* and executing it. `EXEC "PRGn:file"`
  where `n` is the running slot (`EXEC "PRG0:file"` while slot 0 runs — the corpus loader idiom
  where a tiny slot-0 loader `EXEC`s the real program) now reads the file's TXT body from
  storage, compiles it in-VM (`compile_slot_file`), and **replaces the running program** with it
  via the new `vm.rs::load_into_running_slot`: the loaded `Program`/`globals` take over
  `Vm::program`/`Vm::globals`, all execution state (DEF/GOSUB/operand stacks, DATA cursor) is
  discarded, the new globals start at declared-type zeros, and the PC jumps to the top — like a
  restart, since you cannot return to the previous program. This is documented form 1 ("Loads and
  executes a program", exec.yaml) applied to the running slot; the hw_verified error model is
  unchanged (missing file → 46, bad/empty resource → 4). The prior
  `exec_string_into_running_slot_stays_deferred` test is retired and replaced with three e2e tests
  (load+run-from-top with the post-EXEC statement abandoned, fresh-globals scoping, missing-file
  46). spec `exec.yaml` form-1 semantics updated. **Still `[ ]`:** the bare-name (no `PRGn:`)
  default-slot file LOAD (its destination slot is the oracle-pending deferred loader; a `PRG0:`
  resource when slot 0 is *not* the running slot is the same edge), the nested
  `END`-returns-across-slots rule, per-slot vs shared variable scoping, and CALLIDX in
  SPFUNC/BGFUNC callbacks (M3-T6 frame loop) remain the open multi-program pieces (queued,
  `HARVEST_QUEUE.md`).
- **Progress (cross-slot `END`-returns rule landed, 2026-06-23):** the sixth piece — the
  documented "return by using END" behavior. Both EXEC forms' notes say *"It is possible to
  return by using END in a program started with EXEC in another SLOT"* (alongside the general
  "impossible to return to the previous program"). The reconciliation: a SAME-slot EXEC
  (running-slot restart / `EXEC "PRG0:…"`) replaces the running program and cannot return,
  but a DIFFERENT-slot EXEC returns to its launcher when the EXEC'd program reaches `END` (or
  runs off the end of its code). Implemented: `vm.rs::exec_transfer` now captures the
  launcher's full resume state (active slot, the pc just past the EXEC, operand stack, `DEF`
  frames, `GOSUB` stack, `DATA` cursor) into a new `exec_returns` stack before handing control
  to the target; the run loop calls the new `vm.rs::try_exec_return` at both halt sites (`END`
  / end-of-code) to restore the top context and resume the launcher right after the EXEC. It
  nests LIFO (A→B→C: C's `END` returns to B, B's to A). `STOP` does NOT trigger a return (only
  `END`). This **corrects** the prior placeholder: the earlier "abandoned tail" / "GOSUB-state
  discarded" cross-slot tests encoded the not-yet-implemented non-returning model — they (and
  `exec_string_loads_prg_slot_and_transfers_control`) are rewritten to assert the launcher
  resumes (e.g. `EXEC 1`→slot-1 runs→`END`→launcher's `PRINT` after EXEC runs). Structural
  model: osb `VM.d` `Exec.execute` (saves `execReturnAddress` for `slot != currentSlot`,
  restored by `VM.end()`). spec `exec.yaml` control-transfer semantics + sources updated
  (documented; resume-state *granularity* + per-slot scoping stay oracle-pending). 4 new vm.rs
  e2e tests (`exec_cross_slot_end_returns_to_launcher`, `…_nesting_returns_in_lifo_order`,
  `…_return_preserves_caller_gosub_state`, + the corrected `exec_numeric_transfers_to_loaded_slot`).
  **Still `[ ]`:** the bare-name (no `PRGn:`) default-slot file LOAD (osb defaults it to the
  running slot; oracle-pending), per-slot vs shared variable scoping, the exact resume-state
  granularity real SB preserves, and CALLIDX in SPFUNC/BGFUNC callbacks (M3-T6 frame loop)
  remain (queued, `HARVEST_QUEUE.md`).
- **Progress (bare-name default-slot file LOAD landed, 2026-06-23):** the seventh piece — a
  bare filename with no `PRGn:` resource number (the corpus loader idiom `EXEC EXE$` /
  `EXEC FAV$[SELFAV]` where a small slot-0 loader EXECs the chosen program by name) now loads
  + executes. Per osb `Exec.execute` (`if (!file.hasResourceNumber) file.resourceNumber =
  currentSlotNumber`) a bare name defaults to the RUNNING slot, so `do_exec`'s `None` (no-slot)
  string branch reads the TXT body from storage, compiles it in-VM (`compile_slot_file`), and
  replaces the running program via `load_into_running_slot` — the exact same path as
  `EXEC "PRG0:file"` while slot 0 runs (fresh zeroed globals, all execution state discarded, PC
  to the top). The hw_verified error model is unchanged (missing file → 46). 3 new vm.rs e2e
  tests (bare-name load+run-from-top with the post-EXEC statement abandoned, fresh-globals
  scoping, bare-variable filename `EXEC EXE$`). spec `exec.yaml` form-1 semantics + the osb
  source bullet updated. **Still `[ ]`:** a `PRG0:` resource naming a *non-running* slot 0 (the
  slot-0 registry edge — `load_slot_program` ignores slot 0) stays `VmError::Unsupported`;
  whether real SB's bare-name default truly resolves to the running slot vs slot 0 (osb's local
  `slot` stays 0 in that branch — likely an osb bug) needs a ≥2-slot oracle to confirm; per-slot
  vs shared variable scoping, the exact resume-state granularity real SB preserves, and CALLIDX
  in SPFUNC/BGFUNC callbacks (M3-T6 frame loop) remain (queued, `HARVEST_QUEUE.md`).
- **Progress (slot-0 registry edge landed, 2026-06-23):** the eighth piece — `EXEC "PRG0:file"` /
  `USE "PRG0:file"` naming a *non-running* slot 0. The remaining "deferred" carve-out turned out to
  be only an implementation artifact, not a SmileBASIC semantic: osb keeps all program SLOTs in one
  uniform array (`VMSlot[5] slots`; `Exec.execute` compiles into `slots[slot]` for any slot, no
  slot-0 special case), whereas sb-core parked the running program in the VM directly and the
  others in `slot_programs`, so `load_slot_program` ignored slot 0. New `vm.rs::stash_slot_program`
  writes a compiled program into ANY parked slot 0..3; `do_exec`/`do_use` now route a non-running
  slot 0 through it (the existing `slot == current_slot` guards already keep it off the *running*
  program — `EXEC "PRG0:"` while slot 0 runs still replaces the running slot, `USE "PRG0:"` while
  slot 0 runs still → errnum 4). So `EXEC "PRG0:file"` from another running slot cross-slot
  transfers into slot 0 and its `END` returns to the launcher (documented cross-slot return), and
  `USE "PRG0:file"` marks slot 0 executable so its `COMMON DEF` resolves via `CALL "name"` — both
  uniform with `PRG1:`/`PRG2:`/`PRG3:`. The hw_verified error model is unchanged (missing file →
  46). The two `VmError::Unsupported` arms are removed (do_exec's `Some` arms merge: non-running
  slot → transfer, running-slot/bare-name → load-into-running). 4 new vm.rs e2e tests (EXEC into a
  non-running slot 0 with cross-slot return, own-globals scoping, missing-file 46, USE slot-0
  COMMON DEF callable) using a new `run_slot1_with_txt` helper that makes slot 0 non-running via a
  boot `EXEC 1`. specs `exec.yaml`/`use.yaml` semantics + osb source bullet updated (documented;
  confidence unchanged). **Still `[ ]`:** the slot-0 CLOBBER edge (loading into a non-running slot
  that holds a pending EXEC-return launcher overwrites that launcher — osb has the same uniform-slot
  behavior, but the exact resume state real SB preserves needs a ≥2-slot oracle; the tests STOP
  before any return could fold back into the clobbered slot), whether real SB's bare-name default
  resolves to the running slot vs slot 0, per-slot vs shared variable scoping, the exact
  resume-state granularity, and CALLIDX in SPFUNC/BGFUNC callbacks (M3-T6 frame loop) remain
  (queued, `HARVEST_QUEUE.md`).
- **Progress (cross-slot COMMON DEF variable scoping grounded + locked, 2026-06-23):** the ninth
  piece — pinning the "per-slot vs shared variable scoping" question that the earlier slices left
  as a pure oracle-pending hypothesis. Two osb structural rules compose to settle it: (1)
  `compiler.d:318-345` `genCodePushVar` — a bare name inside a DEF resolves to a GLOBAL only when
  that global already exists in the slot's table (`hasGlobalVarIndex`); otherwise it becomes a
  fresh DEF-local; and (2) `VM.d:585-589` — a function call does `if (func.isCommon)
  setCurrentSlot(func.slot.index)`, so a global read/write inside a COMMON DEF resolves against the
  DEFINING slot (`currentSlot.global[...]`). sb-core already matches this (its `activate_slot`
  swaps to the defining slot's program+globals for the cross-slot COMMON call), so this slice adds
  no VM behavior change — it LOCKS the behavior with two deterministic vm.rs e2e tests and raises
  the claim from pure hypothesis to osb-structural (`community`): `cross_slot_common_def_global_binds_to_defining_slot`
  (a COMMON DEF reading global G sees its own slot's zero-init G=0, not the caller's G=7) and
  `cross_slot_common_def_global_write_isolated_from_caller` (a COMMON DEF writing G=99 hits its own
  slot's global — read back as 99 — while the caller's same-named G stays 7). The statement form
  (`PRINT G` inside the DEF) is used because returning a bare global from a value-DEF
  (`RETURN G` / `V=GETG()`) hits a SEPARATE pre-existing single-slot Stack-underflow bug
  (orthogonal to multi-slot, queued in `HARVEST_QUEUE.md` for an M1-T6 follow-up — osb zero-inits
  locals and reads globals fine, so SB should return the value). `common.yaml` semantics + an osb
  structural source updated. **Still `[ ]`:** a ≥2-slot oracle confirm of the defining-slot
  binding (community/structural until then), the exact resume-state granularity real SB preserves
  on the cross-slot `END` return, the bare-name default-slot question, and CALLIDX in SPFUNC/BGFUNC
  callbacks (M3-T6 frame-loop dispatch — no headless caller invokes the registered callbacks yet)
  remain the open multi-program pieces (queued, `HARVEST_QUEUE.md`).

## Milestone verification
```bash
cargo test -p sb-core
cargo run -p sb-platform-native --bin sb -- <a save/load demo>.sb3
```

## Risks / open questions
- SB file/project container format (O-T3) — needed for oracle interop and possibly for
  importing real community programs.
- FREEMEM exact model — osb returns a constant; confirm whether 3.6.0 varies and what
  programs depend on it.
