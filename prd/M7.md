# M7 — Hardening (PRD)

> Status: ⬜ · Depends on: M2-M6 · Read `prd/README.md` first. Tasks: `PRD.md` (M7-T*).

## Context / why

By M7 the interpreter is feature-complete. This milestone pushes it from "works" to
"faithful": broad differential fuzzing against the oracle, raising spec confidence to
`hw_verified` across the set, and nailing the fiddly numeric/string corners (float
formatting, integer overflow, exact error strings). The deliverable is a measurably
high-fidelity interpreter with a large committed regression corpus.

## Goal (Definition of Done)

- Sustained fuzzing campaign with findings minimized + promoted into the committed corpus.
- The large majority of specs at `disassembled`/`hw_verified`, tracked by the coverage
  report.
- Error messages/numbers, float formatting, and overflow/precision corners match the real
  binary; performance is acceptable on native + wasm.

## Reference sources
- Everything: specs, disassembly (error strings ≈ `0x1E965C`; float-format + math
  routines), the oracle (`harness/`), and the coverage report (`sb-spec-coverage`).

## Tasks

### M7-T1 — Fuzzing campaign
- **Files:** `harness/fuzz/generator.py` (full grammar), `harness/diff/run.py`,
  `harness/harvest/harvest.py`.
- **Approach:** flesh out the seeded, grammar-aware generator (spec-signature-driven so
  calls are well-typed). Run differential vs the oracle; minimize each divergence to a
  small program; **promote** it into `harness/corpus/` (or a `spec/tests/` overlay) as a
  permanent seeded regression. → O-T8
- **Acceptance:** a documented fuzzing run; every found divergence either fixed in sb-core
  or captured as a known-divergence case; promoted cases replay deterministically in CI.

### M7-T2 — hw_verified push
- **Files:** `spec/tests/*`, spec `sources`/`confidence` fields; `harness/harvest`.
- **Approach:** harvest oracle `expect:` values across the spec corpus; bump confidence.
  Prioritize math, strings, graphics, sound. Track progress via the coverage report. → O-T8
- **Acceptance:** coverage report shows a large `hw_verified` share (set a concrete target,
  e.g. ≥80% of implemented instructions); CI replays the frozen expects.
- **Progress (2026-06-23, run 1):** First oracle-harvest pass against real SB 3.6.0 (Azahar).
  - **classify → `hw_verified`** (Mathematics now **27/27**). The headline inf→1 / NaN→2
    paths — previously the last unverified piece — were produced via `double` overflow:
    `EXP(1000)`→+inf, `EXP(1000)-EXP(1000)`→NaN, so `CLASSIFY(EXP(1000))=1`,
    `CLASSIFY(-EXP(1000))=1`, `CLASSIFY(EXP(1000)-EXP(1000))=2` (confirms the @0x20c3e0
    helper map; HARVEST_QUEUE S-T1b resolved). 3 inline cases added; sb-core passes them.
  - **Various-input category (13 instructions) frozen as committed fixtures.** `harvest.py
    --category "Various kinds of input"` drove the oracle: **33/33 cases confirmed, 0
    mismatch, 0 capture failures** → `spec/tests/{accel,brepeat,button,gyroa,gyrosync,gyrov,
    micdata,micsave,micstart,micstop,stick,stickex,touch}.yaml`. Hand-added the two OUT-form
    baselines `harvest.py` skips (`STICK`/`STICKEX OUT X,Y`→`0,0`, via `run_case` progstr).
    These error-guards + neutral baselines are now regression-locked + CI-replayed.
  - **Coverage 51.2% → 51.6% hw_verified** (127→128); 1192→1230 frozen cases.
  - **Structural ceiling (why ≥80% is not reachable purely in-loop):** the input/mic/motion/
    wireless family stays top-level `disassembled` on purpose (consistent with M6-T5's
    treatment of this exact hardware-stub family) — only their headless-observable contract
    (errnum guards + neutral 0 baselines) is oracle-confirmable; their LIVE hardware path
    (button bitmask, analog stick, accel/gyro values, mic samples, the post-`XON` sensor
    path, multi-console MP) is permanently un-harvestable headless. And the bulk of the
    remaining `disassembled` set is **Graphics (18) / Sprites (19) / BG (23) / Sound (18)**
    whose headline behavior needs framebuffer/audio goldens (O-T6 composite / O-T7 audio,
    both deferred) or real hardware. So the in-loop hw_verified gain is bounded by the small
    software/value-deterministic remainder; reaching ≥80% requires the deferred media +
    hardware + multi-console harvest, not more value/errnum batches.
  - **Path forward (future runs):** keep harvesting one value/errnum-deterministic category
    per run — Wireless, Screen control, the Files/Control/Data-op stragglers — freezing each
    as `spec/tests/*` regression fixtures (the % may plateau but the deterministic corpus
    grows). The maintainer closes the ≥80% gap via the media/hardware goldens.
- **Progress (2026-06-23, run 2):** Screen-control GET-form harvest against real SB 3.6.0
  (Azahar). The two Screen-control commands with a **scalar GET form** were promoted to
  `hw_verified` (top-level): **BACKCOLOR** and **DISPLAY** (Screen control 2/7 → 4/7).
  - **BACKCOLOR round trip — alpha strip discovered.** `BACKCOLOR c : ?BACKCOLOR()` returns
    the stored color **masked to 24 bits** (`GET = stored & &H00FFFFFF`): RGB(64,128,128)
    (&HFF408080) → 4227200 (&H408080), RGB(16,32,64) → 1056832 (no channel swap), and an
    arbitrary high byte is dropped too (`&H7F112233` → &H112233, `-1` → &HFFFFFF). The spec
    had wrongly claimed a full round-trip; corrected. **Fixed sb-core**: `backcolor()` SET now
    masks `& 0x00FF_FFFF` (it stored/returned the value verbatim). 8 inline value goldens +
    a `backcolor_get_strips_alpha` unit test.
  - **DISPLAY GET** returns the active output-screen id: `?DISPLAY()` → 0 (Upper default),
    `XSCREEN 2:DISPLAY 1:?DISPLAY()` → 1. sb-core already matched; added 3 inline value
    goldens. ACLS/VISIBLE/XSCREEN have **no scalar GET form** (headline = screen state, only
    errnum guards harvestable — already done), so they honestly stay `disassembled`.
  - **Coverage 51.6% → 52.4% hw_verified** (128→130); 1230→1241 frozen cases. Harvest TSVs in
    `harness/harvest/out/screen_get*.tsv`.
- **Progress (2026-06-23, run 3):** Wireless-communication category (8 commands) error-guard
  harvest against real SB 3.6.0 (Azahar) via `harvest.py --category "Wireless communication"`.
  **19/19 error-guard cases confirmed, 0 mismatch** → `spec/tests/mp{end,get,name,recv,send,
  set,start,stat}.yaml` (`mpname.too_many_args` re-run by hand after one transient capture
  failure). The cases cover every headless-reachable validation guard: argcount errnum 4
  (all 8), MPSET/MPSTART range errnum 10, MPSEND/MPSTART type errnum 8, MPSEND empty-string
  errnum 4. Added/extended `hw_verified` source refs on mp{end,set,start,stat,send}.
  - **Discovery — the MP-restriction flag @0x305612 is ZERO in a normal RUN context.** The
    range/type guards sit AFTER the flag check in every handler; their firing as errnum 10/8
    (never 52) PROVES the flag is unset, so a plain program reaches them rather than bouncing
    on errnum 52 "Incompatible statement". This **resolves HARVEST_QUEUE S-T13a open question
    (a)** (a valid `MPSTART` proceeds toward a real connection — hence must NOT be harvested
    headless; it would hang). errnum 52 itself stays oracle-pending (no headless way to set
    the flag).
  - **Top-level confidence stays `disassembled` for all 8** (the structural ceiling): only the
    pre-network validation contract is headless-observable; the LIVE messaging path (MPSTAT
    0/1 connection status, MPNAME$ names, MPGET slot values, MPSEND/MPRECV delivery + errnum
    41/42, errnum 52) needs ≥2 real wireless peers a single Azahar can't form. So the
    hw_verified instruction count is unchanged (still 130) but the deterministic corpus grows.
  - **1241 → 1260 frozen cases.** Harvest TSV in `harness/harvest/out/batch-8.tsv`.
- **Progress (2026-06-24, run 4):** S-T4f Data-ops stragglers — **KEY** and **OPTION** promoted
  to `hw_verified` (Data operations 9/14 → 11/14; coverage 52.4% → **53.2%** hw_verified,
  130 → 132). Two genuinely-unharvested queue items resolved against real SB 3.6.0 (Azahar):
  - **KEY() undocumented function form** reads back the bound string: `KEY 3,"HI":?KEY(3)`="HI"
    (also "CLS"/"HELLO"); function-form guards KEY(6)/KEY(0) → errnum 10, KEY(1,2) → errnum 4.
    Frozen in `spec/tests/key.yaml` (6 cases); sb-core already matches.
  - **OPTION STRICT/DEFINT/TOOL**: STRICT + undeclared `B=2` → errnum 15 errline 2; OPTION TOOL
    compiles cleanly (recognized feature, no error); unknown feature → errnum 3. **OPTION DEFINT
    flips the suffix-less numeric default Real→Integer**, and the real→int conversion **truncates
    toward zero** (A=3.7→3, A=-3.7→-3, A=2.5→2, A=-2.5→-2; A#=3.7 stays 3.7). Decisively, WITHOUT
    any OPTION the suffix-less default is **Real** (A=3.7→3.7, `DIM A[3]:A[0]=3.7`→3.7) — resolving
    the long-standing "suffix-less Real-vs-Int" open question. Frozen in `spec/tests/option.yaml`.
  - **Harness fix (the big one): `spec/tests/*.yaml` overlays were NOT being replayed.** The
    conformance loader's `CaseFile` read a `cases:` key, but every overlay (and `harvest.py`
    output) uses `tests:` — serde silently dropped them to zero cases. So all 23 prior overlay
    files (accel/button/stick/mp*/…, 65 cases) were dead weight, never exercised. Added
    `#[serde(alias = "tests")]` so the gate now replays them — directly fulfilling M7-T2's
    acceptance ("CI replays the frozen expects"), which had not actually been happening. Proven
    via a canary (`corpus_and_overlay_cases_pass` now reports 342 curated cases, all green).
  - **sb-core impl gap found (queued, M1-T4 — NOT this slice):** sb-core types unsuffixed `DIM`
    arrays as Int (should be Real) and treats OPTION DEFINT as a no-op (flag parsed, never
    consumed). So the 4 DEFINT real→int coercion cases are recorded hw_verified in option.yaml's
    sources but NOT frozen as conformance cases yet; corrected the misleading `bytecode.rs`
    comments and queued the fix in `HARVEST_QUEUE.md`.
- **Progress (2026-06-24, run 5):** **PRINT** promoted `disassembled` → **hw_verified**
  (Console input/output 9/12 → 10/12; coverage 53.2% → **53.6%** hw_verified, 132 → 133). The
  number formatter was already hw_verified (M7-T4 run); this run harvested the remaining
  headline behavior — separators, integer formatting, and the newline rule — against real SB
  3.6.0 (Azahar) by reconstructing the rendered console row with `CHR$(CHKCHR(I,0))` up to CSRX
  and reading CSRX/CSRY back (so the goldens are the REAL cells, not inferred values):
  - **Integer = %d, no leading/trailing space:** `PRINT 42`="42", `PRINT -7`="-7", `PRINT 0`="0",
    `PRINT 100` → CSRX=3.
  - **`;` concatenates no-gap:** `PRINT "A";"B"`="AB", `PRINT 5;"X"`="5X", `PRINT 1;2;3`="123".
  - **`,` tabs to the next STRICTLY-greater multiple of TABSTEP** (`?TABSTEP`=4): `PRINT "A","B"`
    ="A   B" (CSRX after `PRINT "A",`=4); decisively, **a cursor already ON a boundary advances a
    full step** — `PRINT "ABCD","Z"`="ABCD    Z" (CSRX after `PRINT "ABCD",`=8, not 4). This
    settled the open edge case; sb-core's `(cur_x/step+1)*step` already matched.
  - **Trailing `;`/`,` suppresses the closing newline** (`LOCATE 0,3:PRINT "X";` → CSRY=3; same
    for `,`); no trailing separator emits one newline (`PRINT "X"` → CSRY=4, CSRX=0); a **bare
    `PRINT` emits exactly one line break** (CSRY=4).
  - 10 cases frozen in `spec/tests/print.yaml` (replayed by the overlay gate); 4 new `vm.rs`
    unit tests (boundary-advance, num;str, trailing-comma, bare-PRINT blank line). sb-core
    matched every harvested value with **no code change** — PRINT's separator/newline lowering
    was already correct. Harvest TSVs in `harness/harvest/out/print_out{,2}.tsv`.
- **Progress (2026-06-24, run 6):** **OUT** promoted `disassembled` → **hw_verified** (Control
  22/27 → 23/27; coverage 53.6% → **54.0%** hw_verified, 133 → 134). OUT is the DEF/CALL
  multiple-return-value receiver; harvested against real SB 3.6.0 (Azahar) via `run_case.py`
  progcase (value readback through a final PRINT) + errcase. The DEF block runs to END and the
  whole program survives to SAVE, so OUT's values ARE batch-capturable (unlike END/STOP, whose
  halt/pause headline is structurally un-capturable headless and which honestly stay
  `disassembled`).
  - **Multi-return works as documented.** Canonical `DEF CALCPM A,B OUT OP,OM` (def.md DEF(4))
    called `CALCPM 5,10 OUT P,M` → OP=15, OM=50 (`PRINT P*100+M`=1550). Paren-free arg list,
    statement-form call with an `OUT v1,v2` tail. Also: 2-OUT no-input `DEF MULTI OUT A,B`
    (A=7,B=9)→79; 3-OUT `TRI`→123; string `DEF GS OUT S$`→"HI"; **OUT overwrites the receivers**
    (seeded X,Y=9,9 → 1,2 = 12).
  - **Discovery — `DEF NAME(args) OUT v` is a Syntax error (errnum 3) at the DEF line.** The
    parenthesised `(args)` header is the single-return *function* form and cannot be combined
    with OUT params; the multi-output form omits the parens. Confirmed via errcase
    (`DEF H(N) OUT D` → errnum=3, errline=1) across four header variants.
  - **sb-core fidelity fix.** The parser accepted `DEF F(N) OUT D` (it parsed `(N)` as function
    params, then left the trailing `OUT D` to mis-parse → the call hit errnum 16, not 3). Added
    a guard in `parse_def`: after the function-form `)`, a following `OUT` → Syntax error (3).
    The other 5 OUT behaviors already matched with no code change.
  - 6 cases frozen in `spec/tests/out.yaml` (overlay gate) + 4 inline tests in `out.yaml`; OUT
    added to `IN_SCOPE_CONTROL` so the inline tests replay too. 1241→… (overlay corpus now 81
    curated overlay cases; conformance `corpus_and_overlay_cases_pass` 358 curated, green).
    Harvest TSV in `harness/harvest/out/out_out.tsv`.
- **Progress (2026-06-24, run 7):** **WAIT** and **VSYNC** promoted `disassembled` →
  **hw_verified** (Data operations 11/14 → **13/14**; coverage 54.0% → **54.8%** hw_verified,
  134 → 136). Their headline frame-wait looks un-capturable headless (the program just blocks),
  but the handler busy-yields ONE VBlank at a time (`swi 0xa`) and stops EXACTLY when the
  free-running frame counter `[0x315ec0]` reaches the target (`cmp r0,r5/bge`) — it can never
  overshoot — so the **MAINCNT delta is frame-precise and deterministic**. Harvested against
  real SB 3.6.0 (Azahar) via `run_case.py prog` (`M0=MAINCNT:WAIT n:?MAINCNT-M0`) + errcase.
  - **WAIT counts from "now":** delta == count exactly — `WAIT`(omitted)=1, `WAIT 30`=30,
    `WAIT 5`=5; `WAIT 0`=0 and `WAIT -5`=0 (negative treated as 0, no error — confirms the
    `count<=0 -> skip` guard, not an errnum). `A=WAIT(1)` → errnum 4 errline 1.
  - **VSYNC counts from the previous VSYNC:** with a leading `VSYNC 0` pinning lastVsync,
    delta == count (`VSYNC`=1, `VSYNC 30`=30); `VSYNC 0`=0, `VSYNC -5`=0. `A=VSYNC(1)` → errnum 4.
  - **Decisive cross-check of the WAIT/VSYNC anchor difference:** `VSYNC 0 → WAIT 5 → VSYNC 30`
    gives a delta of **35**, not 30 — because WAIT re-anchors lastVsync on exit (`str current ->
    [0x315ee8]`), so the following VSYNC 30 measures a full 30 frames from the END of the WAIT.
    This confirms both handlers' lastVsync writes from observable behavior alone.
  - **sb-core matched every harvested value with NO code change** — the M4-T3 `clock.rs`
    FrameClock (`wait`/`vsync`/`maincnt`) already models the anchor semantics exactly (verified
    via `sb-run` on all 6 delta programs: 30/1/0/30/35/0). 7+6 overlay cases frozen in
    `spec/tests/{wait,vsync}.yaml`; the in-scope (`IN_SCOPE_DATA_OPS`) inline tests upgraded
    from trivial `stdout:""` to the verified MAINCNT-delta forms. Harvest TSV in
    `harness/harvest/out/waitvsync.tsv`. Only `DIALOG` remains `disassembled` in Data ops
    (interactive modal, not headless-capturable).

- **Progress (2026-06-24, run 8):** **RGB** promoted `disassembled` → **hw_verified**
  (Graphics 1/19 → **2/19**; coverage 54.8% → **55.2%** hw_verified, 136 → 137). RGB is a
  pure value function (build an ARGB8888 color code) — no framebuffer state — so its ENTIRE
  observable contract is value/errnum-deterministic and harvestable headless. Most of it was
  already oracle-confirmed (batch s_t7e: value packing 3-arg + 4-arg alpha, per-channel clamp
  high + mixed-neg, argcount errnum 4); this run closed the single remaining gap — the
  string-channel **errnum 8** (Type mismatch) — against real SB 3.6.0 (Azahar) via `run_case.py
  batch |err`:
  - `X=RGB("A",2,3)` (string in the 3-arg R slot) → errnum 8 errline 1; `X=RGB(1,"B",3,4)`
    (string in the 4-arg G slot) → errnum 8 errline 1. Confirms the disassembled deferred-error
    path (arg-vtable `[r2,#0x40]` string fetch fails → errnum 8 @0x155058).
  - With value/clamp/argcount (s_t7e) + string type-mismatch (this run) all hw_verified, RGB's
    whole contract is oracle-confirmed, so the top-level confidence is honestly flipped to
    `hw_verified` (nothing oracle-pending — unlike the framebuffer-headline Graphics commands
    around it, which stay `disassembled` pending the O-T6 composite golden). sb-core already
    matched both new cases with no code change (string channel → `to_int()` → errnum 8). 2 new
    inline tests in `rgb.yaml` (replayed via `IN_SCOPE_GRAPHICS`). Harvest TSV in
    `harness/harvest/out/rgb_type.tsv`.

- **Progress (2026-06-24, run 9):** **RGBREAD** promoted `disassembled` → **hw_verified**
  (Graphics 2/19 → **3/19**; coverage 55.2% → **55.6%** hw_verified, 137 → 138). RGBREAD is the
  exact inverse of RGB (run 8): a pure shift+mask channel split with no framebuffer state, so
  its whole contract is value/errnum-deterministic. The value round-trip was the last piece
  queued in HARVEST_QUEUE S-T7e item (b) — previously thought un-batchable because RGBREAD is a
  statement writing OUT vars, but the batch `prog`/`progstr` path (setup runs the RGBREAD, then
  capture `STR$(R)+","+...`) harvests it cleanly. Against real SB 3.6.0 (Azahar) via
  `run_case.py batch`:
  - **Channel split = exact inverse of RGB().** `RGBREAD &HFF804020 OUT R,G,B` → 128,64,32;
    4-OUT `RGBREAD &H80FF8040 OUT A,R,G,B` → 128,255,128,64; `RGBREAD -1 OUT A,R,G,B` →
    255,255,255,255; `RGBREAD 0 OUT A,R,G,B` → 0,0,0,0; round-trip `RGBREAD RGB(160,128,96) OUT
    R,G,B` → 160,128,96. Confirms the disassembled extract (B=`and #0xff`, R/G=`lsl/lsr#0x18`,
    A=`lsr#0x18`).
  - **Corpus alpha-only tail-comma form works.** `RGBREAD &H80FF8040 OUT A,,,` → A=128 (R/G/B
    discarded) — the verified `RGBREAD C OUT P,,,` form (13D4DV3V/TXT/MAIN_PRG_V2) confirmed
    on hardware. sb-core's VM already supports the empty OUT receivers.
  - **errnum guards:** 2-OUT (`RGBREAD 0 OUT R,G`) → 4, 5-OUT (`RGBREAD 0 OUT A,R,G,B,X`) → 4,
    string color (`RGBREAD "X" OUT R,G,B`) → 8 errline 1 (the deferred type-mismatch @0x154df0).
  - sb-core matched every harvested value with **no code change** (verified all 3 new forms via
    `sb-run`: 128 / 0,0,0,0 / ERRNUM=8). 3 new inline tests in `rgbread.yaml`
    (split_black, alpha_only_tailcomma, string_color_errors), replayed via `IN_SCOPE_GRAPHICS`;
    top-level `confidence` flipped to `hw_verified` (whole contract oracle-confirmed). Harvest
    TSV in `harness/harvest/out/rgbread.tsv`.

- **Progress (2026-06-24, run 10):** **GCOLOR** promoted `disassembled` → **hw_verified**
  (Graphics 3/19 → **4/19**; coverage 55.6% → **56.0%** hw_verified, 138 → 139). GCOLOR is the
  graphics draw-color analog of BACKCOLOR (run 2): the SET/GET pair is a pure value round-trip
  over draw-state field +0xc with no framebuffer, so the whole VALUE contract is harvestable
  headless. Against real SB 3.6.0 (Azahar) via `run_case.py batch` prog path:
  - **Draw color round-trips FULL 32-bit ARGB verbatim — alpha NEVER stripped.** This is the
    decisive contrast with BACKCOLOR's 24-bit mask (`& &H00FFFFFF`): `GCOLOR &HFF408080:?GCOLOR()`
    =-12550016, `GCOLOR -1:?GCOLOR()`=-1 (not &HFFFFFF), `GCOLOR &H7F112233:?GCOLOR()`=2131829299,
    `GCOLOR RGB(255,64,128,32):?GCOLOR()`=-12550112, `GCOLOR 0`→0. Confirms the disassembled
    `str r0,[state+0xc]` no-range-check store (the alpha byte is the opacity flag, A=255 opaque).
  - **Form guards:** statement OUT spelling `GCOLOR 50:GCOLOR OUT C`→50; `GCOLOR OUT A,B` (2 OUT
    receivers) → errnum 4, `GCOLOR 5 OUT C` (1 arg + 1 return) → errnum 4 (joining the prior
    hw_verified no-arg/2-arg → 4). RGB's whole value contract was already done (run 8); GCOLOR
    completes the GET-form draw-state pair, so the top-level `confidence` honestly flips.
  - **Discovery — bare `GCOLOR()` as a STATEMENT → errnum 3 (Syntax error), not 4.** A side
    sweep (`harness/harvest/out/exprstmt.tsv`) shows this is the general expr-as-statement rule:
    `RGB(1,2,3)`, `GCLS()`, `GPAGE()`, a bare `5`, `1+2` all → errnum 3, while `ABS(5)` → errnum 4
    — so the 3-vs-4 split is decided by the keyword/command table BEFORE the handler runs. sb-core
    currently returns 4 for the bare `GCOLOR()` statement; this is a **cross-instruction parser
    refinement, not a GCOLOR slice**, so it is recorded as a hw_verified discovery in the spec
    sources + **queued in HARVEST_QUEUE.md** and deliberately NOT frozen as a conformance case.
  - sb-core matched every value + the two new errnum-4 guards with **NO code change** (verified
    all forms via `sb-run`). 9 inline cases now hw_verified in `gcolor.yaml` (4 new full-32-bit
    round-trips + OUT-stmt + 2 new errnum-4 guards), replayed via `IN_SCOPE_GRAPHICS`. Harvest
    TSVs in `harness/harvest/out/{gcolor,exprstmt}.tsv`.

- **Progress (2026-06-24, run 11):** **GPAGE** promoted `disassembled` → **hw_verified**
  (Graphics 4/19 → **5/19**; coverage 56.0% → **56.5%** hw_verified, 139 → 140). GPAGE's error
  guards were already hw_verified (s_t7a: argcount → 4, page range → 10); the remaining piece —
  the SET/GET page round-trip — is a pure draw-state value pair (display field +0x30,
  manipulation field +0x34) with no framebuffer, so it harvests headless via the prog
  OUT-readback path. Against real SB 3.6.0 (Azahar) via `run_case.py batch`:
  - **SET/GET round-trips exact:** `GPAGE 1,2:GPAGE OUT V,W` → 1,2; `GPAGE 0,0` → 0,0;
    `GPAGE 5,5` → 5,5; `GPAGE 3,4` → 3,4. The display≠manip case `GPAGE 2,5` → 2,5 **proves the
    field ordering** V=display (+0x30), W=manipulation (+0x34) — not swapped.
  - With argcount/range (s_t7a) + the value round-trip (this run) all hw_verified, GPAGE's whole
    headless-observable contract is oracle-confirmed (the on-screen display effect itself needs
    the O-T6 composite golden, but the page-number state is fully value-deterministic), so the
    top-level `confidence` honestly flips. sb-core matched every value with **NO code change**
    (verified all 5 forms via `sb-run`). 2 new inline cases (`roundtrip_max`, `roundtrip_order`)
    added to `gpage.yaml`, replayed via `IN_SCOPE_GRAPHICS`. Harvest TSV in
    `harness/harvest/out/gpage_rt.tsv`.

- **Progress (2026-06-24, run 12):** **BGVAR** promoted `disassembled` → **hw_verified** (BG
  1/23 → **2/23**; coverage 56.5% → **56.9%** hw_verified, 140 → 141). BGVAR's error guards +
  default-zero read were already hw_verified (s_t9d 2026-06-22); the remaining piece — the
  write-then-read **VALUE round-trip** (the old single-expression value harvester couldn't run a
  setter statement first) — was harvested against real SB 3.6.0 (Azahar) via `run_case.py batch`
  **prog** (setup-then-read) path. BGVAR carries eight per-layer scratch doubles with no
  framebuffer, so its whole value contract is value-deterministic (like GPAGE/GCOLOR's GET pairs):
  - **Write→read round-trips exact, across all forms.** `BGVAR 0,3,7:?BGVAR(0,3)`=7 (function
    read); `BGVAR 0,5,42:BGVAR 0,5 OUT V`→V=42 (**OUT-form read**, resolves the queued OUT-V case).
    The slot stores a **double verbatim** — `BGVAR 0,2,3.5`→3.5, `BGVAR 0,0,-12.25`→-12.25 (no int
    truncation), `BGVAR 0,7,&H7FFFFFFF`→2147483647 (STR$ "2.14748e+09" = the full i32 as a double).
  - **Per-layer / per-var independence + persistence:** sibling var unaffected
    (`BGVAR 0,0,10:BGVAR 0,1,20`→BGVAR(0,1)=20), sibling layer unaffected
    (`BGVAR 0,0,11:BGVAR 1,0,22`→BGVAR(0,0)=11), overwrite (`5` then `8`→8); layers 2 and 3 also
    round-trip (`BGVAR 2,4,123`→123, `BGVAR 3,6,-1`→-1).
  - **NO BGSCREEN setup required** — `BGVAR 1,1,55`→55 with no prior BGSCREEN, confirming the
    disassembled "every layer 0..3 always exists" note (the old spec tests needlessly prefixed
    BGSCREEN; dropped). var 7 round-trips its value despite the flag-bit-0x20 clear
    (`BGVAR 0,7,99`→99).
  - sb-core matched **every** harvested value with **NO code change** (verified all forms via
    `sb-run`; it stores `to_real()` and reads back `Real(..)`). Top-level `confidence` honestly
    flips — the only un-harvested piece is var 7's flag-bit-0x20 *transform side effect* (the
    BGANIM "V"-channel marker), observable only through the BG framebuffer (O-T6), which stays
    queued. 13 inline value cases now hw_verified in `bgvar.yaml` (incl. doubles/negatives/OUT/
    no-BGSCREEN/independence/i32), replayed via `IN_SCOPE_BG`. Harvest TSV in
    `harness/harvest/out/bgvar_rt.tsv`.

- **Progress (2026-06-24, run 13):** **BGOFS** promoted `disassembled` → **hw_verified** (BG
  2/23 → **3/23**; coverage 56.9% → **57.3%** hw_verified, 141 → 142). BGOFS's argcount/layer
  error guards were already hw_verified (s_t9c); this run harvested the SET→OUT offset VALUE
  round-trip + the depth range guard against real SB 3.6.0 (Azahar) via `run_case.py batch`
  prog readback, resolving the queued "exact stored offset + Z clamping" item.
  - **X,Y stored VERBATIM as i32 — no wrap to layer pixel size, no clamp.** `BGOFS 0,16,-32`
    OUT→16,-32; `0,1000,0`→1000; `0,0,5000`→5000; `0,-1,0`→-1; `0,-1000,0`→-1000. A fractional
    operand truncates toward zero via the integer getter (16.7→16, -16.7→-16). No BGSCREEN is
    required (layers exist always; construction default X=Y=Z=0). Omitted SET coordinate keeps
    current (`BGOFS 0,7,8:BGOFS 0,,99` OUT→7,99); per-layer independence (`BGOFS 2,123,0`→123).
  - **Z (depth) is NOT clamped — it is RANGE-CHECKED to -256..1024 inclusive → errnum 10.** The
    headline discovery: `BGOFS 0,0,0,2000` / `1025` / `-1000` / `-257` → **errnum 10 errline 1**,
    while `1024` and `-256` are accepted and stored verbatim. Confirmed in the disassembly: the
    handler loads the f32 depth and does `vcmpe.f32 s0,s17; bcc err` / `vcmpe.f32 s0,s16; ble ok`
    @0x1644b8-0x1644cc against the literal bounds @0x164658=0xc3800000=**-256.0** and
    @0x16465c=0x44800000=**1024.0**, raising via `mov r0,#0xa; bl 0x1fffdc` @0x1644d8. The 3-arg
    SET keeps an already-set Z (`0,0,0,500:BGOFS 0,9,9` OUT→Z=500), so the guard fires only on
    the explicit 4-arg depth.
  - **sb-core fix:** added the -256..1024 Z guard to `bgofs()` (`builtins/bg.rs`); it previously
    stored Z verbatim with no range check (would not error on Z=2000). Every value round-trip
    already matched (verbatim i32 store/read). 16 inline cases now hw_verified in `bgofs.yaml`
    (8 offset round-trips + 8 Z round-trip/range), replayed via `IN_SCOPE_BG`, plus a
    `bgofs_z_range_guard` unit test. The on-screen scroll/depth EFFECT still needs the BG
    framebuffer golden (O-T6); the stored offset/depth STATE is now fully value-deterministic.
    Harvest TSVs in `harness/harvest/out/bgofs_{rt,zerr,def,zkeep}.tsv`.

- **Progress (2026-06-24, run 14):** **SPOFS** promoted `disassembled` → **hw_verified**
  (Sprites 8/27 → **9/27**; coverage 57.3% → **57.7%** hw_verified, 142 → 143). SPOFS is the
  sprite analog of BGOFS (run 13): the position/depth SET→OUT pair is a pure value contract
  (sprite slot fields +0x30/+0x34/+0x38, no framebuffer) so its whole VALUE contract harvests
  headless. The error guards were already hw_verified (s_t8b: mgmt 0..511→10, before-SPSET→4,
  bad argcount→4); this run harvested the value round-trip + the Z depth range against real SB
  3.6.0 (Azahar) via `run_case.py batch` prog readback + err.
  - **X,Y stored verbatim, incl. fractional + negative.** `SPOFS 0,50,80`→50,80; `0,-30,-60`→
    -30,-60; `0,16.5,-16.5`→16.5,-16.5 (fractional preserved — coords are 32-bit FLOATS, the
    decisive contrast with BGOFS's i32-truncated offsets). Empty-arg skip keeps current coord
    (`SPOFS 0,50,80:SPOFS 0,,,1000`→50,80,1000; `:SPOFS 0,,99`→50,99). A 3-arg SET leaves the
    stored Z unchanged (`SPOFS 0,0,0,500:SPOFS 0,9,9`→9,9,500). Per-sprite independence.
  - **Z (depth) is RANGE-CHECKED to -256..1024 inclusive → errnum 10** (SAME bounds as BGOFS):
    `SPOFS 0,0,0,1025` / `-257` / `5000` / `-2000` → **errnum 10 errline 1**, while `1024` and
    `-256` are accepted and stored verbatim. The 4-arg path reads the depth through the bounded
    float-arg helper at 0x1eeb9c (bl @0x1411ec) with the two literal bounds 1024.0 (0x44800000
    @0x141458) and -256.0 (0xc3800000 @0x14145c): `vcmpe.f32 s0,s17; bcc err` / `vcmpe.f32
    s0,s16; ble ok` @0x1eebd8-0x1eebf0, raising via `mov r0,#0xa; b 0x1fffdc` @0x1eec04.
  - **sb-core fix:** added the -256..1024 Z guard to `spofs()` (`builtins/sprite.rs`); it
    previously stored Z verbatim with no range check. Every value round-trip already matched
    (verbatim store/read; sb-core uses f64). 18 inline cases now hw_verified in `spofs.yaml`
    (round-trips + skip-keep + Z bounds/range + error guards), replayed via `IN_SCOPE_SPRITES`,
    plus `spofs_z_range_guard` + `spofs_value_round_trip` unit tests. The on-screen render
    EFFECT still needs the sprite framebuffer golden (O-T6); the stored position/depth STATE is
    now fully value-deterministic. **Queued (HARVEST_QUEUE):** sprite coords are f32 storage
    (disassembled) but sb-core stores f64 — undistinguishable via 6-sig STR$, harvestable only
    via PRINT %.8f at >2^24 magnitudes; the f32-storage refactor spans every sprite setter.
    Harvest TSVs in `harness/harvest/out/spofs_{rt,zerr}.tsv`.

- **Progress (2026-06-24, run 15):** **BGCOLOR** promoted `disassembled` → **hw_verified** (BG
  3/23 → **4/23**; coverage 57.7% → **58.1%** hw_verified, 143 → 144). BGCOLOR's layer-range
  guards were already hw_verified (s_t9e); this run harvested the SET→GET color round-trip — the
  last queued question: does the layer's stored color strip alpha (24-bit, like BACKCOLOR) or
  keep the full 32 bits (like GCOLOR)? Against real SB 3.6.0 (Azahar) via `run_case.py batch`
  progstr with `HEX$(C,8)` readback (unambiguous 8-digit):
  - **Color stored VERBATIM as full 32 bits — alpha NEVER stripped.** This is the decisive
    contrast with BACKCOLOR's 24-bit mask (run 2): `BGCOLOR 0,&H7F112233`→**&H7F112233** (not
    &H112233), `BGCOLOR 0,-1`→**&HFFFFFFFF** (not &HFFFFFF), `&H80FF8040`→&H80FF8040, `0`→0. The
    default per-layer color is **&HFFFFFFFF** (opaque white = no tint). Function form
    `C=BGCOLOR(0)` reads &H12345678 after the SET; layer 3 round-trips &HABCDEF01; layers are
    independent; **no BGSCREEN required** (`BGCOLOR 2,&H44556677` reads back on a fresh state).
    errnum guards: string color (`BGCOLOR 0,"X"`) → errnum 8, 3-arg SET (`BGCOLOR 0,1,2`) →
    errnum 4 — both errline 1.
  - Confirmed in the disassembly: the apply helper FUN_001163c8 does a plain `str r1,[state,#0x30]`
    (load-compare-store, **no AND mask**) and GET reads the same word back, so SET→GET is a
    bit-exact 32-bit round-trip. sb-core matched **every** harvested value with **NO code change**
    (it stores `to_int() as u32` verbatim, reads `as i32`; verified all forms via `sb-run`).
    Top-level `confidence` flips — only the on-screen tint (color-code × pixel) stays queued
    (needs the O-T6 BG framebuffer golden). 12 inline cases now hw_verified in `bgcolor.yaml`
    (8 value round-trips + 2 errnum + 2 layer-range), replayed via `IN_SCOPE_BG`. HARVEST_QUEUE
    BGCOLOR round-trip item resolved. Harvest TSV in `harness/harvest/out/bgcolor_rt.tsv`.

- **Progress (2026-06-24, run 16):** **BGROT** promoted `disassembled` → **hw_verified** (BG
  4/23 → **5/23**; coverage 58.1% → **58.5%** hw_verified, 144 → 145; 1365 → 1380 frozen cases).
  BGROT's layer-range / arg-shape guards were already hw_verified (s_t9c); this run harvested
  the last queued question — the **exact mod-360 normalization result** for negative / over-360 /
  fractional angles — against real SB 3.6.0 (Azahar) via `run_case.py batch` prog readback
  (`BGROT 0,A:BGROT 0 OUT R`).
  - **Angle is normalized to 0..359 by a truncated-toward-zero remainder + a +360 fixup.**
    hw_verified round-trips: 45→45, 180→180, 359→359, 360→0, 361→1, 450→90, 720→0, -1→359,
    -90→270, -360→0, -450→270, 100000→280. This matches the disassembly's reciprocal-multiply
    mod (smlal/asr @0x164938) with the negative correction `addmi r0,r0,#0x168` @0x164968 — i.e.
    C `%` semantics, NOT a Euclidean wrap (they agree here since both land in 0..359, but the
    intermediate remainder is the truncated one).
  - **Fractional input truncates toward zero THEN mods:** 450.9→90, -90.9→270 (confirms the
    integer getter truncates before the mod, consistent with SPROT 11.2→11 / BGOFS 16.7→16).
  - **BGSCREEN is NOT required** (`BGROT 2,90:BGROT 2 OUT R`→90 — BG layers exist always, same as
    BGOFS run 13); a fresh layer's default angle is **0**; layers are independent
    (`BGROT 0,90:BGROT 1,180:BGROT 1 OUT R`→180).
  - sb-core's `normalize_angle(a.to_int())` (`sb-render::bg`) matched **every** harvested value
    with **NO code change**. 20 inline cases now hw_verified in `bgrot.yaml` (17 round-trips +
    3 error guards), replayed via `IN_SCOPE_BG`, plus a `set_rot_normalizes_mod_360` unit test
    covering the 12 integer boundary edges. The on-screen rotation EFFECT still needs the BG
    framebuffer golden (O-T6); the stored/returned angle STATE is now fully value-deterministic.
    The bgrot.yaml inline "oracle-pending mod-360" queue note is resolved. Harvest TSV in
    `harness/harvest/out/bgrot_rt.tsv`.

- **Progress (2026-06-24, run 17):** **BGSCALE** promoted `disassembled` → **hw_verified** (BG
  5/23 → **6/23**; coverage 58.5% → **58.9%** hw_verified, 145 → 146). BGSCALE's argcount/layer
  error guards were already hw_verified (s_t9c); this run harvested the queued "exact stored/
  returned scale" item — the SET→OUT scale VALUE round-trip — against real SB 3.6.0 (Azahar) via
  `run_case.py batch` progstr readback (`BGSCALE l,sx,sy:BGSCALE l OUT SX,SY:STR$(SX)+","+STR$(SY)`).
  - **Scale round-trips VERBATIM with NO clamp, X/Y independent.** `1.5,2.0`→1.5,2; `0.5,0.5`→
    0.5,0.5; `4,4`→4,4 (above the documented 2.0 max — no clamp); `0.4,0.4`→0.4,0.4 (below 0.5 —
    no clamp); `1,0.6`→1,0.6 (X≠Y kept independent); `0,0`→0,0; `-1,-1`→-1,-1; layer 3
    `0.25,0.25`→0.25,0.25. Confirms the disassembled verbatim float store (`blx [r2,#0x60]` eval →
    layer record, no range guard; read back `[r0,#0x20]` `vcvt.f64.f32`).
  - **Fresh default = 1.0,1.0** (hw_verified via `ACLS:BGSCALE 0/2 OUT SX,SY`→1,1). **No BGSCREEN
    required** — `BGSCALE 1,1.5,2:BGSCALE 1 OUT`→1.5,2 on a fresh state (BG layers 0..3 always
    exist, same as BGOFS run 13 / BGROT run 16); per-layer independence confirmed (set layer 0 +
    layer 1, read layer 0 unaffected). **Harvest gotcha noted:** an empty-setup default read is
    contaminated by prior cases (BG scale state persists across program RUNs in real SB), so the
    true default needs a leading `ACLS`.
  - sb-core matched **every** harvested value with **NO code change** (verified all 8 round-trips
    + default via `sb-run`; it stores `to_real()`/reads `Real(..)`, default 1.0). Top-level
    `confidence` flips — the only un-harvested piece is the on-screen scaling EFFECT (color×pixel
    transform), which needs the O-T6 BG framebuffer golden. 12 new inline cases now hw_verified in
    `bgscale.yaml` (8 round-trips + default + no-BGSCREEN + independence + the 2 existing guards),
    replayed via `IN_SCOPE_BG`. **Queued (HARVEST_QUEUE):** BG scale is f32 storage on hardware
    but sb-core stores f64 — indistinguishable at 6-sig STR$ (same refactor decision as the sprite
    setters). Harvest TSVs in `harness/harvest/out/bgscale_{rt,def}.tsv`.

- **Progress (2026-06-24, run 18):** **SPSCALE** implemented + promoted `disassembled` →
  **hw_verified** (Sprites 9/27 → **10/27**; coverage 58.9% → **59.3%** hw_verified, 146 → 147).
  SPSCALE is the sprite analog of BGSCALE (run 17) but was **never routed in sb-core** (no
  `spscale` builtin — only the `scale_x/scale_y` fields existed on the sprite slot, default 1.0);
  this run wrote the builtin (`builtins/sprite.rs::spscale`), wired the dispatch + `IN_SCOPE_
  SPRITES`, and harvested the value contract against real SB 3.6.0 (Azahar) via `run_case.py
  batch` progstr readback (`SPSCALE l,sx,sy:SPSCALE l OUT SX,SY:STR$(SX)+","+STR$(SY)`) + err.
  - **Scale round-trips VERBATIM with NO upper clamp, X/Y independent.** `1.5,2.0`→1.5,2;
    `0.5,0.5`; `4,4` (above documented 2.0 — no clamp); `0.4,0.4` (below 0.5 — no clamp);
    `1,0.6` (X≠Y kept); `400,8` + `0.125,0.125` (real corpus values); `1000,1000`; `0,1`; tiny
    `0.001,1`. Fresh-`SPSET` default = **1,1** (hw_verified). Per-sprite independence confirmed.
  - **Headline discovery — each axis is RANGE-CHECKED ≥ 0.0 → errnum 10** (the decisive contrast
    with BGSCALE, which stores negatives verbatim): `SPSCALE 0,-1,-1` / `0,-1,1` / `0,1,-1` /
    `0,-0.001,1` → **errnum 10 errline 1**, while `0,0` is accepted. Confirmed in the
    disassembly: each scale arg is read through the **minimum-bound** float getter FUN_001eec18
    (bl @0x143c6c/@0x143c90) with the literal bound **0.0** (`s16` @0x143d2c=0x00000000): it
    evaluates the arg then `vcmpe.f32 s1,s0; bls ok` (s1=bound 0.0, s0=value → OK iff value≥0),
    else `mov r0,#0xa; b 0x1fffdc` @0x1eec60-70 → errnum 10. A **void/non-numeric** scale
    (`SPSCALE 0,,0.5` / `0,0.5,`) → **errnum 8** (the getter's eval fails type before the range
    check). SET requires exactly 3 args (`0,1` / `0,1,1,1` → errnum 4); mgmt∉0..511 → 10;
    before-SPSET → 4.
  - **sb-core:** new `spscale()` mirrors `bgscale`/`spofs` but adds the per-axis `< 0.0` →
    errnum 10 guard (X fully evaluated+checked before Y, matching the handler) over the existing
    `scale_x/scale_y` slot fields; routed in `vm.rs` + added to `IN_SCOPE_SPRITES`. 17 inline
    cases now hw_verified in `spscale.yaml` (round-trips + default + independence + ≥0 range +
    void type + argcount/mgmt/before-SPSET guards), replayed via `IN_SCOPE_SPRITES`, plus a
    `spscale_round_trip_and_guards` unit test. The on-screen scaling EFFECT still needs the
    sprite framebuffer golden (O-T6); the stored scale STATE is now fully value-deterministic.
    **Queued (HARVEST_QUEUE):** sprite scale is f32 storage on hardware but sb-core stores f64
    — indistinguishable at 6-sig STR$ (same refactor decision as the other sprite setters).
    Harvest TSVs in `harness/harvest/out/spscale_{rt,neg}.tsv`.

- **Progress (2026-06-24, run 19):** **SPROT** implemented + promoted `disassembled` →
  **hw_verified** (Sprites 10/27 → **11/27**; coverage 59.3% → **59.7%** hw_verified, 147 → 148).
  SPROT is the sprite analog of BGROT (run 16) but was **never routed in sb-core** (the
  `assert!(!is_builtin("SPROT"))` "later increment" placeholder) — this run wrote the builtin
  (`builtins/sprite.rs::sprot`), registered it in `BUILTIN_NAMES` (it has a function-form get
  `A=SPROT(mgmt)`, so it must compile as a call not a variable — unlike SPSCALE/SPOFS which are
  OUT-only), wired the dispatch + `IN_SCOPE_SPRITES`, and harvested the value contract against
  real SB 3.6.0 (Azahar) via `run_case.py batch` prog readback + the already-`hw_verified` error
  guards (s_t8b).
  - **Decisive contrast with BGROT: SPROT does NOT normalize mod-360 — it stores the angle as a
    SIGNED 16-bit halfword, VERBATIM.** Truncate-toward-zero then `sxth`: `SPROT 0,45`→45,
    `0,-25`→-25, `0,450`→450 (NOT wrapped to 0..359 like BGROT), `0,11.2`→11, `0,-11.9`→-11
    (toward zero, not floor). The signed-16-bit wrap is the headline new harvest: `0,32768`→
    **-32768**, `0,40000`→**-25536**, `0,65536`→**0**, `0,70000`→**4464**, `0,32767`→32767,
    `0,-32768`→-32768. Fresh-`SPSET` default 0; per-sprite independence.
  - Confirmed in the disassembly: the SET apply helper FUN_0017eaa4 sign-extends the
    (already-truncated) integer to 16 bits (`sxth r1,r1` @0x17eab4) and stores it as a halfword
    (`strh r1,[r0,#0x28]` @0x17eac4); GET reads it back signed (`ldrsh r1,[r4,#0x28]` @0x141578).
    The helper ALSO writes a separate mod-360 field [+0x2a] (`addmi r1,r1,#0x168` @0x17eae4) used
    only by the renderer — SPROT() returns the verbatim [+0x28] value, NOT the normalized one
    (which is why the get is verbatim while BGROT's is mod-360).
  - **sb-core:** new `sprot()` mirrors the BGOFS/SPOFS get/set shape but stores `to_int() as i16`
    and returns `Value::Int(.. as i16 as i32)` over the existing `rot` slot field; routed in
    `vm.rs` + added to `IN_SCOPE_SPRITES` + `BUILTIN_NAMES`; the M3 `assert!(!is_builtin("SPROT"))`
    flipped to `assert!(is_builtin("SPROT"))`. Verified every value via `sb-run` (45/450/-25536/
    4464/-11/-32768/0 + the three error guards). 10 new inline cases (wrap_32768/40000/65536/70000,
    max/min_i16, neg_fraction_trunc, per_sprite_independence, fresh_default_zero, set_bad_argcount)
    now hw_verified in `sprot.yaml`, replayed via `IN_SCOPE_SPRITES`, plus a
    `sprot_round_trip_wrap_and_guards` unit test. The on-screen rotation EFFECT still needs the
    sprite framebuffer golden (O-T6); the stored/returned angle STATE is now fully
    value-deterministic. Harvest TSV in `harness/harvest/out/sprot_rt.tsv`.

- **Progress (2026-06-24, run 20):** **SPHOME** implemented + promoted `disassembled` →
  **hw_verified** (Sprites 11/27 → **12/27**; coverage 59.7% → **~60.1%** hw_verified, 148 →
  149). SPHOME is the sprite reference/home-point setter (the SPOFS origin + rotation/scale/
  collision pivot) — it was **never routed in sb-core** (the `home_x/home_y` slot fields existed
  but no builtin); this run wrote the builtin (`builtins/sprite.rs::sphome`), wired the dispatch
  + `IN_SCOPE_SPRITES`, and harvested the value contract against real SB 3.6.0 (Azahar) via
  `run_case.py batch` progstr readback (`SPHOME 0,X,Y:SPHOME 0 OUT HX,HY`) + err cases.
  - **Home round-trips as a VERBATIM FLOAT — no range/sign/integer constraint** (the contrast
    with BG's integer grid home): `0,16,16`→16,16; `0,-16,-16`→-16,-16; `0,127.5,127.5`→127.5,
    127.5; `0,16.25,-8.5`→16.25,-8.5; `0,0.1,0.2`→0.1,0.2. Fresh-`SPSET` default = **0,0**;
    per-sprite independence confirmed (`SPHOME 0,5,6:SPHOME 1,7,8`→5,6 / 7,8). The disassembly
    matches: SET applies via FUN_001ee524 (`vldmia sp,{s0,s1}` / `bl 0x1ee524` @0x142884) storing
    f32 at slot+0x18 with no guard; GET reads it back (`vldr.32 s0,[r4,#0x18]` @0x1428a4).
  - **Headline new harvest — argcount + void guards.** SET requires exactly 3 args: `0,16`
    (2 args) and `0,1,2,3` (4 args) → **errnum 4 errline 1**. A **void coordinate** `0,,16` →
    **errnum 8** (the slot float getter fails the eval before the store — same type-before-store
    shape as SPSCALE void). mgmt∉0..511 → errnum 10; before-`SPSET` → errnum 4 (both re-confirmed).
  - **sb-core:** the renderer's sprite `home_x/home_y` were **i32** but the handler stores f32
    and accepts fractional/negative offsets (corpus: `SPHOME 2,127.5,127.5` ~85×), so this run
    **widened them to f64** (`sb-render/src/sprite.rs` struct + `Default` + `create()` template
    copy + compositor pivot; BG home stays integer). New `sphome()` mirrors the SPOFS get/set
    shape: 3-arg SET reads each coord via `to_real()?` (void → errnum 8), X before Y, over the
    slot fields; OUT-only (returns 2 values, no function form → NOT in `BUILTIN_NAMES`, like
    SPSCALE/SPOFS). 15 inline cases now hw_verified in `sphome.yaml` (round-trips + default +
    independence + argcount/void/mgmt/before-SPSET guards), replayed via `IN_SCOPE_SPRITES`,
    plus a `sphome_round_trip_and_guards` unit test. The on-screen pivot EFFECT still needs the
    sprite framebuffer golden (O-T6); the stored home STATE is now fully value-deterministic.
    **Queued (HARVEST_QUEUE):** home is f32 storage on hardware but sb-core stores f64 —
    indistinguishable at 6-sig STR$ (same refactor decision as the other sprite setters).
    Harvest TSV in `harness/harvest/out/sphome_rt.tsv`.
- **Progress (2026-06-24, run 21):** **SPPAGE** implemented + promoted `disassembled` →
  **hw_verified** (Sprites 12/27 → **13/27**; coverage ~60.1% → **~60.5%** hw_verified, 149 →
  150). SPPAGE selects/reads the GLOBAL graphic page (GRP0..5) the sprite system renders onto;
  it was **never routed in sb-core** (only the per-sprite sheet `page` field existed). This run
  added a global `page: u8` to `SpriteState` (default GRP4), wrote the `sppage()` builtin
  (`builtins/sprite.rs`), wired dispatch + `IN_SCOPE_SPRITES` + `BUILTIN_NAMES` (the GET is a
  0-arg function form `P=SPPAGE()`, so it must be a known builtin), and harvested the value
  contract against real SB 3.6.0 (Azahar) via `run_case.py batch` prog readback + err cases.
  - **Verbatim round-trip, no transform.** `SPPAGE n : SPPAGE()` returns n for every valid page
    n∈{0,1,2,3,5}; fresh default `SPPAGE()`=4 (GRP4). The disassembly matches: SET fetches the
    page through the bounded getter (lower 0, upper `mov r1,#0x5` @0x142b1c) and commits it
    globally (`bl 0x14ee48` @0x142b44); GET reads `[[0x315d60]+0x4c]` (@0x142b54-0x142b70).
  - **Headline new harvest — the (return,arg)-shape guard.** `SPPAGE 1,2` (SET, 2 args) and
    `R=SPPAGE(0)` (GET, 1 arg) both → **errnum 4 errline 1** (the disassembled shape check at
    @0x142b04 — neither the 1-arg SET nor the 0-arg GET form). Page ∉ 0..5 → errnum 10 (`SPPAGE
    6`/`SPPAGE -1`, re-confirmed; hw_verified s_t8a). Bare `SPPAGE` (no args) reaches the same
    errnum-4 site in the handler, but the oracle reports it as a parse-capture artifact
    (errnum 0 — program never ran), so that exact edge is **not** frozen as a fixture.
  - **sb-core:** new `SpriteState.page` global (default GRP4); `create()` now copies it into a
    new sprite's per-slot `page` at SPSET time (the page active at creation; default unchanged).
    `sppage()` is SET (ret 0, 1 arg, range 0..5 → errnum 10) / GET (ret 1, 0 args) / else
    errnum 4. 10 inline cases now hw_verified in `sppage.yaml` (default + 0..5 round-trips +
    shape/range guards), replayed via `IN_SCOPE_SPRITES`, plus a `sppage_round_trip_and_guards`
    unit test. **Harness fix:** the prior `roundtrip_set_get`/`default_page` cases used YAML
    *single*-quotes, so `\n` stayed a literal backslash-n (the multi-line program never parsed);
    they were dead until SPPAGE entered scope this run — rewritten with double-quoted code. The
    on-screen render-target EFFECT (which GRP a sprite composites onto) still needs the O-T6
    sprite framebuffer golden. Harvest TSV in `harness/harvest/out/sppage_rt.tsv`.
- **Progress (2026-06-24, run 22):** **SPCLR** promoted `disassembled` → **hw_verified**
  (Sprites 13/27 → **14/27**; coverage ~60.5% → **~60.9%** hw_verified, 150 → 151). SPCLR
  was already implemented + routed (`builtins/sprite.rs`); only its top-level confidence was
  `disassembled` because the slot-release EFFECT was thought to need the O-T6 sprite-framebuffer
  golden. **It doesn't** — the effect is fully observable through the routed, hw_verified in-use
  scalar `SPUSED`, so this run harvested the value contract directly against real SB 3.6.0
  (Azahar) via `run_case.py batch` (no framebuffer).
  - **Slot-free effect, hw_verified via SPUSED.** Baseline `SPSET 0,0:?SPUSED(0)`→1; 1-ARG
    `SPCLR 0` frees only the named slot (`?SPUSED(0)`→0 while a co-existing `SPSET 1,0` stays
    `?SPUSED(1)`→1); 0-ARG `SPCLR` is the bulk clear-all — frees BOTH (`?SPUSED(0)`→0 AND
    `?SPUSED(1)`→0), confirming the disassembled clear-all loop @0x13f7fc-0x13f85c; `SPCLR 5`
    on an unused slot is harmless (`?SPUSED(5)`→0); and a freed slot is reusable
    (`SPSET 0,0:SPCLR 0:SPSET 0,0:?SPUSED(0)`→1).
  - **Arg-shape guards re-confirmed + new.** `SPCLR 512`/`SPCLR -1`→errnum 10 errline 1
    (mgmt OOR, 1-arg form, re-confirmed s_t8a); >1 arg `SPCLR 0,1`→errnum 4; and the function
    form `X=SPCLR(0)`→**errnum 4 errline 1** — the return-count!=0 reject (`ldrle [r0,#0xc]
    cmple #0` @0x13f7c4) genuinely reaches the handler, NOT a parse-capture artifact (unlike
    SPPAGE's bare-no-arg edge in run 21).
  - **sb-core:** no code change needed — `spclr()` (`clear`/`clear_all` over `SpriteState`)
    + `spused()` already produce every harvested value. spclr.yaml's 5 inline cases replaced
    with 12 hw_verified cases (8 SPUSED-readback effect cases + 4 arg-shape guards), replayed
    via `IN_SCOPE_SPRITES`. HARVEST_QUEUE S-T8a's "SPCLR slot-free + bulk clear-all" line marked
    resolved. Harvest TSV in `harness/harvest/out/spclr_rt.tsv`.

- **Progress (2026-06-24, run 23):** **SPSET** promoted `disassembled` → **hw_verified**
  (Sprites 14/27 → **15/27**; coverage ~60.9% → **~61.3%** hw_verified, 151 → 152). SPSET
  was already implemented + routed (`builtins/sprite.rs`); its top-level confidence was
  `disassembled` because the creation EFFECT was thought to need the O-T6 sprite-framebuffer
  golden. **It doesn't** — creation is observable through the routed, hw_verified in-use
  scalar `SPUSED` plus the auto-allocate forms' returned management number, so this run
  harvested the value contract directly against real SB 3.6.0 (Azahar) via `run_case.py batch`
  (no framebuffer). Each prog case is **SPCLR-prefixed** — discovery: SB retains sprite state
  across program RUNs, so the first (unprefixed) pass leaked slots and auto-allocate returned
  1,2,3,5,6 instead of 0; the SPCLR clean-slate pass gives the true values.
  - **Creation via SPUSED.** Explicit forms create the named slot: `SPSET 0,0`→`SPUSED(0)`=1
    (and `SPUSED(5)`=0); `SPSET 54,0,0,32,32,1`→`SPUSED(54)`=1; `SPSET 10,0,0,1`→`SPUSED(10)`=1;
    re-`SPSET` is harmless (stays 1).
  - **Auto-allocate picks the LOWEST free slot.** `IX=SPSET(0)`→0; with slot 0 taken→1; with
    0,1 taken→2; `SPSET 0 OUT IX`→0 (OUT == function form); direct form `IX=SPSET(0,0,16,16,1)`
    →0; the allocated slot reads `SPUSED(IX)`=1. Range forms scan `[upper,lower]` low-to-high:
    `IX=SPSET(100,105,0)`→100, with 100 taken→101, `IX=SPSET(5,5,0)`→5, a full single-slot
    range→-1, `IX=SPSET(100,105,0,0,16,16,1)`→100 (form 6).
  - **DISCOVERY — reversed range is errnum 4, not a downward scan.** `IX=SPSET(105,100,0)`
    (upper>lower) → **errnum 4 errline 1**, confirming the handler's `cmp upper,lower / ble ok`
    else errnum 4 @0x141a30-0x141a44. **Fixed sb-core**: `SpriteState::alloc` was scanning
    descending for a reversed range; it's now forward-only, with a new `alloc_range` errnum-4
    guard in `builtins/sprite.rs` for forms 5/6.
  - **DISCOVERY — U+W / V+H > 512 is errnum 10** (was the spec's "errnum pending oracle"
    assumption). `SPSET 0,500,0,20,16` (U+W=520) and `SPSET 0,0,500,16,20` (V+H=520) → errnum 10;
    the `U+W==512` edge (`SPSET 0,496,0,16,16`) is accepted. Matches sb-core's existing `rect()`
    + disasm `cmp #0x44000000` (512.0) `/ bgt errnum-10` @0x141b44/@0x141b5c.
  - **sb-core:** the reversed-range fix is the only code change; lowest-free alloc, range
    scan, and U+W/V+H guards already matched. spset.yaml's 9 inline cases replaced with 23
    hw_verified cases (5 SPUSED-creation + 11 auto-allocate/range value + 7 error guards),
    replayed via `IN_SCOPE_SPRITES`; 1 new `builtins/sprite.rs` unit test
    (`spset_alloc_picks_lowest_and_rejects_reversed_range`); the `sb-render` alloc test renamed
    `alloc_scans_range_low_to_high`. Two HARVEST_QUEUE items (rect overflow errnum + alloc
    tie-break/direction) marked resolved. Harvest TSVs in
    `harness/harvest/out/spset_rt{,2}.tsv`.
- **Progress (2026-06-24, run 24):** **SPLINK** promoted `disassembled` → **hw_verified**
  (Sprites 15/27 → **16/27**). SPLINK was already implemented + routed (`builtins/sprite.rs`
  SET/GET forms over `SpriteState::link`/`parent_of`); most of its contract was already
  hw_verified from the 2026-06-22 s_t8c batch, but top-level confidence stayed `disassembled`
  pending the remaining value/error gaps. This run closed them via `run_case.py batch`
  (SPCLR-prefixed clean-slate, `splink_rt.tsv`) — **no framebuffer**, since the undocumented
  function form `=SPLINK(mgmt)` exposes the whole link state as a scalar.
  - **Value contract (function form).** Re-linking overwrites: `SPLINK 2,0:SPLINK 2,1` then
    `SPLINK(2)`→**1** (last parent wins). Nested hierarchy reads back per-slot: with
    `SPLINK 1,0:SPLINK 2,1`, `SPLINK(2)`→**1** and `SPLINK(1)`→**0**; a parent's own query
    `SPLINK(0)`→**-1**.
  - **DISCOVERY — re-SPSET clears the link.** `SPSET 1,0:SPLINK 1,0:SPSET 1,0` then
    `SPLINK(1)`→**-1**: re-creating a slot resets its parent pointer. sb-core already matches
    (`SpriteState::create` rebuilds the whole `Sprite` from `default()`, so `parent: None`) —
    locked with a new `sb-render` unit test `respset_clears_parent_link`.
  - **Error guards.** child SPSET but parent NOT SPSET (`SPSET 15,0:SPLINK 15,4`)→errnum 4
    (active-bit guard); SET form 1 arg (`SPLINK 15`) or 3 args (`SPLINK 15,4,1`)→errnum 4;
    function form on an un-SPSET slot (`X=SPLINK(5)`)→errnum 4; function form 2 args
    (`X=SPLINK(15,4)`)→errnum 4 (GET needs argcount 1); negative mgmt in either form
    (`X=SPLINK(-1)`, `SPLINK -1,0`)→errnum 10.
  - **sb-core:** zero code changes — the SET/GET routing, ordering rule, active-bit guards,
    and mgmt range-check all already matched. splink.yaml's confidence bumped to
    `hw_verified` with a 2026-06-24 source entry; 12 new test cases added (5 function-form
    value + 7 error guards) replayed via `IN_SCOPE_SPRITES`, plus the new `sb-render` unit
    test. The rendered coordinate-inheritance EFFECT (renderer adds parent X/Y) stays O-T6
    framebuffer-deferred, consistent with the rest of the sprite family. Harvest TSV in
    `harness/harvest/out/splink_rt.tsv`.
- **Progress (2026-06-24, run 25):** **SPUNLINK** promoted `disassembled` → **hw_verified**
  (Sprites 16/27 → **17/27**). The mirror of run 24's SPLINK push. Most error guards were
  already hw_verified (2026-06-22 s_t8c), but top-level confidence stayed `disassembled`
  pending the missing VALUE contract — does SPUNLINK actually tear the link down, and does it
  touch only the named slot? Harvested via `run_case.py batch` (`spunlink_rt.tsv`, SPCLR
  clean-slate) reading the state back through the undocumented function form `=SPLINK(mgmt)` —
  **no framebuffer needed**, 11/11 cases confirmed, 0 mismatch.
  - **Value contract.** `SPLINK 2,0:SPUNLINK 2` → `SPLINK(2)`=**-1** (link torn down). Nested
    `SPLINK 1,0:SPLINK 2,1:SPUNLINK 2` → `SPLINK(2)`=**-1** AND `SPLINK(1)`=**0**: SPUNLINK
    detaches ONLY the named child; the parent's own link is untouched. No-op forms: `SPUNLINK 0`
    on a never-linked slot → `SPLINK(0)`=**-1**, and a double `SPUNLINK 1:SPUNLINK 1` →
    `SPLINK(1)`=**-1** (idempotent, no error — the detach routine FUN_001edcc8 handles a null
    parent, per the disassembly).
  - **Error guards (all errline 1).** before SPSET (`SPUNLINK 5`) → errnum 4; out-of-range
    `SPUNLINK 512` / negative `SPUNLINK -1` → errnum 10; arg-shape `SPUNLINK` (0 args) /
    `SPUNLINK 0,1` (2 args) / `X=SPUNLINK(0)` (return context) → errnum 4.
  - **sb-core:** zero code changes — `spunlink` (builtins/sprite.rs) already gated ret==0 +
    exactly-1-arg + mgmt range + active-bit, and `SpriteState::unlink` nulls only the one slot's
    `parent`. spunlink.yaml confidence bumped to `hw_verified` with a 2026-06-24 source; 9 new
    conformance cases added (5 function-form value + 4 remaining error guards), replayed via
    `IN_SCOPE_SPRITES`. Harvest TSV in `harness/harvest/out/spunlink_rt.tsv`.

- **Progress (2026-06-24, run 26):** **SPSTART + SPSTOP** promoted `disassembled` →
  **hw_verified** (Sprites 17/27 → **19/27**; coverage 62.1% → **62.9%** hw_verified, 154 →
  156). Error guards were already hw_verified (s_t8c 2026-06-22), but top-level confidence
  stayed `disassembled` pending the missing VALUE contract — does SPSTOP actually set the
  0x2000000 stop bit (and SPSTART clear it), and does the one-arg form touch ONLY the named
  slot? Harvested via `run_case.py batch` (`spstartstop_rt.tsv`, SPCLR clean-slate, endless-loop
  SPANIM `SPANIM m,0,60,100,100,0`) reading the state back through **SPCHK** (which forces 0
  when the stop bit is set) — **no framebuffer needed**, 12/12 cases confirmed, 0 mismatch.
  - **Value contract.** Baseline `SPSET 0,0:SPANIM 0,0,60,100,100,0:?SPCHK(0)`=**1** (XY channel
    running). One-form `:SPSTOP 0`→SPCHK=**0**, then `:SPSTART 0`→SPCHK=**1**. No-arg `:SPSTOP`
    →0, `:SPSTART`→1. **Named-slot isolation** (slots 0+1 both animating): `SPSTOP 0`→SPCHK(0)=0
    but SPCHK(1)=**1** (slot 1 untouched); after `SPSTOP:SPSTART 0`→SPCHK(0)=1 but SPCHK(1)=**0**
    (only slot 0 resumed); `SPSTOP:SPSTART`→SPCHK(1)=1 (all). Both **idempotent**, no error
    (`SPSTOP 0:SPSTOP 0`→0, `SPSTART 0:SPSTART 0`→1).
  - **Bonus — SPCHK mid-animation #CHK* bit RESOLVED** (was oracle-pending, HARVEST_QUEUE s_t8e
    / S-C3): an XY SPANIM gives `SPCHK`=1 (#CHKXY), confirming `status=(flags>>17)&0xFF` since
    SPANIM's XY anim-active bit is 0x20000 (bit 17). spchk.yaml `running_xy_bit` test added.
  - **sb-core:** zero code changes — `set_anim_stopped`/`set_anim_stopped_all` (sb-render
    sprite.rs) toggle the per-slot `anim_stopped` bool (one slot vs all), and `anim_status`
    returns the running-channel mask forced to 0 when stopped — already exactly the harvested
    contract. spstart.yaml/spstop.yaml confidence bumped to `hw_verified` with a 2026-06-24
    source; 11 new conformance cases added (6 spstop + 6 spstart value, minus 1 shared running
    baseline) + 1 spchk, replayed via `IN_SCOPE_SPRITES`. Harvest TSV in
    `harness/harvest/out/spstartstop_rt.tsv`.

- **Progress (2026-06-24, run 27):** **SPCHR** promoted `disassembled` → **hw_verified**
  (Sprites 19/27 → **20/27**; coverage 62.9% → **63.3%** hw_verified, 156 → 157). SPCHR's value
  contract is entirely framebuffer-free (it has GET forms), so it was harvestable in-loop — but
  it was also the rare case where **sb-core had not implemented the command at all** (not in the
  VM dispatch, not in `IN_SCOPE_SPRITES`, its inline spec tests never replayed). To honestly meet
  M7-T2's acceptance ("CI replays the frozen expects") it was implemented, not just spec-bumped.
  - **Complete value contract harvested** via `run_case.py batch` (4 rounds, `spchr_rt*.tsv`,
    no framebuffer — read back through the OUT forms), 24 cases, 0 mismatch:
    - **GET DEFNO** (1-return): template `SPCHR 0,42`→42, custom `SPDEF 100,…`+`SPCHR 0,100`→100,
      `SPSET 0,7`→7, a **direct-image** `SPCHR 0,64,64,16,16,1`→**0** (drops the template assoc,
      even after `SPSET 0,7`).
    - **The 3-return form is `OUT U,V,ATTRIBUTE`, NOT `U,V,W`** (the spec note was wrong):
      `SPCHR 0,64,64,32,32,1 : SPCHR 0 OUT U,V,X`→64,64,**1** (X=attr 1, not W 32). The shared
      last-value code reads `flags & 0x3F` (@0x13f774).
    - **The attribute SET applies bits 1-5 only, PRESERVING the display bit b00** (`bic r1,#0x3e`
      @0x13f008): from a shown sprite `…,&H08`→A=**9** (8|1), `…,0`→1, `…,&H3F`→63. SPCHR does
      NOT show/hide — only SPSHOW/SPHIDE do. GET attr reflects the live display state.
    - **W,H round-trip the source rect verbatim regardless of rotation** (SET+GET rotation swaps
      cancel): W=32,H=8 with attr &H02/&H04/&H06 (rot90/180/270) all read back 32,8.
    - **Form-1 (template)** copies the source rect + origin→home + attr bits1-5 + defno and
      **preserves position** (`SPDEF 100,40,48,24,32,7,9,&H21`+`SPCHR 0,100`→U,V,W,H=40,48,24,32,
      home 7,9, A=33; a prior `SPOFS 0,50,60` survives).
    - **Skip-empty keeps current** (`SPCHR 0,,,8,8` keeps U,V); **absent defaults** W,H→16,
      attr bits1-5→0. Errors: U+W=520 / V+H=520 → errnum 10 (was *assumed*); 6-OUT GET → errnum 4.
  - **sb-core impl (new):** `builtins/sprite.rs::spchr` (form select by ret/argcount, mgmt+active
    guards, Void-keep / absent-default resolution, rect guard, 5 GET forms) + sb-render
    `chr_template` / `set_attr_keep_display` / `get_attr`; wired into VM dispatch + added to
    `IN_SCOPE_SPRITES`. 27 inline conformance cases (now actually replayed) + a `spchr_value_contract`
    unit test. spchr.yaml `disassembled`→`hw_verified`, the wrong 3-return claim corrected, the
    "assumed errnum" / "oracle-pending" notes resolved. TSVs `harness/harvest/out/spchr_rt*.tsv`.

- **Progress (2026-06-24, run 28):** **SPCOL** promoted `disassembled` → **hw_verified**
  (Sprites 20/27 → **21/27**; coverage 63.3% → **63.7%** hw_verified, 157 → 158). SPCOL's
  read-back VALUE contract was the last unverified piece (the error guards were already
  hw_verified, s_t8d 2026-06-22). It has four OUT-getter forms, so the entire set-then-read
  contract is framebuffer-free — harvested against real SB 3.6.0 (Azahar) via `run_case.py
  batch` (`spcol_rt.tsv` + `spcol_rt2.tsv`, 18 cases, 0 mismatch).
  - **Value contract.** A bare `SPCOL m` (no rect) defaults the detection area to the sprite's
    full size with origin (0,0) — `SPSET 0,0:SPCOL 0:SPCOL 0 OUT sx,sy,w,h,sc,mk`→**0,0,16,16,0,-1**
    (SPDEF #0 is 16×16; scale 0; default mask &HFFFFFFFF). Explicit rect/scale/mask round-trip
    verbatim (`SPCOL 0,1,2,16,16,TRUE,255`→1,2,16,16,1,255; negatives kept: `SPCOL 0,-8,-12,15,15`
    →-8,-12,15,15). **Scale flag is stored NORMALIZED to a boolean** — `SPCOL 0,TRUE` and
    `SPCOL 0,5` both read back 1, `SPCOL 0,FALSE`→0. **Mask is a signed-i32 read-back into an
    integer var** (&H12345678→305419896, &H80000000→-2147483648, default→-1; into a Real var
    large masks show STR$ %g, e.g. 3.0542e+08). Skipped-scale setter `SPCOL 0,,mask` keeps scale
    0; leading-comma getter `SPCOL 0 OUT ,mk` reads only the mask. All four getter shapes
    (1/2/4/5/6-OUT) exercised.
  - **Discovery — collision state survives a re-SPSET; only the scale flag resets.** The
    disassembly puts collision params in a SEPARATE array (slot stride 0x48, indexed by mgmt)
    distinct from the 2296-byte sprite slot, so `SPSET 0,0:SPCOL 0,7,8,40,48,TRUE,99:SPSET 0,0:
    SPCOL 0 OUT …`→**7,8,40,48,0,99** (rect+mask preserved, scale cleared). sb-core's
    `SpriteState::create` full-resets the whole slot (incl. collision) via `..Sprite::default()`
    — a real gap, queued in `HARVEST_QUEUE.md` (NOT frozen as a conformance case; SPCOL's own
    set-then-read contract is what's verified + passing). Fix = split collision into a parallel
    mgmt-indexed array; spans SPSET/SPCLR/SPCOL/SPHIT*.
  - **sb-core:** zero code changes — `spcol` (builtins/sprite.rs) already normalizes scale to
    bool (`scale_arg`), defaults the mask all-bits, derives the bare-form rect from the sprite
    size, and returns each getter shape by ret_count; `set_col` (sb-render) matched every
    harvested value. spcol.yaml `disassembled`→`hw_verified` with a 2026-06-24 source + two
    value-contract semantics bullets; 13 inline value cases added, replayed via `IN_SCOPE_SPRITES`.
    TSVs `harness/harvest/out/spcol_rt*.tsv`.

- **Progress (2026-06-24, run 29):** **BGGET** promoted `disassembled` → **hw_verified** (BG
  6/24 → **7/24**; coverage 63.7% → **64.1%** hw_verified, 158 → 159). BGGET's read-back VALUE
  contract is entirely framebuffer-free (BGPUT writes the tilemap cell, BGGET reads the packed
  16-bit value), so it was harvestable in-loop — only its error guards were prior `hw_verified`
  (s_t9b). Harvested against real SB 3.6.0 (Azahar) via `run_case.py batch` prog-mode (20 cases,
  0 mismatch, `bgget_rt.tsv` + `bgget_rt2.tsv`).
  - **Full 16-bit packed value round-trips verbatim** (NOT collapsed to the char field or the
    mod-1024 displayed value): `BGPUT 0,0,0,&H8123`→`BGGET`=33059 (char 291 + #BGREVV), `&H1064`
    →4196 (#BGROT90 b12 — confirms rotation at b12-13, not the stale &H0800 guess), `&H3064`→12388
    (#BGROT270), `&HC005`→49157 (#BGREVH+#BGREVV), `&HFFFF`→65535. A BGPUT value above 16 bits is
    **stored masked to its low 16 bits** (`&H1FFFF`→65535); a hex-string operand round-trips
    (`BGPUT 0,6,6,"1234"`→4660); an untouched cell reads 0. Resolves the long-standing
    bit-layout + cycle-1024 HARVEST_QUEUE questions.
  - **Discovery — pixel mode (coordFlag=1) FLOORS with NO wrap and is NOT range-checked.** On a
    32×32/tile-16 layer px 16→char1, px 31→char1, px 496..511→char31, tile-8 px16→char2; but
    px 512 (char col 32, off map)→**0**, px -1→0, py 512→0, px 528 (one tile past a full map
    width)→**0** (does NOT wrap back to col 1). Off-map pixel reads the empty cell 0 — no errnum
    10 (unlike char mode, which range-checks). This **resolves HARVEST_QUEUE M3-T4** (the
    rounding + off-map open question).
  - **sb-core fix (the real bug):** `sb-render::bg::cell_pixel` had converted px→char then
    **wrapped via `rem_euclid(width/height)`** — modeling a repeating map. The oracle proves
    there is no wrap: it now floors (`div_euclid`) and returns 0 for an off-map char coordinate.
    The old `pixel_coord_converts_and_wraps` unit test (which asserted the wrong wrap) is
    replaced by `pixel_coord_floors_no_wrap_offmap_zero`. bgget.yaml `disassembled`→`hw_verified`
    with a 2026-06-24 source + two semantics bullets (pixel no-wrap, packed-value); 20 inline
    value cases added, replayed via `IN_SCOPE_BG`. TSVs `harness/harvest/out/bgget_rt*.tsv`.

- **Progress (2026-06-24, run 30):** **BGHOME** promoted `disassembled` → **hw_verified** (BG
  7/24 → **8/24**; coverage 64.1% → **64.5%** hw_verified, 159 → 160; 1532 → 1541 frozen cases).
  BGHOME's argcount/layer error guards were already hw_verified (s_t9c); this run harvested the
  SET→OUT origin VALUE round-trip — the last queued question (bghome.yaml community note: "integer
  coercion of non-integral X/Y + exact stored origin"). BGHOME's origin is per-layer scratch state
  with no framebuffer, so its whole value contract is value-deterministic (like BGOFS run 13 /
  SPHOME run 20). Against real SB 3.6.0 (Azahar) via `run_case.py batch` prog OUT-readback
  (`bghome_rt.tsv`, 11 cases, 0 mismatch).
  - **Origin reads back VERBATIM as an integer — NOT offset by the layer's reference geometry.**
    The decisive finding (the disassembly's `bl 0x139cc4` set-origin helper offsets the *internally
    applied* display origin, but BGHOME() returns the *raw stored* value): `BGHOME 0,200,120:BGHOME
    0 OUT HX,HY`→**200,120** (not a shifted value); negatives kept (`0,-30,-60`→-30,-60); large
    verbatim (`0,1000,-1000`→1000,-1000); overwrite (`0,50,60` then `0,7,8`→7,8).
  - **X,Y truncate TOWARD ZERO before storage** (the integer getter, not floor): `0,16.7,32.9`→
    16,32; `0,-16.7,-32.9`→-16,-32 (consistent with BGOFS/SPROT). The **fresh per-layer default
    origin is 0,0** (`ACLS:BGHOME 0 OUT`→0,0), and **NO BGSCREEN is required** — BG layers 0..3
    always exist (`BGHOME 2,11,22`→11,22 on a fresh state, same as BGOFS run 13 / BGROT run 16);
    layers are independent (`BGHOME 0,5,6:BGHOME 1,7,8`→layer0 5,6 / layer1 7,8).
  - sb-core matched **every** harvested value with **NO code change** (`bghome` stores `to_int()`
    /reads back `Int`; default 0,0; verified all forms via `sb-run`). Top-level `confidence` flips
    — the only un-harvested piece is the on-screen pivot EFFECT (origin for BGROT/BGSCALE), which
    needs the O-T6 BG framebuffer golden. 10 inline value cases now hw_verified in `bghome.yaml`
    (round-trips + truncation + default + no-BGSCREEN + independence + overwrite + the 3 existing
    error guards), replayed via `IN_SCOPE_BG`, plus a `bghome_value_contract` unit test. Harvest
    TSV in `harness/harvest/out/bghome_rt.tsv`.

- **Progress (2026-06-24, run 31):** **BGCOORD** promoted `disassembled` → **hw_verified** (BG
  8/24 → **9/24**; coverage 64.5% → **64.9%** hw_verified, 160 → 161). BGCOORD's coordinate
  conversion is value-deterministic (scalar transform state only — scroll/scale/home/rotation —
  no framebuffer), so its NON-ROTATION value contract — wrongly deferred to O-T6 by the spec —
  was harvestable in-loop. Against real SB 3.6.0 (Azahar) via `run_case.py batch` progstr
  OUT-readback (`bgcoord_rt.tsv`, 29 cases, 0 mismatch).
  - **The forward map (mode 0) is `display = BGHOME + scale·rotate(bg) − BGOFS`, pivot at the
    layer ORIGIN (0,0) — NOT BGHOME** (confirmed by `FUN_0028defc` @0x28df48: `out =
    [obj+0x24] + scale·(rotate(src − pivot[obj+0x34]))`, pivot 0). Decisive findings: scroll
    **SUBTRACTS** in mode 0 (`BGOFS 0,8,8 : BGCOORD 0,0,0,0`→**−8,−8**) and **ADDS** in the
    inverse (`BGOFS 0,8,8 : BGCOORD 0,8,8,2`→**16,16**); **BGHOME is a pure translation, not a
    pivot** (`BGHOME 0,16,16 : BGCOORD 0,16,16,0`→**32,32**, `0,32,32,0`→48,48); **BGSCALE
    enlarges about the origin** (`BGSCALE 0,2,2 : BGCOORD 0,16,16,0`→**32,32**). Mode 1 floors
    the BG-pixel result to a tile index (`0,200,120,1`→12,7; `0,15,15,1`→0,0).
  - **OUT values truncate toward zero to integers** (`vcvt.s32.f32` @0x28df5c/@0x28df70):
    `BGCOORD 0,16.5,16.5,0`→**16,16** (mode 1 already floors).
  - **sb-core fix:** `sb-render::bg::coord` had the WRONG model — it added `BGOFS` in mode 0
    (real SB subtracts), pivoted scale/rotation at `BGHOME` (real pivot is the origin), and
    `builtins::bg::bgcoord` returned raw `f64` (real handler truncates to `i32`). Rewrote the
    forward/inverse maps to the verified contract and truncate the OUT components in the
    wrapper. The round-trip unit test still holds (pivot choice is round-trip-invariant);
    replaced the misleading "BGHOME = rotation/scale pivot" doc/comments. 19 value + 2 error
    inline cases now hw_verified in `bgcoord.yaml` (replayed via `IN_SCOPE_BG`), plus a
    `coord_value_contract_no_rotation` unit test.
  - **Rotation queued (the only un-harvested piece):** `BGROT 0,90` of BG point (16,0) returns
    display (−1,15) — NOT a clean origin rotation; the matrix comes from a non-trivially-indexed
    lookup table `[0x306de0]`. The exact rotated values + on-screen pivot effect need the O-T6
    BG framebuffer golden (HARVEST_QUEUE.md); the rotated branch stays a best-effort standard
    rotation about origin. Harvest TSV in `harness/harvest/out/bgcoord_rt.tsv`.

- **Progress (2026-06-24, run 32):** **BGFILL** promoted `disassembled` → **hw_verified** (BG
  9/24 → **10/24**; coverage 64.9% → **65.3%** hw_verified, 161 → 162; 1558 → 1570 frozen cases).
  BGFILL's tilemap-write contract — wrongly deferred to the O-T6 BG framebuffer golden by the spec
  — is entirely framebuffer-free: BGGET (hw_verified run 29) reads each packed cell value back, so
  the whole fill geometry + data contract was harvestable in-loop. Against real SB 3.6.0 (Azahar)
  via `run_case.py` progcase (BGGET read-back) + errcase (`bgfill_rt.tsv`, 12 cases, 0 mismatch).
  - **The fill helper FUN_0015fcc0 (`bl @0x1654c8`) NORMALIZES + CLAMPS, never errors on coords.**
    Reversed corners are swapped to start≤end on each axis (`cmp r1,r3`/swap @0x15fcc4, `cmp r2,r12`/
    swap @0x15fcf8); the rect is clamped to `[0, size-1]` (movlt 0 @0x15fd2c/@0x15fd34, cpygt size-1
    @0x15fd3c/@0x15fd48); a fully-off-map rect writes nothing (early-out @0x15fdb8). Oracle: `BGFILL
    0,5,5,10,10,1024`→(7,7)/(5,5)/(10,10)=1024, neighbours 0; `0,10,10,5,5,777`→same rect; single
    `0,3,3,3,3,42`→only (3,3); `0,28,28,40,40,55` on 32×32→(31,31)=55 (40 clamps, **no errnum**);
    `0,-5,-5,2,2,66`→(0,0)/(2,2)=66; `0,40,40,50,50,99`→nothing, no error. Coords are NEVER
    range-checked — UNLIKE BGPUT (which raises errnum 10 for an off-map X/Y).
  - **Discovery — a NUMERIC screen-data value is range-checked to 0..65535 → errnum 10** (the
    numeric path @0x1654e4 then `cmp #0x10000` `bcc`=ok @0x165504, else `mov r0,#0xa` @0x165518):
    `BGFILL 0,0,0,1,1,65536`→**errnum 10**, `…,-1`→**errnum 10** (negative is unsigned-compared),
    `65535`→ok. This **DIFFERS from BGPUT**, whose numeric data is stored masked to the low 16 bits
    (`&H1FFFF`→65535, hw_verified BGGET run 29). String hex `"C040"`=49216 and `&HFFFF`=65535
    round-trip; a wrong-typed data arg (`DIM Z[2]:BGFILL 0,0,0,1,1,Z`)→errnum 8.
  - **sb-core fix:** the shared `screen_data` MASKED numeric data for both BGPUT and BGFILL, so
    BGFILL silently accepted `65536`/negatives instead of erroring. Added a BGFILL-specific
    `screen_data_fill` that range-checks numeric data to `0..=65535` (errnum 10) while delegating
    the string-hex/type-mismatch paths to `screen_data` (BGPUT keeps masking, unchanged). The fill
    geometry/normalization/clamp already matched (`sb-render::bg::fill`). bgfill.yaml
    `disassembled`→`hw_verified` with a 2026-06-24 source + two semantics bullets (coord
    normalize/clamp-no-error, numeric-data range guard); 12 inline cases (9 value via BGGET
    read-back + 3 error) added, replayed via `IN_SCOPE_BG`, plus a `bgfill_value_contract` unit
    test. TSV `harness/harvest/out/bgfill_rt.tsv`.
- **Progress (2026-06-24, run 33):** **BGPUT** promoted `disassembled` → **hw_verified** (BG
  10/24 → **11/24**; coverage 65.3% → **65.7%** hw_verified, 162 → 163; 1570 → 1577 frozen cases).
  BGPUT's single-cell write VALUE/mask contract — wrongly deferred to the O-T6 BG framebuffer
  golden by the spec — is framebuffer-free: BGGET (hw_verified run 29) reads the stored cell value
  back. Harvested against real SB 3.6.0 (Azahar) via `run_case.py batch` prog cases (BGGET
  read-back): `harness/harvest/out/bgput_rt.tsv` (8 cases) + `bgput_attr_rt.tsv` (4 cases), 0 mismatch.
  - **The cell is written with a 16-bit HALFWORD STORE (strh r1,[r2,#0x90] @0x164874), so numeric
    data is MASKED to its low 16 bits — NOT range-checked like BGFILL.** The numeric path
    (blx [r2,#0x40] @0x164844) has NO `cmp #0x10000` guard (BGFILL has one @0x165504). Oracle:
    `BGPUT 0,0,0,&H1FFFF`→BGGET 65535, `…,65536`→**0** (wraps), `…,-1`→**65535** (low 16 bits of -1),
    `…,&HFFFF`→65535. The `65536`→0 / `-1`→65535 cases are the exact mirror-image of BGFILL, which
    raises errnum 10 for both — this is the documented BGPUT-vs-BGFILL contrast, now both hw_verified.
  - **Value + attribute round-trip:** `BGPUT 0,3,4,5`→BGGET(0,3,4)=5, BGGET(0,4,4)=0 (only the
    written cell changes); `"80FF"`→33023 (hex string parse); char 0→empty (0). The packed
    rotation/flip attribute bits store verbatim: `&H1064`(#BGROT90)→4196, `&H3064`(#BGROT270)→12388,
    `&H4064`(#BGREVH)→16484, `&H8064`(#BGREVV)→32868 — BGGET returns the full 16-bit packed value,
    confirming the b12-13 rotation / b14 H-flip / b15 V-flip layout (only the *rendered* display
    effect still needs O-T6).
  - **sb-core already matched** (`screen_data` masks via `& 0xFFFF`); no code-behavior change. Spec
    `bgput.yaml` `disassembled`→`hw_verified`: a store/mask disassembled source (strh @0x164874,
    no numeric range check) + a 2026-06-24 hw_verified source; semantics bullets extended (mask
    vs BGFILL range-check, BGGET read-back); 9 inline value cases (7 mask/value + 2 attribute)
    added, replayed via `IN_SCOPE_BG`, plus masking assertions folded into the `bgput_get_round_trip`
    unit test. (HARVEST_QUEUE S-T9b BGPUT bit-layout/mask items were already RESOLVED in run 29 via
    BGGET; this run formalizes them into the spec's tests + confidence.)
- **Progress (2026-06-24, run 34):** **BGPAGE** promoted `disassembled` → **hw_verified** (BG
  11/24 → **12/24**; coverage 65.7% → **66.1%** hw_verified, 163 → 164; 1577 → 1584 frozen cases).
  BGPAGE is the BG analog of SPPAGE (run 21) / GPAGE (run 11): the SET/GET page selector is a pure
  global value (one shared graphic page for all four BG layers, no framebuffer), so its whole VALUE
  contract — wrongly deferred to the O-T6 BG framebuffer golden by the spec — was harvestable
  in-loop. The error guards (page ∉ 0..5 → errnum 10) were already hw_verified (s_t9a); this run
  closed the SET→GET round-trip + default + the (return,arg)-shape guards against real SB 3.6.0
  (Azahar) via `run_case.py batch` prog readback + err (`bgpage_rt.tsv`, 10 cases, 0 mismatch).
  - **Page round-trips VERBATIM, default GRP5.** `ACLS:BGPAGE()`→**5** (the documented default, now
    hw_verified); `BGPAGE 0..5 : BGPAGE()`→0/1/2/3/4/5; OUT form `BGPAGE 1:BGPAGE OUT P`→P=1. The
    page is one global shared across all layers (confirms the disassembled `[[0x315d60]+0x64]`
    single-word state). **Harvest gotcha:** BG page persists across program RUNs in real SB, so the
    true default read needs a leading `ACLS` (same as BGSCALE run 17).
  - **(return,arg)-shape guards (new hw_verified):** `BGPAGE 1,2` (2-arg SET) and `R=BGPAGE(0)`
    (1-arg GET fn form) both → **errnum 4 errline 1** — the @0x165e6c/@0x165e88 shape check, exactly
    mirroring SPPAGE run 21.
  - sb-core matched **every** harvested value with **NO code change** (`bgpage` SET 1-arg 0..5→errnum
    10 / GET 0-arg returns `bg.page` / else errnum 4; `BG_PAGE_DEFAULT=5`; verified default + OUT form
    via `sb-run`). Top-level `confidence` flips — the only un-harvested piece is the on-screen
    tile-source EFFECT (which GRP the BG layers sample), which needs the O-T6 BG framebuffer golden.
    bgpage.yaml's 4 inline cases replaced with 11 hw_verified cases (default + 0/2/4/5 round-trips +
    OUT form + range + 2 new shape guards), replayed via `IN_SCOPE_BG`; 2 semantics bullets added.
    Harvest TSV in `harness/harvest/out/bgpage_rt.tsv`.
- **Progress (2026-06-24, run 35):** **BGSTART + BGSTOP** promoted `disassembled` → **hw_verified**
  (BG 12/24 → **14/24**; coverage 66.1% → **66.9%** hw_verified, 164 → 166; 1584 → 1597 frozen cases).
  The BG analog of SPSTART+SPSTOP (run 26): the error guards (layer ∉ 0..3 → errnum 10) were already
  hw_verified (s_t9d), but top-level confidence stayed `disassembled` pending the missing VALUE
  contract — does BGSTOP actually set the 0x40 stop bit (BGSTART clear it), and does the one-arg form
  touch ONLY the named layer? Harvested via `run_case.py batch` (`bgstartstop_rt.tsv`, an endless XY
  `BGANIM 0,0,60,100,100,0` keeping #CHKXY running) reading state back through **BGCHK** (which forces
  0 when the stop bit is set) — **no framebuffer needed**, 17/17 cases confirmed, 0 mismatch.
  - **Value contract.** Baseline `BGANIM 0,0,60,100,100,0:?BGCHK(0)`=**1** (XY channel running). One-form
    `:BGSTOP 0`→BGCHK=**0**, then `:BGSTART 0`→BGCHK=**1**. No-arg `:BGSTOP`→0, `:BGSTART`→1.
    **Named-layer isolation** (layers 0+1 both animating): `BGSTOP 0`→BGCHK(0)=0 but BGCHK(1)=**1**
    (layer 1 untouched); after `BGSTOP:BGSTART 0`→BGCHK(0)=1 but BGCHK(1)=**0** (only layer 0 resumed);
    `BGSTOP:BGSTART`→BGCHK(1)=1 (all). Both **idempotent**, no error (`BGSTOP 0:BGSTOP 0`→0,
    `BGSTART 0:BGSTART 0`→1). **Shape guards (new hw_verified):** `BGSTART 1,2` / `BGSTOP 1,2` (2-arg)
    and `R=BGSTART(0)` / `R=BGSTOP(0)` (return ctx) all → **errnum 4 errline 1** (the @0x16688c/@0x166244
    gates).
  - **Bonus — BGCHK mid-animation #CHKXY bit RESOLVED** (was oracle-pending, HARVEST_QUEUE s_t9d /
    S-C3): a running XY BGANIM gives `BGCHK`=1, confirming the raw `1<<channel` low-byte layout (XY=bit 0,
    NO `>>17` shift, unlike sprite SPCHK). bgchk.yaml `running_xy_bit` test added; the Z/R/S/C/V bits stay
    queued.
  - **sb-core:** zero code changes — `set_anim_stopped`/`set_anim_stopped_all` (sb-render bg.rs) toggle
    the per-layer `anim_stopped` bool (one layer vs all), and `anim_status` returns `1<<channel` forced
    to 0 when stopped — already exactly the harvested contract. bgstart.yaml/bgstop.yaml confidence
    bumped to `hw_verified`; 13 new conformance cases (5 bgstart + 7 bgstop + 1 bgchk) replayed via
    `IN_SCOPE_BG` + a `bgstart_bgstop_isolate_named_layer` vm.rs unit test. Harvest TSV in
    `harness/harvest/out/bgstartstop_rt.tsv`.

- **Progress (2026-06-24, run 36):** **BGCLR** promoted `disassembled` → **hw_verified**
  (BG 14/24 → **15/24**; coverage 66.9% → **67.3%** hw_verified, 166 → 167). The error guards
  (layer ∉ 0..3 → errnum 10; >1 arg or used-as-function → errnum 4) were already hw_verified
  (s_t9a), but top-level confidence stayed `disassembled` pending the missing VALUE contract —
  the tilemap clear side-effect (HARVEST_QUEUE S-T9a). Harvested via `run_case.py batch`
  (`bgclr_rt.tsv`) reading each cleared cell back through **BGGET** (hw_verified run 29 — reads
  the packed cell value with **no framebuffer**), so the clear effect is fully oracle-confirmable
  without O-T6. 12/12 cases confirmed, 0 mismatch.
  - **Value contract.** Baseline `BGPUT 0,5,5,100:?BGGET(0,5,5)`=**100** (set path). One-arg
    `BGCLR 0` zeros the **whole** named-layer map — two seeded cells (3,3 & 7,7) both read back
    **0**, not just one. No-arg `BGCLR` clears layer 0 → **0**. **Named-layer isolation** (layers
    0+1 seeded 100/200): `BGCLR 0` → BGGET(0,5,5)=**0** but BGGET(1,5,5)=**200** (layer 1
    untouched); no-arg `BGCLR` → both **0**. **Idempotent** `BGCLR 0:BGCLR 0` → 0 (no error).
    **Shape guards (re-confirmed):** `R=BGCLR(0)` and `BGCLR 1,2` → **errnum 4 errline 1**.
  - **sb-core:** zero code changes — `BgState::clear(layer)` (sb-render bg.rs) zeroes only that
    layer's `cells`, `clear_all()` loops every layer, and `bgclr` (sb-core builtins/bg.rs) routes
    0-arg→clear_all / 1-arg→clear / else errnum 4 / ret→errnum 4 — already exactly the harvested
    contract. bgclr.yaml confidence bumped to `hw_verified`; 11 new conformance cases replayed via
    `IN_SCOPE_BG`. Harvest TSV in `harness/harvest/out/bgclr_rt.tsv`; HARVEST_QUEUE S-T9a BGCLR
    clear-effect item resolved.
- **Progress (2026-06-24, run 37):** **BGCOPY** promoted `disassembled` → **hw_verified**
  (BG 15/24 → **16/24**; coverage 67.3% → **67.7%** hw_verified, 167 → 168). The error guards
  (layer ∉ 0..3 → errnum 10; ≠7 args or used-as-function → errnum 4) were already hw_verified
  (s_t9b), but top-level confidence stayed `disassembled` pending the tilemap copy VALUE contract
  (HARVEST_QUEUE S-T9b). Read the copy helper FUN_0015f9a0 first: it **normalizes** the source
  rectangle (swap X corners @0x15f9a4-0x15f9c8 / Y corners @0x15f9cc-0x15f9dc), **clamps**
  source+dest to the map edge (subgt @0x15fa44-0x15fa6c, negative→0 @0x15fa18-0x15fa3c), picks an
  overlap-safe copy direction (cmp r6,r1/cmpeq r2,r7 @0x15faac), then halfword-copies
  (ldrh/strh @0x15fb00). Then harvested the side-effect via `run_case.py batch`
  (`bgcopy_rt.tsv`) reading each cell back through **BGGET** (hw_verified run 29, framebuffer-free).
  18/18 cases confirmed, 0 mismatch.
  - **Value contract.** Single-cell `BGPUT 0,2,2,77:BGCOPY 0,2,2,2,2,8,8` → BGGET(0,8,8)=**77**,
    source BGGET(0,2,2)=**77** kept (**copy, not move**). **Orientation preserved**: a 2x2 block
    (11,12,13,14) at (0,0)-(1,1) copied to (10,10) reads back tl/tr/bl/br = **11/12/13/14**.
    **Source rectangle normalized**: reversed-corner `BGCOPY 0,1,0,0,0,10,10` == the non-reversed
    form (BGGET(0,10,10)=11, (11,10)=12); reversed-XY also lands block-top-left at the dest.
    **Overlap-safe**: (0,0)=11 (1,0)=12, `BGCOPY 0,0,0,1,0,1,0` → (0,0)=11 (1,0)=**11** (2,0)=12.
    **Verbatim incl. 0**: copying a 0-valued source cell over a 99 dest → BGGET=**0**. **Layer
    isolation**: a layer-0 copy leaves BGGET(1,8,8)=**200**; cell outside the dest rect stays 0.
    **Shape guards re-confirmed**: `R=BGCOPY(0,0,0,1,1,2,2)` and `BGCOPY 0,0,0,1,1,2` (6 args) →
    **errnum 4 errline 1**.
  - **sb-core:** zero code changes — `BgState::copy` (sb-render bg.rs) min/max-normalizes the
    source, buffers the block before writing (overlap-safe), preserves orientation and copies
    verbatim, and `bgcopy` (sb-core builtins/bg.rs) enforces ret→errnum 4 / ≠7-arg→errnum 4 —
    already exactly the harvested contract. bgcopy.yaml confidence bumped to `hw_verified`; 5
    error/no-error + 18 value cases replayed via `IN_SCOPE_BG`; 6 new semantics bullets. Harvest
    TSV in `harness/harvest/out/bgcopy_rt.tsv`. HARVEST_QUEUE S-T9b BGCOPY rectangle-semantics item
    resolved (the in-bounds copy contract); the partial-off-map clamp **span** stays queued (read
    in the disasm, not yet oracle-harvested — observable result for in-bounds cells is identical).
- **Progress (2026-06-24, run 38):** **BGSAVE + BGLOAD** promoted `disassembled` → **hw_verified**
  (BG 16/24 → **18/24**; coverage 67.7% → **68.5%** hw_verified, 168 → 170). The error guards were
  already hw_verified (s_t9e); the missing piece — the array data / round-trip VALUE contract — was
  thought to need the BG framebuffer oracle (O-T6), but (like BGCOPY run 37) it is fully harvestable
  **framebuffer-free via BGGET read-back** plus reading saved elements through an array index.
  - **BGSAVE value/layout.** Saved element == the packed cell value, identical to BGGET / what BGPUT
    wrote (`BGPUT 0,3,3,&H8001` → saved **32769**). Whole-screen layout is ROW-MAJOR **y*width+x**
    (`BGPUT 0,1,0` → A[1]=11; `BGPUT 0,0,1` → A[32]=22 on a width-32 layer); unwritten cell → 0. The
    ranged form (sx,sy,w,h) lays the sub-rect out from its own origin: **(y-sy)*w+(x-sx)** (A[0]/A[1]/
    A[2] = 55/66/67). 8 value cases frozen (`bgsaveload_rt.tsv`).
  - **BGLOAD round-trip + the KEY finding.** A BGSAVE→BGLOAD round trip reproduces the tilemap (123
    whole, 88/89 ranged, 321 via A#) and keeps the source layer. Read the copy helper FUN_0015f1fc:
    it has **two element→cell conversion paths by array TYPE**. An **integer array** does a raw
    halfword store of the low 16 bits (`ldrh/strh` @0x15f330-0x15f33c) — values WRAP: `A%=-1`→**65535**,
    `A%=&H10001`→**1**. A **real (double) array** uses a **saturating `vcvt.u32.f64`** (@0x15f3f0,
    truncate toward zero, clamp negatives→0) then a halfword store — `A#=-1`→**0**, `-100`/`-70000`→0,
    `77.9`→**77**, `&H10001`(65537)→1, `65536`→0, `70000`→**4464**, `&H1FFFF`→65535. So an unsuffixed
    `DIM A` (which is REAL in SB) loads `-1` as **0**, not 65535.
  - **sb-core fix.** `read_cells` (builtins/bg.rs) was masking the Real path like Int (`-1.0`→65535).
    Corrected to the saturating cast `(v as u32 & 0xFFFF)` matching `vcvt.u32.f64`; the Int path
    (low-16-bit wrap) was already right. New `bgload_value_conversion_depends_on_array_type` unit
    test + 11 BGSAVE / 17 BGLOAD value/conversion cases replayed via `IN_SCOPE_BG` (the conversion
    cases use explicit `A%`/`A#` suffixes — the Real path is only reachable for suffixed arrays in
    sb-core until M1-T4 fixes the unsuffixed-`DIM` default, queued from run 4). Harvest TSVs in
    `harness/harvest/out/bgsaveload_{rt,conv,typed}.tsv`. Still queued: the auto-grown 1-D array's
    exact length (not headless-readable) and the undocumented 3/7-arg trailing-operand meaning.
- **Progress (2026-06-24, run 39):** **BGSCREEN** promoted `disassembled` → **hw_verified** (BG
  18/24 → **19/24**; coverage 68.5% → **69.0%** hw_verified, 170 → 171). The error guards were
  already hw_verified (s_t9a); the missing piece — the map-size SIDE EFFECT — was previously
  thought to need the BG framebuffer oracle (O-T6), but (like BGSAVE run 38) it is fully
  harvestable **framebuffer-free**: BGSCREEN sets the layer's row-major BGSAVE stride (= width),
  observable by BGPUT-ing a marker at a known (x,y) and reading where it lands in the whole-screen
  BGSAVE array (`index = y*width+x`, hw_verified run 38). Harvested via `run_case.py batch` prog
  read-back against real SB 3.6.0 (Azahar); **11/11 cases confirmed, 0 mismatch** (`bgscreen.tsv`).
  - **Default (initial) map size is 25 x 15.** With no BGSCREEN, a marker at (0,1) reads back at
    array index **25** (NOT index 1 → not column-major), and the last cell (24,14) at index **374**
    (= 14*25+24) — confirming width 25 AND height 15. Resolves the long-doc-only "initial 25×15"
    claim against the live engine.
  - **WIDTH is the FIRST size argument (the stride).** `BGSCREEN 0,32,32` → marker at (0,1) lands at
    A[**32**]; `BGSCREEN 0,64,8` → A[**64**] (and A[8]=0, so it's width-not-height); `BGSCREEN 0,8,64`
    → A[**8**]. Decisively pins arg order to `layer,width,height`.
  - **Resizing reallocates a FRESH, CLEARED map.** `BGSCREEN 0,32,32:BGPUT 0,5,5,99:BGSCREEN 0,16,16`
    → the (5,5) cell reads back **0**, not 99 — the old tilemap is discarded on resize (matches the
    handler's map-buffer reallocation via FUN_0011605c @0x166b50).
  - **4th (tile-size) arg is orthogonal to the data layout** — `BGSCREEN 0,32,32,8` still has stride
    32 (only the on-screen pixel size changes; the per-tile pixel EFFECT itself stays oracle-pending,
    rendering-only). **Per-layer**: resizing layer 0 leaves layer 1 at the default width 25.
  - **sb-core:** zero code changes — `BgState::resize`/`default` (sb-render bg.rs) already use
    `BG_DEFAULT_WIDTH=25`/`HEIGHT=15`, row-major `y*width+x`, and `cells = vec![0; w*h]` on resize.
    bgscreen.yaml confidence bumped to `hw_verified`; 6 error/no-error + 11 value/layout cases
    replayed via `IN_SCOPE_BG`; 5 new semantics bullets. Harvest TSV in
    `harness/harvest/out/bgscreen.tsv`. **Remaining BG `disassembled`** are the visual-only
    layer-state ops (BGANIM/BGCLIP/BGFUNC/BGHIDE/BGSHOW — visibility/animation/clip, headless
    framebuffer-bound like the SP* analogs) and the 8 BGM* music commands (audio, O-T7) — the
    BG-graphics value/layout family is now exhausted in-loop.
- **Progress (2026-06-24, run 40):** **GPSET** promoted `disassembled` → **hw_verified**
  (Graphics 5/19 → **6/19**; coverage 69.0% → **69.4%** hw_verified, 171 → 172). The error
  guards were already hw_verified (s_t7b); the missing piece — the pixel SIDE-EFFECT — was
  thought to need the framebuffer oracle (O-T6), but it is fully harvestable **framebuffer-free
  via GSPOIT scalar read-back** (the same s_c2 method that already verified GSPOIT's RGBA5551
  round-trip). Harvested against real SB 3.6.0 (Azahar) via `run_case.py batch` setup/result
  `prog` cases; 12 + 4 cases, 0 mismatch (`gpset.tsv` + `gpset_alpha.tsv`).
  - **Pixel write round-trips through RGBA5551 (8->5 truncation).** `GPSET x,y,RGB(255,0,0)` →
    GSPOIT **-524288** (&HFFF80000), `RGB(0,0,255)` → -16776968 (&HFF0000F8), `RGB(0,100,0)` →
    -16752640 (&HFF006000), white/`-1` → -460552 (&HFFF8F8F8) — identical to the colors GSPOIT
    already round-trips, proving GPSET stores the device halfword.
  - **2-arg form uses the current GCOLOR draw color** — `GCOLOR RGB(255,0,0):GPSET x,y` reads
    back -524288, == the explicit-red form (confirms the disassembled `ldreq r0,[r6,#0xc]`
    draw-color default @0x153e1c).
  - **Float coordinates FLOOR (resolves the queued open question).** `GPSET 60.9,60.9,white`
    plots pixel **(60,60)** (GSPOIT=-460552) and leaves **(61,61)** empty (GSPOIT=0) — floor,
    not round-to-nearest. Resolves HARVEST_QUEUE S-T7b/M2-T2 "float→int coordinate rounding"
    for GPSET; sb-core's `floor_coord` already matched.
  - **Off-page clip raises no error** (`GPSET 999,999,-1` runs to a sentinel) and the in-page
    edge pixel draws (`GPSET 511,511,white` → GSPOIT(511,511)=-460552).
  - **Direct overwrite, not alpha-blend.** A non-opaque color GPSET OVER an opaque pixel
    REPLACES it (white then `&H000000FF` → 248=&H000000F8); a 0-alpha color still writes
    (`&H00FF0000` → 16252928=&H00F80000). The stored 1-bit alpha is set **only for A==255**
    (`&H80000000`/`&HFE000000` read back 0) — pinning the RGBA5551 alpha threshold exactly.
  - **sb-core: zero code changes** — `argb8888_to_rgba5551` already gates the alpha bit on
    `a == 255`, `floor_coord` floors, `plot_dev` clips off-page, and the pixel store overwrites.
    gpset.yaml confidence bumped to `hw_verified`; 13 round-trip/floor/alpha cases replayed via
    `IN_SCOPE_GRAPHICS`; 4 new semantics bullets. Harvest TSVs in `harness/harvest/out/gpset*.tsv`.
    The single-pixel primitive is now fully verified scalar-only; the multi-pixel rasterizers
    (GLINE/GBOX/GFILL/GCIRCLE/GTRI/GPAINT) stay `disassembled` — checkable the same GSPOIT way,
    queued.
- **Progress (2026-06-24, run 41):** **GLINE** promoted `disassembled` → **hw_verified**
  (Graphics 6/19 → **7/19**; coverage 69.4% → **69.8%** hw_verified, 172 → 173; tests 1669 →
  1687). Line side-effects harvested framebuffer-free via GSPOIT scalar read-back (the same s_c2
  / GPSET method), against real SB 3.6.0 (Azahar). Used **single-program multi-readback** captures
  (one drawn line, many GSPOIT reads concatenated into one string result) after discovering the
  per-case `prog` path leaves graphic-page residue across cases + has intermittent capture flakes
  — `harness/harvest/out/gline_robust.tsv`.
  - **Geometry exact for cardinal + 45-deg + degenerate.** Horizontal `GLINE 10,100,15,100,white`
    lights (10..15,100)=-460552 and leaves (13,101)=0; vertical (30,110)-(30,115) lights the
    column, (31,112)=0; 45-deg (40,40)-(45,45) lights only the (i,i) diagonal, (41,42)=0;
    degenerate `GLINE 50,50,50,50` lights exactly (50,50), (51,51)=0.
  - **Color round-trips through RGBA5551 identical to GPSET** (white/-1 → -460552, blue → -16776968)
    and the **4-arg form uses the current GCOLOR** (`GCOLOR RGB(255,0,0):GLINE 60,120,65,120` →
    (62,120)=-524288, confirming `ldreq r0,[r6,#0xc]` @0x1533a4). Off-page lines raise **no error**
    (`GLINE -50,-50,999,999,-1` → sentinel) and a partially-off line still draws its on-page pixels.
  - **Discovery — SB's line rasterizer is a round-half-DOWN DDA, NOT a textbook Bresenham, and
    sb-core diverges.** The `steep_map` bitmap of (200,150)→(210,155) (slope 0.5) shows hardware
    lighting y per x = 150,150,151,151,152,152,153,153,154,154,155 (y = y1 +
    round_half_down((x-x1)·dy/dx)): **(205,152) lit, (205,153) clear**. sb-core's
    `sb-render::raster::line_dev` integer Bresenham lights the **opposite** mid-pixel ((205,153)),
    diverging on every non-cardinal/non-45-deg slope. Read the draw helper @0x1e6700 (called
    @0x1534d8): driving-axis |dx| vs |dy| split @0x1e69ac, 8×8-tiled framebuffer swizzle tables
    @0x1e6f28/@0x1e6f30 — added as a `disassembled` source. The fix (replace `line_dev` with the
    DDA, verify across all 8 octants via a broader harvest, then freeze `steep_map`) is its own
    M2-T2 fidelity slice — **queued in HARVEST_QUEUE** (a one-octant fix from this single line would
    be under-verified). NOT frozen as a conformance case (sb-core would fail it).
  - **sb-core: zero code changes this run** (all 18 frozen cases already match — the divergence is
    only the queued non-45 raster). gline.yaml confidence → `hw_verified`; 18 geometry/color/clip
    cases replayed via `IN_SCOPE_GRAPHICS`; 6 new semantics bullets + 2 new sources. Harvest case
    files `harness/harvest/gline_*.txt`, TSVs `harness/harvest/out/gline*.tsv`.
- **Progress (2026-06-24, run 42):** **GBOX** promoted `disassembled` → **hw_verified**
  (Graphics 7/19 → **8/19**; coverage 69.8% → **70.2%** hw_verified, 173 → 174). Box outline
  side-effects harvested framebuffer-free via GSPOIT scalar read-back (same s_c2 / gpset / gline
  method) against real SB 3.6.0 (Azahar), using `progstr` isolated-program cases (one box draw
  per case, no cross-case graphic-page residue) — `harness/harvest/out/gbox.tsv`, 10/10 confirmed,
  0 mismatch. Unlike GLINE, every GBOX edge is CARDINAL, so the round-half-down DDA divergence
  (run 41) does not apply — sb-core matches exactly.
  - **Outline only — interior untouched.** `GBOX 10,100,20,110,white` lights all four corners
    (10,100)/(20,100)/(10,110)/(20,110), the four edge midpoints (15,100)/(15,110)/(10,105)/
    (20,105) = -460552, but the interior (15,105) and a just-inside pixel (15,101) stay 0; outside
    pixels (9,100)/(21,110)=0. Corners are inclusive on all four edges.
  - **Corner order normalized.** `GBOX 20,110,10,100,white` (corners reversed) draws the IDENTICAL
    box: (10,100)/(20,110)=-460552, interior (15,105)=0 — matching the handler's swap @0x193a28/
    @0x193a44.
  - **Color RGBA5551 round-trip == GPSET/GLINE.** explicit RGB(0,0,255)→-16776968, -1→white
    -460552; the **4-arg form uses the current GCOLOR** (`GCOLOR RGB(255,0,0):GBOX 40,100,45,105`
    → (40,100)=-524288, == explicit-red). Read the box-outline draw helper @0x1939d8 (called
    @0x1552bc): ARGB8888→RGBA5551 channel build (mov r5,#0x3e / mov r6,#0xf800 / mov lr,#0x7c0,
    alpha bit orreq r6,r6,#0x1 only for A==255) + per-edge page clip — added as a `disassembled`
    source.
  - **Degenerate forms.** `GBOX 60,60,60,60,white`→single pixel (60,60), (61,61)=0; zero-width
    `GBOX 70,70,70,75`→vertical column (70,70)/(70,75), (71,72)=0; zero-height `GBOX 80,80,85,80`
    →horizontal row (80,80)/(85,80), (82,81)=0.
  - **Clipping.** `GBOX -50,-50,999,999,-1` runs without error; `GBOX 395,130,405,140,white` still
    lights the on-page edge pixels (399,130)/(399,140) — partial off-page boxes clip, no range error.
  - **sb-core: zero code changes** — `Grp::gbox` (sb-render raster.rs) normalizes corners min/max,
    draws the four cardinal edges inclusive, and `plot_dev` clips off-page — already exactly the
    harvested contract. gbox.yaml confidence → `hw_verified`; 28 outline/corner-order/color/
    degenerate/clip cases replayed via `IN_SCOPE_GRAPHICS`; 6 new semantics bullets + 2 new sources.
    Harvest case file `harness/harvest/out/gbox_cases.txt`, TSV `harness/harvest/out/gbox.tsv`.
    The remaining multi-pixel rasterizers (GFILL/GCIRCLE/GTRI/GPAINT/GPUTCHR) stay `disassembled`
    — checkable the same GSPOIT way, queued.
- **Progress (2026-06-24, run 43):** **GFILL** promoted `disassembled` → **hw_verified**
  (Graphics 8/19 → **9/19**; coverage 70.2% → **70.6%** hw_verified, 174 → 175). Solid-fill
  side-effects harvested framebuffer-free via GSPOIT scalar read-back (same s_c2 / gpset / gline /
  gbox method) against real SB 3.6.0 (Azahar), using `progstr` isolated-program cases (one fill
  per case, no cross-case page residue) — `harness/harvest/out/gfill.tsv`, 11/11 confirmed, 0
  mismatch (`clip_noerr` had one transient capture halt in the batch, re-confirmed alone via
  `run_case.py progcase`). All edges are cardinal so the round-half-down DDA divergence (GLINE
  run 41) does not apply — sb-core matches exactly.
  - **Whole interior filled — the GBOX-vs-GFILL distinction.** `GFILL 10,100,20,110,white` lights
    all four corners (10,100)/(20,100)/(10,110)/(20,110), the four edge midpoints, AND the interior
    (15,105) + a just-inside pixel (15,101) = -460552; only the outside pixels (9,100)/(21,110)
    stay 0. (Contrast GBOX run 42, where (15,105)/(15,101)=0.) Span inclusive on every edge.
  - **Corner order normalized.** `GFILL 20,110,10,100,white` (corners reversed) fills the
    IDENTICAL box, interior (15,105)=-460552 — matching the helper swap @0x193e18/@0x193e28.
  - **Color RGBA5551 round-trip == GPSET/GLINE/GBOX**, applied to corner AND interior alike:
    RGB(0,0,255)→-16776968 (corner (30,100) + interior (32,102)), -1→white -460552; the 4-arg
    form uses the current GCOLOR (`GCOLOR RGB(255,0,0):GFILL 40,100,45,105` → (40,100)/(42,102)
    =-524288). Read the filled-rectangle draw helper @0x193dec (called @0x1532bc): corner-norm
    (sub/cmp/cpygt @0x193e10-@0x193e38), per-span page clip ([r6,#0x14..0x1c] @0x193e38-@0x193e78,
    bgt skip on a fully-off span), ARGB8888→RGBA5551 build @0x193e80 (mov r1,#0x7c0 / mov r0,#0x3e
    / blue (c>>8)&0xf800, alpha bit only for A==255), 8-pixel tile rounding (bic ...,#0x7) — added
    as a `disassembled` source.
  - **Degenerate forms.** `GFILL 60,60,60,60,white`→single pixel (60,60), (61,61)=0; zero-width
    `GFILL 70,70,70,75`→vertical column (70,70)/(70,72)/(70,75), (71,72)=0; zero-height
    `GFILL 80,80,85,80`→horizontal row (80,80)/(82,80)/(85,80), (82,81)=0.
  - **Clipping.** `GFILL -50,-50,999,999,-1` runs without error; `GFILL 395,130,405,140,white`
    still fills the on-page edges + interior (399,130)/(399,135)/(399,140) — partial off-page
    fills clip, no range error.
  - **sb-core: zero code changes** — `Grp::gfill` (sb-render raster.rs) normalizes corners min/max,
    fills the inclusive solid rectangle, and `plot_dev` clips off-page — already exactly the
    harvested contract. gfill.yaml confidence → `hw_verified`; 35 fill/corner-order/color/
    degenerate/clip cases replayed via `IN_SCOPE_GRAPHICS`; 5 new semantics bullets + 2 new sources.
    Harvest case file `harness/harvest/out/gfill_cases.txt`, TSV `harness/harvest/out/gfill.tsv`.
    The remaining multi-pixel rasterizers (GCIRCLE/GTRI/GPAINT/GPUTCHR) stay `disassembled`
    — checkable the same GSPOIT way, queued.
- **Progress (2026-06-24, run 44):** **GCIRCLE** promoted `disassembled` → **hw_verified**
  (Graphics 9/19 → **10/19**; coverage 70.6% → **71.0%** hw_verified, 175 → 176). Circle AND
  arc/sector side-effects harvested framebuffer-free via GSPOIT scalar read-back (same
  gpset/gline/gbox/gfill method) against real SB 3.6.0 (Azahar), `progstr` isolated-program
  cases — `harness/harvest/out/gcircle*.tsv`, all confirmed, 0 mismatch. Found+worked-around a
  gotcha: `progstr` does NOT clear the graphic page and GRP state persists across programs, so
  run 43's full-page `GFILL -50,-50,999,999` left every pixel white; each case now `ACLS`-clears
  first (validated by a 1-case probe). Both the midpoint circle AND the arc/sector angle
  convention match sb-core **with zero code changes**.
  - **Full circle is a 1-pixel OUTLINE, not a disc.** `GCIRCLE 100,100,30,white` lights the four
    cardinals (130,100)/(70,100)/(100,130)/(100,70)=-460552 but centre (100,100), interior
    (110,110) and a between-centre-and-rim point (115,100) all read 0; the outline is single-pixel
    wide ((129,100)/(131,100)/(100,129)/(100,131)=0).
  - **Standard midpoint (Bresenham) octant set.** Down the x=130 column it lights y=100..105 and
    leaves y=106=0; the 45-deg 3x3 cluster lights exactly (122,120)/(121,121)/(120,122) (the six
    other neighbours=0) — identical to sb-core's midpoint walk. Read the circle draw helper
    @0x18fa7c (called @0x154ab8): ARGB8888→RGBA5551 build IDENTICAL to GPSET/GLINE/GBOX/GFILL
    (mov r12,#0x3e / mov r3,#0x7c0 / blue (c>>8)&0xf800 / alpha orreq only A==255 @0x18fa84-0x18fab8),
    initial decision **d = 3 - 2r** (mov r6,#0x3 @0x18fb18 / rsb r12,r0,#0 / add r9,r6,r12,lsl #1
    @0x18fb24), 8-pixel tile store (and #0x7 / asr #3 / strh @0x18fb90-0x18fbc8) — added as a
    `disassembled` source.
  - **Color round-trip == GPSET/GLINE/GBOX/GFILL.** RGB(0,0,255)→-16776968, -1→white -460552;
    the 3-arg form uses the current GCOLOR (`GCOLOR RGB(255,0,0):GCIRCLE 150,100,20`→-524288).
    Radius ≤ 0 (0 and -5) draws nothing. Small r=5 @ (60,60): cardinals lit, centre 0.
  - **Arc/sector angle convention (now hw_verified, previously oracle-pending):** 0° points at +X
    and the sweep runs toward +Y (screen-down). `GCIRCLE 100,100,30,0,90` lights only the
    lower-right quadrant ((130,100)/(100,130)/(121,121) lit; (70,100)/(100,70)/centre=0). `end<start`
    wraps +360 (`315,45` lights 315° (121,79)+0° (130,100)+45° (121,121), 90° (100,130)=0); negative
    angles sweep directly (`-90,0` lights (100,70)/(121,79)/(130,100), (100,130)=0). flag=1 sector
    adds the two centre→endpoint radii ((115,100)/(100,115)/centre lit); flag=0 open arc draws none
    ((115,100)/(100,115)/centre=0).
  - **sb-core: zero code changes** — `Grp::gcircle` (midpoint) + `gcircle_arc` (0°=+X, sweep +Y,
    end≥start normalize, sector radii) already match exactly. gcircle.yaml confidence →
    `hw_verified`; 45 circle/octant/color/radius/arc/sector/wrap/negative cases replayed via
    `IN_SCOPE_GRAPHICS`; 6 new semantics bullets + 2 new sources. Harvest case files
    `harness/harvest/out/gcircle{,_arc,_arc2}_cases.txt`, TSVs `harness/harvest/out/gcircle{,_arc,_arc2}.tsv`.
    The remaining multi-pixel rasterizers (GTRI/GPAINT/GPUTCHR) stay `disassembled` — GTRI's
    diagonal edges diverge (M2-T5), GPAINT/GPUTCHR checkable the same GSPOIT way, queued.
- **Progress (2026-06-24, run 45):** **GPAINT** promoted `disassembled` → **hw_verified**
  (Graphics 10/19 → **11/19**; coverage 71.0% → **71.4%** hw_verified, 176 → 177). Flood-fill
  side-effects harvested framebuffer-free via GSPOIT scalar read-back (same gpset/gline/gbox/
  gfill/gcircle method) against real SB 3.6.0 (Azahar), `progstr` isolated-program cases, each
  `ACLS`-clearing first (GRP state persists across progstr programs — run 44 gotcha) —
  `harness/harvest/out/gpaint.tsv`, 7/7 confirmed, 0 mismatch. sb-core (`Grp::gpaint`, sb-render
  raster.rs) matched **all 7 with zero code changes**. white=-460552 red=-524288 blue=-16776968.
  - **Implicit (no-border) form replaces the contiguous SEED-color region, bounded by any drawn
    outline.** `GBOX 10,10,20,20,white:GPAINT 15,15,red` fills the interior ((15,15)/(12,12)/
    (18,18)=red), keeps the outline (10,10)=white, and does NOT leak outside ((5,5)/(21,21)=0).
  - **Implicit fill STOPS at an internal different-colored barrier.** A blue `GLINE 35,11,35,19`
    splits the white box; `GPAINT 32,15,red` fills only the LEFT half ((32,15)/(33,15)=red),
    leaving the divider (35,15)=blue and the right half (37,15)=0 — proving seed-color contiguity.
  - **Border (4-arg) form CROSSES internal colors, stops only at the border.** Same scene +
    `GPAINT 32,15,red,white` fills the whole interior including overwriting the blue line
    ((35,15)/(37,15)=red), bounded by the white outline (30,10)=white. The decisive implicit-vs-
    border distinction, confirmed on hardware.
  - **Unbounded implicit fill covers the whole connected page** (`GPAINT 0,0,red` → (0,0)/
    (200,120)/(399,239)=red); **2-arg form defaults to GCOLOR** (`GCOLOR RGB(0,0,255):...GPAINT
    65,15`→(65,15)=blue); **no-op when seed already == fill** (`GPSET 50,50,red:GPAINT 50,50,red`
    → (50,50)=red, (51,50)=0 — nothing spreads); border form stays inside its outline.
  - Read both fill helpers: no-border `FUN_00191870` (called @0x154614) — seed clipped to the
    draw window (`cmp r12,r0/bgt`, out-of-window seed = no-op), ARGB8888→RGBA5551 build IDENTICAL
    to GPSET/GFILL/GCIRCLE (mov #0x7c0 / alpha orreq only A==255), reads the seed pixel `ldrh
    r0,[r0]`@0x191940 then `cmp r1,r0/beq` (fill==seed no-op), 4-connected horizontal-span
    scanline flood (predicate `bl 0x1d6584`, vertical propagation to rows ±1 via [r6,#0x18]/
    [r6,#0x1c]); with-border `FUN_00191b1c` (called @0x1546bc) builds TWO RGBA5551 values (fill
    r11, border r1), seed==border no-op (`cmp r0,r1/beq`@0x191c1c), distinct predicate `bl
    0x1d62c4` testing 'pixel==border?'. Both added as `disassembled` sources. The fill is
    4-connected (no diagonal spread) and compares/writes in RGBA5551 framebuffer space.
  - 27 fill/barrier/border/unbounded/gcolor/no-op cases frozen in `spec/tests/gpaint.yaml` (via
    the gpaint.yaml `tests:` overlay, replayed by `IN_SCOPE_GRAPHICS`) + 5 new semantics bullets +
    3 new sources; top-level `confidence` flipped to `hw_verified`. Harvest case file
    `harness/harvest/out/gpaint_cases.txt`, TSV `harness/harvest/out/gpaint.tsv`. The remaining
    multi-pixel rasterizers GTRI (diagonal edges diverge, M2-T5) and GPUTCHR (glyph, needs the
    font ROM but checkable the same GSPOIT way) stay `disassembled` — queued.
- **Progress (2026-06-24, run 46):** **GCLS** promoted `disassembled` → **hw_verified**
  (Graphics 11/19 → **12/19**; coverage 71.4% → **71.8%** hw_verified, 177 → 178). Page-clear
  side-effects harvested framebuffer-free via GSPOIT scalar read-back (same gpset/gline/gbox/
  gfill/gcircle/gpaint method) against real SB 3.6.0 (Azahar), `progstr` isolated programs each
  `ACLS`-clearing first — `harness/harvest/out/gcls.tsv`, **9/9 confirmed, 0 mismatch**. sb-core
  (`graphics::gcls`, `sb-render Grp::gcls`) matched **all 9 with zero code changes**.
  - **GCLS color fills the ENTIRE 512×512 manip page**, not just the 400×240 visible area:
    `GCLS RGB(255,0,0)` → GSPOIT(0,0)=(399,239)=(511,511)=-524288 (red).
  - **No-arg GCLS fills color 0 = TRANSPARENT black** (clears prior drawing): a solid `GFILL`
    region after `GCLS` reads back 0. Decisively DISTINCT from `GCLS RGB(0,0,0)` (opaque black,
    &HFF000000) which reads back -16777216 — the no-arg path passes literal `mov r1,#0` while the
    1-arg path keeps the ARGB alpha, so the RGBA5551 alpha bit differs.
  - **Color converts ARGB8888→RGBA5551 identically to GPSET/GFILL** (8→5 trunc): white→-460552,
    pure green RGB(0,255,0)→-16713728 (0xFF00F800). **Alpha bit set ONLY when input A==255**:
    non-opaque &H80FF0000 (A=128) → 16252928 (0x00F80000, alpha stripped). And GCLS **overwrites**
    whatever was drawn (a prior pixel + a blank both take the fill).
  - **Clears ONLY the manip page:** draw on GRP0, `GPAGE 0,1:GCLS RGB(0,0,255):GPAGE 0,0` leaves
    GRP0's pixel intact (GSPOIT(5,5)=-524288) while page 1 holds the fill (-16776968).
  - Read the fill helper `FUN_0018ffcc` (called @0x155318/@0x155360): RGBA5551 build from the
    ARGB color (mask consts 0x3e/0x7c0/0xf800, `lsr`-shifted + orr-combined @0x18fffc), alpha
    test `mov r0,#0xff / cmp r0,r1,lsr#0x18 / orreq r0,r0,#0x1` @0x190000 (1 alpha bit only when
    A==0xff), then a clipped span-fill loop (`bl 0x1d670c`) — IDENTICAL conversion to the GPSET/
    GFILL builders. Added as a `disassembled` source.
  - 14 fill/clear/trunc/alpha/manip-page cases frozen in `spec/instructions/gcls.yaml` (inline
    `tests:`, replayed by `IN_SCOPE_GRAPHICS`) + 4 new semantics bullets + 2 new sources (fill
    helper body + the hw_verified harvest); top-level `confidence` flipped to `hw_verified`.
    Harvest case file `harness/harvest/out/gcls_cases.txt`, TSV `harness/harvest/out/gcls.tsv`.
    Remaining Graphics `disassembled`: GCLIP, GCOPY, GLOAD, GPRIO, GSAVE (GSPOIT-checkable next),
    GTRI + GPUTCHR (multi-pixel, queued).
- **Progress (2026-06-24, run 47):** **GCLIP** promoted `disassembled` → **hw_verified**
  (Graphics 12/19 → **13/19**; coverage 71.8% → **72.2%** hw_verified, 178 → 179). Write-clip
  drawing side-effects harvested framebuffer-free via GSPOIT scalar read-back (same gpset/gline/
  gbox/gfill/gcircle/gpaint/gcls method) against real SB 3.6.0 (Azahar), `progstr` isolated
  programs each `ACLS`-clearing then setting their own clip — `harness/harvest/out/gclip.tsv`,
  **7/7 confirmed, 0 mismatch**. sb-core (`graphics::gclip`, `sb-render` `plot_dev`/`draw_bounds`
  write-clip gate) matched **all 7 with zero code changes**.
  - **Write clip (mode != 0) RESTRICTS DRAWING to the rect:** `GCLIP 1,100,100,200,200:GFILL
    0,0,300,300,white` fills GSPOIT(150,150)=-460552 (inside) but leaves GSPOIT(50,50)=0 (outside).
    GPSET/GLINE obey it too — `GPSET 50,50` outside is dropped (0) while `GPSET 150,150` inside
    plots (-524288); a `GLINE 0,150,300,150` crossing the boundary draws only its in-clip span
    (50,150=0 / 150,150=-524288 / 250,150=0).
  - **Rect is INCLUSIVE on both corners:** GSPOIT(100,100)=GSPOIT(200,200)=-460552 (drawn), one
    pixel beyond each corner GSPOIT(99,100)=GSPOIT(201,200)=0 (untouched).
  - **Corners normalized (min/max):** reversed `GCLIP 1,200,200,100,100` clips the SAME region.
  - **Decisive distinction — DISPLAY clip (mode 0) does NOT restrict page writes.** `GCLIP
    0,100,100,200,200:GFILL 0,0,300,300,white` → GSPOIT(50,50)=GSPOIT(150,150)=-460552: the
    display clip only constrains screen compositing, so GSPOIT (which reads the page buffer) sees
    the whole-page fill. This separates the two GCLIP modes behaviorally (matches the disasm split:
    write clip via page clip-set helper `bl 0x209e98`, display clip via `bl 0x1edeac/0x1ee0bc`).
  - **1-arg reset clears the clip:** `GCLIP 1,100,100,200,200:GCLIP 1` then whole-page GFILL fills
    everywhere (GSPOIT(50,50)=-460552).
  - 16 write-clip side-effect cases frozen in `spec/instructions/gclip.yaml` (inline `tests:`,
    replayed by `IN_SCOPE_GRAPHICS`) + 3 new semantics bullets + 1 new `hw_verified` source;
    top-level `confidence` flipped to `hw_verified`. Harvest case file
    `harness/harvest/out/gclip_cases.txt`, TSV `harness/harvest/out/gclip.tsv`.
    Remaining Graphics `disassembled`: GCOPY, GLOAD, GPRIO, GSAVE (GSPOIT-checkable next),
    GTRI + GPUTCHR (multi-pixel, queued).
- **Progress (2026-06-24, run 48):** **GCOPY** promoted `disassembled` → **hw_verified**
  (Graphics 13/19 → **14/19**; coverage 72.2% → **72.6%** hw_verified, 179 → 180). Region-blit
  pixel side-effects harvested framebuffer-free via GSPOIT scalar read-back (same gpset/gline/
  gbox/gfill/gcircle/gpaint/gcls/gclip method) against real SB 3.6.0 (Azahar), `progstr`
  isolated programs each ACLS-clearing first — `harness/harvest/out/gcopy.tsv`, **7/7 confirmed,
  0 mismatch**. sb-core (`builtins::graphics::gcopy`, `sb-render` `bitmap::gcopy`
  read_region/write_region) matched **all 7 with zero code changes**.
  - **7-arg same-page copy maps source rect top-left -> dest top-left:** `GPSET 10,10,red:GCOPY
    0,0,32,32,100,100,1` -> GSPOIT(110,110)=-524288 (src(10,10)->dest(110,110), offset = dest -
    src-min-corner). The SOURCE pixel is PRESERVED (GSPOIT(10,10)=-524288 still) — copy, not move.
    The dest corner (100,100) receives the transparent src(0,0) under mode 1 -> stays 0.
  - **copy_mode governs TRANSPARENT source pixels:** over a blue dest (`GFILL 100,100,150,150,
    blue`), copying a transparent source region with **mode 0 SKIPS** transparent (dest survives
    blue, GSPOIT(120,120)=-16776968) while **mode 1 COPIES** transparent (overwrites with 0).
  - **8-arg cross-page copy reads the explicit source page:** `GPAGE 0,1:GCLS:GPSET 20,20,red:
    GPAGE 0,0:GCOPY 1,0,0,32,32,100,100,1` -> page0 dest(120,120)=-524288, page0 src spot(20,20)=0
    (the red lived only on page 1). Confirms src_page is the FROM page, current manip page the TO.
  - **Source rect INCLUSIVE on both corners:** a 1x1 rect (5,5)-(5,5) copies exactly one pixel
    (GSPOIT(100,100)=-524288; neighbors (101,100)/(101,101)=0) — width/height = |x2-x1|+1.
  - **Corners NORMALIZED (min/max):** reversed `GCOPY 32,32,0,0,...` copies the SAME region as
    ordered (GSPOIT(110,110)=-524288).
  - **Only pixels INSIDE the source rect are copied:** src rect (0,0)-(20,20) carries (10,10) red
    -> (110,110)=-524288 but excludes (40,40) blue -> (140,140)=0.
  - 13 pixel side-effect cases frozen in `spec/instructions/gcopy.yaml` (inline `tests:`, replayed
    by `IN_SCOPE_GRAPHICS`) + 1 new `hw_verified` source; top-level `confidence` flipped to
    `hw_verified`. Harvest case file `harness/harvest/out/gcopy_cases.txt`, TSV
    `harness/harvest/out/gcopy.tsv`. Remaining Graphics `disassembled`: GLOAD, GPRIO, GSAVE
    (GSAVE/GLOAD round-trip via array read-back next), GTRI + GPUTCHR (multi-pixel, queued).
- **Progress (2026-06-24, run 49):** **GTRI** promoted `disassembled` → **hw_verified**
  (Graphics 14/19 → **15/19**; coverage 72.6% → **73.0%** hw_verified, 180 → 181). Filled-triangle
  side-effects harvested framebuffer-free via GSPOIT read-back (same gpset/gline/gbox/gfill/gcopy
  method) against real SB 3.6.0 (Azahar), `progstr` programs each GCLS-clearing first —
  `harness/harvest/out/gtri.tsv` + `gtri_color.tsv`.
  - **Read the actual rasterizer FUN_00194120** (handler @0x1556c0 `bl 0x194120`): vertices
    sorted by y-then-x (@0x194164), ARGB8888→RGBA5551 build @0x194234 (r=(c>>2)&0x3e /
    g=(c>>5)&0x7c0 / b=(c>>8)&0xf800 / alpha bit when (c>>0x18)==0xff), whole-triangle clip-reject
    vs the clip rect [r4,#0x10..0x1c], scanline fill @0x194374 `strh r5,[r3]`, and a **degenerate
    branch @0x1942ec** that routes to the line helpers FUN_00155f8c + FUN_0015cab0.
  - **Fill mask is the inclusive edge-function set** — `GTRI 100,100,104,100,100,104,white` lights
    exactly the pixels with dx+dy<=4: every vertex, both legs, and the FULL hypotenuse
    ((104,100)/(103,101)/(102,102)/(101,103)/(100,104)) are lit; (104,101)=dx4+dy1=5 and the
    outside corner (104,104) stay 0. sb-core's existing `sb-render raster::gtri` inclusive rule
    matched the mask exactly.
  - **Winding-independent** — reversed vertex order (100,104/104,100/100,100) reads back
    byte-identically (the rasterizer sorts first).
  - **DEGENERATE (collinear) triangles DRAW a line, not nothing** — `GTRI 10,200,20,200,30,200`
    lights the row 10..30 @ y=200; `GTRI 200,10,205,15,210,20` lights the slope-1 segment;
    all-coincident → a single pixel; two-coincident → the segment to the third. **This corrected a
    real sb-core bug**: `raster::gtri` returned early on zero area (drew nothing). Fixed to draw
    `line_dev` between the two most-distant vertices, and hardened `line_dev` to i64 deltas so an
    extreme-coord degenerate segment can't overflow i32 (also hardens GLINE/GBOX). Updated the
    `gtri_fills_interior_and_draws_degenerate_line` unit test accordingly.
  - **Color**: 6-arg form defaults to GCOLOR (RGB(255,0,0)→-524288), 7-arg explicit
    (RGB(0,0,255)→-16776968), -1→white -460552. **Clip**: huge off-page `GTRI -50,-50,999,999,
    500,-50,-1` no error; a triangle straddling x=400 still fills its on-page interior.
  - 21 pixel side-effect cases frozen in `spec/instructions/gtri.yaml` (inline `tests:`, replayed
    by `IN_SCOPE_GRAPHICS`) + 1 `disassembled` (rasterizer) + 1 `hw_verified` (GSPOIT) source;
    top-level `confidence` flipped to `hw_verified`. Harvest case file
    `harness/harvest/gtri_cases.txt`. Remaining Graphics `disassembled`: GLOAD, GPRIO, GSAVE,
    GPUTCHR.
- **Progress (2026-06-24, run 50):** **GLOAD** promoted `disassembled` → **hw_verified**
  (Graphics 15/19 → **16/19**; coverage 73.0% → **73.4%** hw_verified, 181 → 182). GLOAD's
  round-trip + placement + error guards were already hw_verified (graphics_bitmap.yaml, s_t7d);
  this run pinned the three remaining headline behaviors via GSPOIT read-back (framebuffer-free,
  same method as gpset/…/gtri) against real SB 3.6.0 (Azahar) — `harness/harvest/out/gload.tsv`
  + `gload_extra.tsv`, case file `harness/harvest/gload_cases.txt`.
  - **copy_mode is the transparent-pixel gate** — an image whose pixel is the transparent color
    (color 0, alpha bit clear) loaded over a red background: **mode 0 (FALSE) SKIPS it** (red
    kept = -524288), **mode 1 (TRUE) COPIES it** (reads back 0); an opaque pixel (white = -460552)
    draws under both. sb-core's `write_region` `!copy_transparent && hw&1==0 -> skip` matched.
  - **Form-2 PALETTE recolor** — each image element is an INDEX into the palette array; palette
    entries are **LOGICAL ARGB** (`RGB()`) colors (not physical codes), truncated to RGBA5551 on
    write: indices [0,1] over P=[red,blue] paint red/blue; indices [2,0,1] over P=[red,green,blue]
    paint blue/red/green. Resolves the long-standing "exact palette semantics" queue item;
    sb-core's palette path (`gload_halfword(P[idx], raw=false)`) matched.
  - **convert_flag is unvalidated** — read the actual form-branch FUN_0015352c @0x153880
    (`cmp r0,#0 beq 0x153898 -> bl 0x191414 convert / cmp r0,#1 beq 0x1538fc -> bl 0x190aa8 raw`,
    array form -> `bl 0x1911c4 @0x1539b0`): flag 0 = convert path, **any non-zero scalar takes the
    raw path** — flag 2 round-trips a physical code identically to flag 1 (GSAVE flag 1 ->
    GLOAD flag 2 = -524288 red) and does NOT raise (corrected an earlier mis-read that flag≠0/1 ->
    errnum 8; oracle says NOERR). sb-core's `form_op.to_int() != 0 -> raw` matched.
  - 10 pixel side-effect cases frozen in `spec/instructions/gload.yaml` (inline `tests:`, replayed
    by `IN_SCOPE_GRAPHICS`) + a new `hw_verified` GSPOIT source ref; top-level `confidence` flipped.
    **No sb-core code change needed** — copy_mode/palette/raw-flag were all already correct. Queued
    (HARVEST_QUEUE.md): the pathological cross-flag raw extraction (a 32-bit LOGICAL word loaded via
    the raw path yields an un-modelled value on real SB; never reached when GLOAD's flag matches
    GSAVE's). Remaining Graphics `disassembled`: GPRIO, GSAVE, GPUTCHR.
- **Progress (2026-06-24, run 51):** **GSAVE** promoted `disassembled` → **hw_verified**
  (Graphics 16/19 → **17/19**; coverage 73.4% → **73.8%** hw_verified, 182 → 183). GSAVE's
  arg-shape + auto-expand + the single red-pixel flag values were already hw_verified (gsave.yaml
  s_t7d / s_t7d_bitmap); this run pinned the remaining headline array-content behaviors by **direct
  array read-back** (the saved pixels ARE the numeric array — no framebuffer/GSPOIT needed) against
  real SB 3.6.0 (Azahar) — `harness/harvest/out/gsave.tsv`, case file `harness/harvest/gsave_cases.txt`.
  - **Fill order is ROW-MAJOR.** A 2x2 sub-rect painted (10,10)=red (11,10)=green (10,11)=blue
    (11,11)=white, saved raw, reads back `W=[63489,1985,63,65535]` — element 1 is the next column in
    the same row, element 2 is the next row — confirming `W[j*w+i]=pixel(x+i,y+j)`. The 2-arg whole-area
    form is likewise row-major from pixel(0,0) (`W[0]=pixel(0,0)`, `W[1]=pixel(1,0)`, LEN=262144).
  - **Color tables broadened beyond red.** flag 1 raw RGBA5551 (layout R[15:11] G[10:6] B[5:1] A[0]):
    green=1985 (0x07C1), blue=63 (0x003F), white=65535. flag 0 32-bit logical (Int array, signed;
    5-bit channel left-shift-expand → 255 becomes 0xF8): green=-16713728 (0xFF00F800), blue=-16776968
    (0xFF0000F8), white=-460552 (0xFFF8F8F8). An untouched pixel reads 0 under both flags.
  - **The explicit src_page arg selects that page, not the manip page.** With red on page 0 and the
    manip page switched to a blank page 1 (`GPAGE 0,1:GCLS`): `GSAVE 0,…`→63489 (page 0 red),
    `GSAVE 1,…`→0 (page 1 blank); a no-page-arg `GSAVE` reads the current manip page (page 1)→0. Backed
    by the disasm page-select math @0x154270 (page struct stride = 25 words; GRPF global @0x154364).
  - 18 array-content cases frozen in `spec/instructions/gsave.yaml` (inline `tests:`, replayed by
    `IN_SCOPE_GRAPHICS`) + a new `hw_verified` source ref; top-level `confidence` flipped.
    **No sb-core code change needed** — `read_region` (row-major), `gsave_word`, and the page-select
    path were all already correct (cross-checked through `sb-run`). Remaining Graphics `disassembled`:
    **GPRIO, GPUTCHR** (GPUTCHR is blocked on sb-core having no glyph renderer / system-font ROM, so its
    pixel side-effects aren't yet gate-replayable — a font-ROM impl task, not an in-loop harvest).
- **Progress (2026-06-24, run 52):** **BGMVAR** headless-deterministic surface fully harvested
  against real SB 3.6.0 (Azahar) — and it caught a **real docs+sb-core bug**. Top-level confidence
  stays `disassembled` (the during-PLAY read value is MML-runtime state, O-T7 — same structural
  ceiling as the input/wireless families), but the **stopped-read value, call-shape selection, and
  0..7 range guards are now hw_verified** and frozen (bgmvar.yaml 5 → 14 inline cases; total spec
  cases 1904 → 1912).
  - **The docs' "-1 when stopped" is WRONG — real SB 3.6.0 returns 0.** `BGMVAR(0,5)`, `BGMVAR(7,7)`,
    `BGMVAR(0,0)` all → 0 while stopped, and a write is **not observable while stopped**
    (`BGMVAR 0,5,42 : ?BGMVAR(0,5)` → 0, not 42). The disassembly explains why: the read routine
    `FUN_001a4ea8` guards on the audio-availability counter `ldr r3,[r0,#0x554] / cmp r3,#0x100 /
    movge r0,#0x0 / bxge lr` — when the BGM system isn't serving the track (stopped) that counter is
    >= 0x100, so it returns 0 before ever indexing the var array (during play it indexes [ctx+0x530],
    varnum*3-word stride, reads a **signed-16** via bl 0x2939d4 → `ldrsh`). **Fixed sb-core**:
    `AudioState::bgmvar` stopped sentinel −1 → 0; corrected 2 unit tests + the spec/docs claims.
  - **Range + shape confirmed:** track/varnum outside 0..7 (incl. negative track `BGMVAR -1,0,1`) →
    errnum 10 (both write and read forms); a call shape that is neither (statement,3 args) nor
    (function,2 args) — `BGMVAR 0,5`, `A=BGMVAR(0,5,1)`, `BGMVAR 0`, `BGMVAR 0,5,1,2` — → errnum 4.
  - Harvest TSV `harness/harvest/out/bgmvar.tsv`; hw_verified count unchanged (183) — corpus grows,
    one fidelity bug fixed.
- **Progress (2026-06-24, run 53):** **BGANIM** promoted `disassembled` → **hw_verified** (BG
  19/24 → **20/24**; coverage 73.8% → **74.2%** hw_verified, 183 → 184) — and it caught a **real
  bug** in both the spec and sb-core. BGANIM's channel-activation contract is fully observable
  headless by reading **BGCHK** (the running-channel mask, hw_verified run 35 / framebuffer-free);
  the interpolated transform stays O-T6-pending. Harvested against real SB 3.6.0 (Azahar) via
  `run_case.py batch` (`harness/harvest/out/bganim.tsv`, 30 cases, 0 mismatch / 0 capture failures).
  - **BG's BGCHK bit layout is COMPACTED, not the documented #CHK* / `1<<channel`.** Numeric or
    string target XY/Z/R/S/C/V reads back BGCHK = **1/2/4/8/16/32** (bits 0..5), NOT the docs'
    #CHKR=16 / #CHKS=32 / #CHKC=64 / #CHKV=128 (those are the *sprite* SPCHK layout, where channels
    are the contiguous 0..7). BG, lacking the UV(2)/I(3) channels, packs R/S/C/V down into bits 2..5.
    Multiple channels OR (`Z+R`=6, `XY+V`=33). **This corrects the run-35 extrapolation** ("Z=2/R=16/
    S=32/C=64/V=128 follow the same `1<<channel` raw layout"), which had only ever confirmed XY=1 (bit
    0 — ambiguous between the two layouts). The disassembly proves it: BGANIM's per-channel switch
    (jump table @0x164dbc) ORs the COMPACTED constants into the layer flags word — XY `orr #0x1`
    @0x164e44, Z `#0x2` @0x164ea4, R `#0x4` @0x164ee8, S `#0x8` @0x164f38, C `#0x10` @0x164f78, V
    `#0x20` @0x164fbc — and BGCHK returns that word raw.
  - **Fixed sb-core:** `BgState::anim_status` (sb-render `bg.rs`) emitted `1<<raw_channel` (giving
    R=16/S=32/C=64/V=128); added a `bg_anim_bit` raw-channel→compacted-bit map (XY0/Z1/R2/S3/C4/V5)
    so it now returns the real 1/2/4/8/16/32. Corrected the `anim_status_bits_and_stop` unit test
    (Z+R now 6, not 18).
  - **Relative + guards confirmed:** the `+8` / trailing-`+` relative forms set the SAME channel bit
    (the +8 bit is masked off by `and r0,r1,#0x7` before the switch) — `"XY+"`/`"R+"`/`8`/`12` →
    1/4/1/4. Per-layer independence (`BGANIM 1,0,…` → BGCHK(1)=1, BGCHK(0)=0). BG has no UV/I channel:
    target **2/3 or "UV"/"I" → errnum 4** (the switch's cases 2/3 jump to the shared errnum-4 site
    @0x164fc8) — resolves a HARVEST_QUEUE open question; sb-core's existing errnum-4 rejection is
    correct. Negative target → errnum 4; too-few-args / return-context → errnum 4; layer 4/-1 →
    errnum 10; a non-number/non-string target (array) → errnum 8. All errline 1.
  - `bganim.yaml` 6 → 33 inline cases (confidence flipped to `hw_verified`); `bgchk.yaml` summary/
    semantics/sources corrected + 6 → 9 cases (added R/V/multi bits); HARVEST_QUEUE BGANIM-UV/I +
    BGCHK-channel-bit items resolved. Spec cases 1912 → 1942. Harvest TSV `harness/harvest/out/bganim.tsv`,
    case file `harness/harvest/bganim_cases.txt`.
- **Progress (2026-06-24, run 54):** **SPANIM** promoted `disassembled` → **hw_verified** (Sprites
  21/27 → **22/27**; coverage 74.2% → **74.6%** hw_verified, 184 → 185) — the sprite analog of
  run 53's BGANIM, and it caught a **real sb-core bug**. SPANIM's channel-activation contract is
  fully observable headless by reading **SPCHK** (the running-channel mask, hw_verified; flag set at
  the SPANIM call, no VSYNC); the interpolated transform stays O-T6-pending. Harvested against real
  SB 3.6.0 (Azahar) via `run_case.py batch` (`harness/harvest/out/spanim.tsv`, 30 cases + 2 extra,
  0 mismatch / 0 capture failures).
  - **Sprite SPCHK layout is the full CONTIGUOUS 8 channels = `1<<channel` = the docs' #CHK* bits**
    (XY 1, Z 2, UV 4, I 8, R 16, S 32, C 64, V 128) — UNLIKE BG's compacted layout (run 53). Confirmed
    in the disassembly: SPANIM's per-channel switch (jump table @0x142120) ORs CONTIGUOUS constants into
    the slot flags — XY `orr #0x20000` @0x142188 (bit 17), Z `#0x40000`, UV `#0x80000`, I `#0x100000`,
    R `#0x200000`, S `#0x400000`, C `#0x800000`, V `#0x1000000` (bit 24) — and SPCHK returns
    `(flags>>17)&0xFF`. Numeric AND string targets, and the relative `+8`/trailing-`+` forms (the +8 bit
    masked by `and r0,r0,#0x7`), all set the same bit; multiple SPANIMs OR (Z+R=18, XY+V=129, UV+I=12);
    sprites are per-slot independent. **sb-core already matched** (`SpriteState::anim_status` emits
    `1<<ch`) — no fix needed for the bits, in contrast to BG.
  - **Real bug caught — array TARGET errnum differs from BGANIM.** A whole-array target
    (`DIM AR[2]:SPANIM 0,AR,10,0,0`) raises **errnum 4** on real SB, whereas BGANIM gives errnum 8 for
    the identical shape (run 53). sb-core routed both through the shared `parse_target`, whose
    non-number/non-string arm returned errnum 8 — wrong for SPANIM. **Fixed** `do_spanim` (vm.rs) to map
    that type-mismatch to errnum 4 (the array never reaches SPANIM's keyframe-data type check), leaving
    BGANIM's errnum 8 intact. The genuine errnum-8 path is a non-numeric KEYFRAME value
    (`SPANIM 0,"XY",10,"S",5` → 8, also harvested).
  - **Guards confirmed:** before-SPSET / too-few-args / unknown-string "ZZ" / negative-target /
    return-context → errnum 4; mgmt 512 / -1 → errnum 10; zero-time keyframe → errnum 40. All errline 1.
    Corpus sweep (verified word-boundary rg) added a `community` note: mgmt is any integer EXPRESSION
    (`SPANIM SPVAR(N,3),…` / `SPHITSP(…)` / `RND(…)` / arith array indices) — no new error shape.
  - `spanim.yaml` 7 → 30 inline cases (confidence flipped to `hw_verified`), error table corrected
    (errnum 8 is keyframe-data-only; array target is errnum 4). Spec cases 1942 → 1961. Harvest TSV
    `harness/harvest/out/spanim.tsv`, case file `harness/harvest/spanim_cases.txt`.
- **Progress (2026-06-24, run 55):** **SPCOLVEC** promoted `disassembled` → **hw_verified** (Sprites
  22/27 → **23/27**; coverage 74.6% → **75.0%** hw_verified, 185 → 186) — and it drove a real sb-core
  feature: **true swept collision in sb-render**. SPCOLVEC just stores a per-frame velocity vector; its
  whole observable contract is reachable headless by reading the velocity back through **SPHITINFO**
  (hw_verified) after a swept **SPHITSP** (hw_verified) collision — no framebuffer needed. Harvested
  against real SB 3.6.0 (Azahar) via `run_case.py batch` (`harness/harvest/out/spcolvec.tsv`, 21 cases,
  0 mismatch / 0 capture failures).
  - **Velocity round-trip (already-overlapping pair, TM=0):** two 16×16 sprites, `SPCOLVEC 0,3,-2` /
    `SPCOLVEC 1,5,7` → `SPHITSP(0)=1`; `SPHITINFO OUT TM,X1,Y1,VX1,VY1,X2,Y2,VX2,VY2` = TM 0, X1/Y1
    100/100, **VX1/VY1 3/-2**, X2/Y2 108/100, **VX2/VY2 5/7** (object 1 = tested sprite, object 2 = hit
    sprite). Float/negative preserved (`SPCOLVEC 0,-1.5,2.5` → VX1 -1.5, VY1 2.5). A sprite never given
    SPCOLVEC, and the auto-calc 1-arg `SPCOLVEC 0` on a stationary sprite, both → 0,0.
  - **Real swept collision confirmed + IMPLEMENTED (the headline).** Real SB does TRUE swept (moving-
    quadrangle) detection: two sprites NOT overlapping at frame start (100,100 and 130,100, a **14px
    gap**) DO collide when sprite 0 carries `SPCOLVEC 0,20,0` — `SPHITSP(0)=1`, **TM = 14/20 = 0.7** (the
    edge-touch instant), and SPHITINFO reconstructs the collision coordinate as **position + velocity·TM**
    (X1 = 100 + 20·0.7 = **114**). A velocity pointing away (`SPCOLVEC 0,-20,0`), or the pair with no
    velocity, → -1. sb-core had been STATIC-AABB only (HitInfo.time always 0); added `swept_toi`
    (slab-method TOI on the relative SPCOLVEC velocity over the unit frame) to `sb-render` SPHITSP/SPHITRC,
    and made `sphitinfo` reconstruct coords as pos+vel·time (matching the disasm `vmla.f32`). vel-0
    reduces exactly to the old static behavior, so all prior collision tests still pass.
  - `spcolvec.yaml` 4 → 12 inline cases (confidence `hw_verified`); added the swept finding to
    `sphitinfo.yaml` (+swept-coord test) and `sphitsp.yaml` (+swept-approach test); 1 new sb-render unit
    test (`swept_collision_detects_approach_and_reports_time`). Resolves the queued SPHITINFO swept-
    coordinate + SPHITSP-swept-with-SPCOLVEC items (SPHITRC swept-value harvest still queued). Harvest TSV
    `harness/harvest/out/spcolvec.tsv`, case file `harness/harvest/spcolvec_cases.txt`.

### M7-T3 — Exact error strings
- **Files:** `crates/sb-core/src/error.rs`, spec `errors`.
- **Approach:** reconcile each error's message + number against the strings in the
  disassembly (`0x1E965C`+) and oracle behavior (capitalization/punctuation). → O-T5
- **Acceptance:** error messages match the binary byte-for-byte where SB surfaces them;
  errnum assignments verified.

### M7-T4 — Float formatting
- **Files:** `crates/sb-core/src/builtins/{math,string}.rs` (STR$/PRINT/FORMAT$/VAL).
- **Approach:** reverse-engineer SB's exact double→string algorithm from the disassembly
  (the `otya_test` `VAL(STR$(RNDF))==0.836095` shows ~6 sig figs; pin rounding, exponent
  threshold, trailing-zero rules). Apply to PRINT/STR$/FORMAT$. → disassembly
- **Acceptance:** a broad table of float→string cases matches the oracle exactly; the
  M1 "best-effort" formatter specs flip to `hw_verified`.
- **Progress (DONE 2026-06-23):** The disassembly shows SB uses TWO different doube→string
  formatters, both literal C `sprintf` calls: **STR$** = `%g` at 6 sig figs (handler
  @0x1eb2a8, fmt "%g" @0x1eb4a8), **PRINT** = `%.8f` then trailing-zero/bare-dot trim
  (handler @0x180a50, fmt "%.8f" @0x180b0c, trim loop @0x180a8c) — fixed, never exponential.
  `builtins::format_g` already matched `%g` (verified exact against a 2000-case bit-exact
  `%.6g` sweep); the bug was PRINT reusing it. Added `format_print_number`/`format_fixed8`
  (`%.8f`+trim) and routed `format_print_item` through it. Both keep signed zero ("-0").
  hw_verified PRINT values harvested via a `PRINT`+`CHKCHR` console read-back (e.g. PRINT
  12345678.0="12345678" where STR$="1.23457e+07"; PRINT 0.00001="0.00001"; PRINT 1/3=
  "0.33333333"; PRINT -0.0="-0"). Updated 24 math-spec test cases (they expected the STR$
  `%g` value under a bare `PRINT`), added broad %g/%.8f conformance tables to str.yaml &
  print.yaml + Rust unit tables. (E-notation source literals like `1E100` are a separate
  parser gap — already queued, covered here by a unit-test instead.)

### M7-T5 — Overflow/precision + performance
- **Files:** core hot paths; `vm.rs`.
- **Approach:** verify i32 overflow/wrap + int↔double promotion corners (MAX/MIN overflow,
  shifts, DIV/MOD sign rules) against the oracle. Profile the VM; optimize dispatch/value
  representation without breaking determinism. Check wasm perf.
- **Acceptance:** overflow/precision cases match oracle; benchmark a heavy program at
  acceptable fps native + wasm; no determinism regressions.

## Milestone verification
```bash
cargo test --workspace
cargo run -p sb-spec --bin sb-spec-coverage     # hw_verified share at target
# offline (maintainer): python3 harness/harvest/harvest.py  (refresh fixtures, open PR)
```

## Risks / open questions
- Some behaviors may be emulator-faithful but not hardware-faithful; cross-check
  suspicious cases against real-hardware run logs where possible.
- Float-format RE is the highest-effort numeric task; budget accordingly.
